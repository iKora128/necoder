# Changelog

[Keep a Changelog](https://keepachangelog.com/) 形式 / [Semantic Versioning](https://semver.org/)。
リリース手順は [`docs/RELEASE.md`](docs/RELEASE.md)（タグを切ると CI が署名済み .dmg を Releases へ添付する）。

## [Unreleased]

### Changed

- **Fleet の既定を、系譜グラフ＋セルのグリッドに戻しました**。0.1.15 で既定に据えた「作業」タブ（worktree を列に並べる机）は、実際に毎日使ってみると今までの編隊グリッドより取り回しが悪く、既定から外して中央タブの 1 つ（任意の別表示）に降格しています。中央タブの「作業」を選べば従来どおり開きます。
- **セルを足す ＋ が 3 つのボタンになりました**（＋ ACP / ＋ Terminal / ＋ Worktree）。グリッド下部に常時出ていて、押した先が「いまどこか」も隣に出ます（プロジェクト ⎇ ブランチ・ホバーで実パス）。
  - **＋ ACP は会話を独立させます**。同じ作業ディレクトリに ACP セルをいくつでも並べられ、既存の会話を置き換えません（0.1.15 までの ＋ は必ず新しい worktree を切るか、同じパネルにタブを足すかのどちらかでした）。
  - **＋ Terminal も同じ**で、選んだ作業ディレクトリに独立したシェルのセルが増えます。
  - **別ブランチで並走したいときだけ ＋ Worktree**。既存ブランチ・既存 worktree・新しいブランチ名から選びます。
- **元の作業ディレクトリ（`main` 側）もセルとして並ぶようになりました**。編隊の左側一覧（herd）も既定で畳まず全部見せます。Git の統合先を守るのは Git 側の gate の仕事で、画面から消すことではないためです。
- **グリッドの 8 枚上限を撤廃しました**。3 枚は横一列、4 枚以降はグリッドに積み、入りきらない行はスクロールします。セルを閉じても中身（会話・PTY）は生きたままで、一覧から戻せば同じ実体が出ます。配置を変えても拡大しても ACP と PTY は作り直しません。

### Fixed

- **transcript をクリックすると落ちることがあったのを直しました**（0.1.15 で混入）。会話が画面に収まらない長さになると、necoder は見えている範囲だけを描き、画面外の発言は高さを測るためだけに描いて捨てます。クリック位置の判定がこの「捨てた分」まで対象にしていたため、画面上の位置を持たないものを踏んだ瞬間に落ちていました。あわせて、どこを押しても一番下の発言の先頭が選ばれてしまう／コピーが上下逆に繋がる、という選択の取り違えも直っています。
- **入力欄にカーソルがある状態で Esc を押してもターンが止まらなかったのを直しました**。エディタ側が Esc を受け取って「複数カーソルを畳む」役をしており、畳むものが無いときも Esc をそこで握り潰していたため、中断が Agent パネルまで届かず停止ボタンからしか止められませんでした。スレッド名の変更中と候補メニューを開いている間は、これまでどおり Esc がそれを閉じるだけで、走っているターンは止めません。
- **閉じたスレッドタブが再起動のたびに復活しなくなりました**。タブの × とパネル内 ⌘W で閉じたスレッドは台帳（DB）でアーカイブされておらず、起動時の復元が「アーカイブされていないスレッドを全部開く」ため、閉じたはずのタブがそろって戻っていました。閉じる経路を 1 本に統合して修正しています。あわせて、⌘⇧T や履歴から開き直したスレッドが次回起動で消える逆側の取りこぼしも直しました（アーカイブを解除するようにしました）。すでに溜まっている行は次の起動で一度だけ出てくるので、その場で閉じ直せば以後は残りません。

## [0.1.15] - 2026-09-11

MCP サーバをスレッドから使えるようにし、起動直後からモデルを選べるようにしました。worktree を横に並べて見る「作業」タブ、PDF タブ、通知音の作り分けを追加し、スマホ連携のQRが必ず失敗していた不具合（配布ビルドで管制 IPC が一度も起動していなかった）を根治しています。

### Added

- **MCP サーバをスレッドに渡せるようになりました**。ACP では「どの MCP サーバに繋ぐか」を決めるのはエディタ側の仕事で、necoder はこれまで 1 つも渡していませんでした。Codex CLI や Claude Code に登録したのに necoder のスレッドからは `not installed or connected` になっていたのはこのためです。
  - `settings.json` の `mcp_servers.<名前>` に書いたものを渡します（`command` = stdio / `url` = http・sse。ヘッダと環境変数で `${VAR}` を展開）。
  - `~/.codex/config.toml` / `~/.claude.json` / `~/.cursor/mcp.json` に登録済みのサーバも**一覧に出ます**。ただし**既定はオフ** — 他のツールの設定を根拠に勝手に子プロセスを起こしたり、課金されるリモートへ繋いだりしないためです（設定画面で1回オンにするだけ・`docs/DECISIONS.md` §8）。
  - 設定画面に「MCP サーバ」セクション（出所ラベル + 伝送方式 + トグル）。変更は次に開くスレッドから効きます。
  - 渡せなかったサーバは黙って落とさず、transcript に理由を1行出します（広告の無い伝送方式・リモートセッションでの local stdio・未設定の `${VAR}`）。
- **起動直後にモデルを選べるようになりました**。アクティブなスレッド1本だけ、送信を待たずに ACP セッションを先に張ります（500ms 後）。モデル・思考量・権限モードの一覧が最初の送信前から出て、押して選べます。設定「エージェントを先に起動」（`agent_prewarm`・既定オン）で切れます。
  - 立てたまま一度も使われないセッションは**2本まで**。タブを流し見しても、見た枚数ぶんプロセスが増えることはありません（未使用セッションは実測で約16MB。古い順に畳み、戻れば `session/load` で続きに戻ります）。
- **Fleet に「作業」タブ**。リポジトリ1つを机として開き、worktree を列に並べて同時に見られます（`main` も1つの worktree として同じ列に置けます）。
  - 左の**作業ツリーは3段**（リポジトリ → worktree → 面）。スレッド行をクリックするとその列の Agent へ寄って切り替わります＝ツリーがそのままスレッド切替器です。
  - ペイン内のタブ行は1本。**タブの向きはペイン単位**で上／左を選べます（縦タブのとき識別色は下線ではなく左バーへ）。既定は設定 `work_tabs_position`。
  - 下ドックのターミナルを作業面へ移しても**同じ PTY が動き続けます**（往復可能）。列を閉じてもセッションは生き、次に開くとタブ配置ごと戻ります。配置はウィンドウのセッションに保存されます。
- **PDF をタブで開けるようになりました**。自前のレンダラは持たず、OS のビューア（macOS = PDFKit / Windows = WebView2）に描かせます。Remote SSH 越しの PDF はローカルに一時複製して表示し、**タブを閉じると複製を消します**（リモートの中身を手元に残しません）。Linux はフォールバック表示（右クリック →「既定アプリで開く」）。
- **エージェントの「選択肢つき質問」に答えられるようになりました**（AskUserQuestion）。これまではフォームごと拒否していたため、エージェント側からは「ユーザーが答えなかった」ように見えていました。**複数選択の質問にも対応**（押すたびにトグルし、「これで回答」で確定）。Remote PWA も複数選択を受け付けます。
- **通知音が2種類になりました**。ターンが終わったときと、承認・質問で**あなたの入力を待って止まっている**ときとで、違う音が鳴ります。耳だけで「終わった」と「呼ばれている」を区別できます。音は necoder 同梱の猫の鳴き声（合成・`scripts/gen-chime.py` で生成）です。
  - 入力待ちの音は、**そのスレッドが見えていないとき**に鳴ります（他のアプリにいる間だけでなく、同じウィンドウで別のタブを見ているときも）。目の前で止まったときは鳴りません。自動承認で素通りした要求とミュート中は鳴りません。

### Changed

- 設定の「完了音」がオン/オフから**音の選択**になりました（にゃー / システム音 / オフ）。「入力待ちの音」も同じように選べます。`settings.json` に音声ファイルのパスを書けば、自分で用意した音も鳴らせます（キーは `sound_done` / `sound_waiting`。旧 `completion_sound` は廃止）。
- transcript の heredoc 畳み込みを、`python3 - <<'PY'` のように**標準入力へ流し込む実行**まで広げました。走ったコマンドは見出しに残り、本文だけが展開領域に入ります。heredoc の後続コマンドも見出しに残ります。
- 作業タブに埋め込んだ Agent パネルは chrome を畳みます（自前のスレッドタブ行を描かず、状態・トークンのメタ行を1行に）。**トークン表示は畳みません**。

### Fixed

- **配布ビルドで管制 IPC（`~/.necoder/gui.sock`）が一度も起動していませんでした**。呼び出しが開発用ブロック（`#[cfg(debug_assertions)]`）の中にあったためで、スマホ連携の接続用QRは必ず失敗していました。`ne` コマンドも起動中のウィンドウを見つけられず、毎回 LaunchServices 経由で開いていました。
- QRの発行に失敗したとき、原因に関係なく同じ文言（Node.js のバージョンなど）が出ていました。原因ごとに異なる案内を表示します（未初期化・端末数の上限・ホスト未同梱・中継サーバ・タイムアウトなど）。
- GUI が socket を見失っても復帰できるようになりました（30秒ごとに持ち主を確認して張り直します）。
- **起動直後にモデル名が「—」のままだった**（0.1.14 の退行）。自分でモデルを選んだことがないと、最初の送信までピルが空でした。記録した最後のモデルから出し、先張りのセッションが広告を返したら表示名に引き直します。
- **閉じたタブが再起動で復活する**残り2経路を塞ぎました。①開けなかったプロジェクト（消えた worktree など）が1つ混ざると、それ以降の全プロジェクトが**隣のプロジェクトのタブ列**を受け取っていた ②ウィンドウを閉じると直前の保存が捨てられ、「タブを閉じて即 ×」が無かったことになっていた（自前タイトルバーの × ＝ Windows / Linux）。
- **HTML プレビューを開くと ACP で日本語が打てなくなる**。ネイティブの WebView が OS のキーボードフォーカスを握ったままで、IME の変換セッションがそちらに付いていました（変換を介さないキーは効くので、キーマップだけ動いて見えていました）。プレビューの外をクリックした時点でフォーカスを取り返します。PDF タブも同じ経路です。
- **PDF をクリックしても何も起きなかった**。テキストとして読もうとして静かに失敗していました。
- **AI 全画面のあいだ、設定タブや他のタブが開けなかった**（開いても中央が Agent のままで何も起きないように見えていました）。ファイルを開く・⌘, ・レールの ⚙ など、エディタを前に出す操作で全画面を畳みます。
- **上部に固定された「いまの問い」が長いとエージェントの回答を押し出していました**。複数行の問いは先頭行だけに畳み、▸ で展開できます。展開しても高さは頭打ちで、あふれは帯の中でスクロールします。
- 設定画面で副題が長い行（MCP の stdio コマンドなど）がトグルを幅0まで潰していました。
- Remote SSH で再接続した直後の初回のエージェント探索が落ちて `npx` 経路に倒れる問題を、読み取り専用コマンドの再送で塞ぎました。

### 利用上の注意

- MCP の変更は**次に開くスレッドから**効きます（実行中のセッションは繋ぎ直しません）。他のツールから発見したサーバは既定オフなので、使いたいものは設定画面で一度オンにしてください。
- 通知音は現在 macOS のみ鳴ります（Windows は W フェーズで対応）。`completion_sound` を設定していた場合は `sound_done` へ書き直してください（旧キーは無視されます）。
- PDF タブは macOS / Windows のみです（OS のビューアを使うため）。
- エージェントの先張りにより、タブを開いて見るだけでもセッションが最大2本立ちます（未使用は1本あたり約16MB）。切りたい場合は設定「エージェントを先に起動」をオフにしてください。

## [0.1.14] - 2026-09-10

スマホから既存の作業を操作できる Remote PWA を追加しました。SSH再接続の修正に加え、認証切れからの復帰、モデル設定の保持、長文入力の見やすさを改善しています。

### Added
- **設定画面からスマホ連携のQRを発行**。歯車 → スマホ連携でQR表示・URLコピー・再発行に対応し、5分後にQRを自動で隠す。
- **スマホ向け Remote PWA**。`control.necoder.com` で Mac / Windows の既存 ACP スレッドを閲覧・操作。
  QR ペアリング、端末間暗号化、今回だけの承認・拒否、選択式質問、Git diff、通知、切断からの再同期に対応。
  `ne remote` で接続・端末失効を管理する（Node.js 22+ が必要）。セットアップと制約は `relay/README.md`
- **長いユーザー入力を transcript で折り畳む**。12 行（または 1000 バイト）を超える入力は既定で先頭 8 行だけ見せ、
  本文下の `▸ 全 N 行を表示` チップか、エントリ右上の折り畳みボタンで全文へ切り替わる。貼り付けたログや仕様で
  スレッドが自分の入力だけで埋まり、エージェントの応答が画面外へ押し出されるのをやめる
- **エージェントの認証切れを専用カードで案内**。一般的な Internal error と分けて、再ログインに使うコマンドと「再接続して再送」「閉じる」を表示。認証切れ中はキューの自動送信を止める

### Changed
- **モデル・思考量・権限モードの記憶をACPの値IDに統一**。エージェントごとの `agent_config_defaults` に保存し、表示名の表記差で既定モデルへ戻る問題を防ぐ。復元したスレッドにも保存済みの選択を適用
- **Mac / Windowsの配布物にRemoteホストを同梱**。Windowsは名前付きパイプとユーザー限定ACLを使用。QR発行は背景で実行し、設定画面の操作を止めない
- **transcript エントリの操作ボタンを大きく**。コピー `⧉` は 22px 角 → **28px 角**（グリフ 12.5px → 15px・hover で背景も変わる）。
  折り畳みボタンはその左に並ぶ

### Fixed
- **Remote SSH の再接続が「再開」からも送信からも永久に失敗する**。切断後の master 張り直しが、remote に残った
  前回の `ne` gateway socket（`/tmp/necoder-cli-….sock`）で `-R` の bind に落ち、`ExitOnForwardFailure=yes`
  が master ごと exit 255 にしていた（client 側の `StreamLocalBindUnlink` は `-R` に効かない）。端末を
  開き直したときだけ standalone session が `-R` 無しの master になって回復していたのはこのため。
  master は `-R` 無しで張り、gateway の転送は master 経由で古い socket を消してから `ssh -O forward` で
  後付けする。転送の失敗は `ne` が使えないだけに留め、接続は道連れにしない
- **SSH 側の失敗が「claude-agent-acp が見つかりません」と誤報される**。remote でのエージェント探索は
  「探せたが無い」と「探せなかった（再接続失敗など）」を分け、後者は原因文をそのままエラー行に出す
- **再接続直後の 1 発目の探索が落ちて npx 経路へ倒れる**。`command -v` は読み取り専用なので再送可で送る
  （`Host::run_command_retry_safe`）
- **認証切れで会話の文脈が失われる問題を抑制**。認証エラーだけではセッションを破棄せず、エージェントによる再開を利用。「session has ended」で再開不能になった場合にだけ送信路を作り直す

### 利用上の注意
- Remoteは **Node.js 22以降とホストの初回接続設定** が必要です。アプリのインストールだけで全ユーザーが共有relayへ接続できるわけではありません。[セットアップ手順](https://github.com/iKora128/necoder/blob/v0.1.14/relay/README.md)
- 発行時に開いている全プロジェクトを共有し、スマホでプロジェクト・スレッドを切り替えて個別に操作します。PCのスリープ・電源断・回線断中は操作できません。未確認の命令や古い承認を自動再送しません
- QRは5分間有効です。カメラで読み取ったリンクは通常のブラウザで開きます。ホーム画面のPWAに接続情報が引き継がれない場合は、新しいペアリングURLをPWAに貼り付けてください。iPhoneの通知にはホーム画面への追加と許可が必要です
- 古い `agent_defaults` / `default_model` / `default_effort` 形式は新しい設定には引き継がれません。更新後にモデル等の選択を確認し、必要なら一度選び直してください
- Chromium / WebKit、本番relay、MacのGUI IPCを検証済み。実機iPhoneの通知とWindows実機のGUI連携は別途確認が必要です。Remoteの独立したセキュリティ監査は未実施です

## [0.1.13] - 2026-09-08

### Added
- **statusbar の SSH チップが接続状態を映す**。「SSH」の色が接続済み / 接続中 / 切断 / 未接続を表し、
  切断・未接続のときは「再接続」「接続」の文字チップから今すぐ張り直せる（heartbeat が到達不能で
  最大 60 秒バックオフしている間を待たない）。状態の変化は host からの購読で即時に反映される
- **切れたエージェントのセッションを引き継いで再開**。SSH 切断などでエージェントとの接続が閉じたスレッドは
  composer の直上に「セッションが切れました」と「再開」チップを出し、次の送信（または再開）で
  エージェントを立ち上げ直す。エージェントが `loadSession` を広告していれば前回のセッション id で
  `session/load` し、同じ会話の続きから話せる（引き継げたか/引き継げなかったかは一言で表示）。
  セッション id はスレッドごとに DB へ残るので、アプリを再起動したあとの最初の送信でも引き継ぐ

### Changed
- **UI スレッドからの remote I/O 検出を、debug ビルドでは panic に**（開発者向け）。release は
  従来どおり警告して続行。既知の違反を潰し終えたので、新しい違反をテストと開発中に即座に捕まえる
- **リモートプロジェクトの色をホスト単位からプロジェクト単位に**。同じホストのプロジェクトが
  全部同色になっていたのをやめ、ローカルと同じ「1 プロジェクト 1 色」にした（DB の `host_colors`
  は使わなくなる。残っていても無害）

### Fixed
- **Remote SSH の接続中、アイドルでも necoder が 1 コアを使い切る**。ファイル監視の購読が
  入れ替わってチャネルが切れた後、待機が即時エラーになるのを「変更なし」として再試行し続けていた。
  切断時は監視 pump を終了し、古い監視の後始末が新しい購読を解除しないよう世代管理と直列化も加えた
- **SSH が切れたあと、送るたびに「Incoming transport closed」が返り続ける**。エージェントとの
  transport が閉じても「そのターンだけの失敗」としてセッションを維持していたため、以後の送信が
  死んだ接続へ流れて即エラーになっていた。transport 断はセッションごと畳み、待機中の切断も
  次の送信を待たずに検知する（送信して初めて分かる、をやめた）
- **新規スレッドの初回ターンが、ピルの表示と違うモデル/思考量で走る**（Opus を表示したまま応答は
  Fable、など）。遅延起動ではモデル変更の指示が最初の prompt の後ろに並び、ターンが終わるまで
  送られなかった。権限モードと同じく、セッション生成直後・最初の prompt より前にスレッドの
  モデル/思考量をエージェントへ合わせるようにした（広告に無い値は従来どおり広告側を採用）
- **Remote SSH の統合ターミナルで `ne .` が接続先 Mac の necoder を開く**。SSH reverse Unix
  socket で接続元へ戻し、リモートの絶対パスを同じ SSH host の URI として現在のウィンドウへ渡す。
  reverse socket のローカル側には `open` 専用 gateway を置き、管制 IPC の他操作は remote へ公開しない
- **Remote SSH: UI スレッドに残っていた remote 問い合わせを全部背景へ**。プロジェクトをレールへ
  足す（worktree の解決と git の問い合わせ）・explorer から別窓 / 現レールに開く・分割ペイン・
  hunk の巻き戻し・hot exit の復元・`necoder ssh://host/path` 起動が、再接続の要る場面
  （スリープ復帰直後など）でその待ち時間ぶん固まっていた。`ssh://` 引数の起動は復元と同じく
  窓を先に出して接続は背景で追いつく
- **再起動やレールの並び替えでプロジェクトの色が変わる**。自動で配った色を保存しておらず、
  レール上の位置でパレットを巡回していたのが原因。決めた色を necoder.db（`project_colors`・
  local/ホスト鍵 + root パス）に焼き、次に開くときはそれを使う。優先順は
  `.necoder/settings.json` > DB > 未使用のパレット色。手動で選んだ色も同じ表に写す

## [0.1.12] - 2026-09-07

Remote SSH の「スリープ復帰後に固まる」を根治した版。プロジェクト切替・起動・エクスプローラ・
外部変更の照合・端末 / LSP / エージェントの起動が UI スレッドで SSH の往復を待っていた経路を
すべて背景へ移し、切れた接続の検知と張り直しを速くした。併せてエディタのクリック位置のずれと、
Mac 標準の編集キーの不足を直した。

### Added
- **Mac 標準のテキスト編集キーを一式**（エディタと AI の入力欄で共通。ショートカット一覧にも載る）
  - 選択: ⇧⌘← / ⇧⌘→・⇧Home / ⇧End = 行頭 / 行末まで、⇧⌘↑ / ⇧⌘↓ = 文書の先頭 / 末尾まで
  - 削除: ⌘⌫ = 行頭まで、⌃K = 行末まで（行末で押すと次の行と結合）、⌥Delete = 次の単語
  - emacs 風: ⌃N / ⌃P / ⌃F / ⌃B = 上下右左、⌃D = 前方削除、⌃H = Backspace
  - Windows は VSCode の綴り（Ctrl+Shift+Home / End・Ctrl+Delete 等）に落とし、⌘⌫ / ⌃K は
    Ctrl+Shift+Backspace / Ctrl+Shift+Delete。衝突する emacs 風は付けない
- **Remote SSH の実機回帰テスト**（開発者向け）。`scripts/test-remote-ssh-docker.sh` が Linux 用
  remote-server のビルド、Docker 上の SSH サーバ起動、「TCP は生きたまま相手が黙る」切断の注入まで
  面倒を見る。スリープ復帰そのものの形（`docker compose pause`）で、ユーザーが何もしなくても
  自動で張り直して読めるところまでを固定した

### Changed
- **更新の確認を 6 時間ごとに**。従来は起動 10 秒後の 1 回きりで、necoder を開いたままだと
  その後に出た版は開き直すまで見えなかった。経過は壁時計で測るので、スリープを挟んでも
  「寝ていた時間」がちゃんと数えられる。ダウンロード中・再起動待ちの状態は新しい告知で潰さない
- **AI トランスクリプトのツール出力は先頭 100 行 + 末尾 100 行を残す**（中間は「… (N 行省略)」）。
  以前は末尾 24 行だけで、`Read` の頭か `cargo build` の尻のどちらかが必ず落ちていた
- **UI スレッドからの remote I/O を実行時に検出**（開発者向け）。既定は stderr に警告して続行、
  `NECODER_STRICT_MAIN_THREAD_IO=1` で panic、`NECODER_ALLOW_MAIN_THREAD_IO=1` で検査を止める。
  「blocking API は背景で呼ぶ」という規約を、破られたときに捕まえるための仕掛け

### Fixed
- **エディタ: クリック位置とキャレットのずれ**。ヒットテストが描画と別のフォント（OS 既定の
  プロポーショナル）で幅を測っていたため、日本語と ASCII が混ざる markdown の行ではクリック点から
  全角 3 文字ほど離れた位置にキャレットが置かれ、ドラッグ選択・hover・IME の候補位置も同じだけ
  ずれていた。描画に使ったフォントと同じ run 構成（見出し・太字・強調の書体を含む）で測るようにした
- **Remote SSH: プロジェクト切替でアプリが数十秒無反応**。切替ハンドラが UI スレッドから remote host の
  ファイル一覧・タブ復元・変更監視を同期で呼んでおり、スリープ復帰直後のように再接続が要る場面では
  その待ち時間がそのまま操作不能時間になっていた。背景処理に移し、宛先チップは即座に、＋context の
  候補ファイルとタブは読めた順に追いつく
- **Remote SSH: 起動時に接続を待ってウィンドウが出ない**。remote のプロジェクトを開いたまま終了すると、
  次の起動で接続（到達できないホストが混じればその待ち時間）が済むまで窓が出なかった。前回保存した
  情報だけでレールとタブを組み立てて先に窓を出し、接続と中身の読み込みは背景で追いつく。
  到達できないホストはトーストで知らせ、操作したときに再接続を試みる
- **Remote SSH: スリープ復帰後の再接続が長引く**。`ssh -O check` は多重化 master の生死しか見ず、
  TCP だけ死んだ master を「生きている」と答えるため、その上に張った接続が黙って固まっていた。
  ハンドシェイクを 10 秒で見切って master ごと張り直し、到達不能なホストへの再試行間隔は
  5 秒から最大 60 秒へ広げる
- **Remote SSH: 切れた接続に気付くまで最大 30 秒**。生存確認（Ping）の待ち時間を 5 秒にし、
  張り直しが決まった時点でその接続に乗っていた要求を即座に失敗させて新しい接続でやり直す
  （以前は要求ごとのタイムアウトまで待っていた）。時間切れでも受信が進んでいれば「混んでいるだけ」と
  みなして切らない（低速回線で大きなファイルを転送中に切って再送を繰り返すのを防ぐ）
- **Remote SSH: 切断中に操作するほど遅くなる**。到達不能なホストへの再接続は 1 回で十数秒かかるのに、
  待たされていた操作がそれぞれ最初からやり直していた。待っている間に起きた失敗はそのまま受け取り、
  後から来た操作（＝ユーザーの再試行）だけが繋ぎに行く
- **Remote SSH: ハンドシェイクが即座に閉じたときに端末を巻き添え**。時間切れではなく即 EOF のときにも
  ControlMaster を作り直し、同じ接続に乗っていた端末を切っていた。remote-server 側の起動失敗では
  master を触らない
- **Remote SSH: エクスプローラ操作のたびに固まる**。フォルダの展開・折り畳み・リネーム・変更監視の
  イベント・手動更新（↻）・ブランチ切替のたびに SSH の往復ぶん止まっていた。ツリーの読み取りを背景へ
  移し、折り畳みと読み込み済みフォルダの展開は即座に映る
- **Remote SSH: 外部変更の照合・端末 / LSP / エージェントの起動が UI スレッドで接続を待つ**。
  開いているファイルが外で変わったときの照合と再読込、エージェントのターン開始 / 終了時の HEAD 取得も
  背景へ。端末と LSP の SSH セッションは master が無ければ自分で繋ぐ（`ControlMaster=auto`）ので、
  起動仕様を組む段階ではネットワークに触らない
- **AI トランスクリプト: 畳んだ要約の行数が実際より小さい**。「▸ N 行」の N を丸めた後の行数から
  数えていたため、1000 行の出力が「24 行」と出て、開いても増えなかった。丸める前の行数を出す

## [0.1.11] - 2026-09-04

### Added
- **更新チップに進捗バー**。「⬆ 更新」クリック後は「ダウンロード中 NN% → 署名を検証中 → 差し替え中」と
  文言が進み、下の細いバーが伸びる（About モーダルの 1 行も同じ文言）
- **差し替え済みチップが「⟳ 再起動して vX へ」ボタンに**。クリックで necoder が終了 → 新バージョンで
  自動的に開き直す（従来は手動で再起動する案内のみ）。終了は通常の ⌘Q と同じ経路（未保存
  スナップショットの破棄・窓セッションの整理）

### Changed
- Workspace 切り替えモード中の左レール背景を `bg1` から `bg2` へ明るくし、`⌘{` / `⌘}` の操作対象が
  レールへ移っていることを判別しやすくした
- Claude Code が Bash tool title に載せる `cat > file <<EOF` のファイル全文を、`書き込み file` + 行数へ
  既定で折り畳む（英語 UI は `Write file`）。展開すると拡張子に応じたシンタックスカラーで全文を確認できる

### Fixed
- **再起動時に開いていた Workspace が復元されないことがある**。終了直前の窓セッションを Turso へ
  同期保存し、DB を開けなかったプロセスが Workspace ごとに open を繰り返さないようにした
- statusbar の診断・ターミナル SVG が透明になっていた。GPUI の SVG は親の文字色を継承しないため、
  各 SVG に色を直接指定（同じ問題があったプレビューとエクスプローラ更新アイコンも修正）

## [0.1.10] - 2026-09-03

### Added
- **複数ウィンドウの復元**。起動時に前回開いていた窓を全部戻す（最後に触った窓が前面）。
  ユーザーが閉じた窓は次回戻らず、全部閉じてから終了した場合は最後の窓だけ戻る
- **エクスプローラに更新ボタン**（ヘッダ右端の ↻）。ツリー・git の色・gutter diff を読み直す。
  AI エージェントが作ったファイルなど、監視が取りこぼしたときの明示的な逃げ道
- **statusbar にターミナル切替ボタン**（診断件数の右）。レール / ⌃` と同じ動作で、開いている間は灯る
- **レール = 最後に触った面**。レールのどこかを押した後は ⌘{ ⌘} が前後のプロジェクト切替になる
  （レール外を押すまで。面が一段明るくなって宛先が分かる）。トラックパッドで「突いて ⌘}」の動線
- **キーボードでのプロジェクト切替（⌃⌘↑↓ / ⌘1..9）で行き先の名前を中央に約 1 秒フラッシュ**
  表示（プロジェクト名 + ⎇ ブランチ・左にプロジェクト色のバー）
- HTML プレビューで右クリック →「要素を検証」（Web Inspector）が release ビルドでも使える

### Changed
- **窓の状態（開プロジェクト・開タブ）の保存先を `state.json` から necoder.db（`window_sessions`）へ
  移行**。1 窓 1 行で、各窓は自分の行だけを更新する。旧 `state.json` は読まない（残っていても
  無視される・削除して可）
- statusbar の診断件数の記号を文字グリフ（✗ ▲）から Lucide アイコン（circle-x / triangle-alert）に
  変更（フォント差で崩れない）
- Release ページの本文を CHANGELOG から CI が生成するようにした（0.1.3〜0.1.6 の本文も補完）

### Fixed
- **別の窓で閉じたタブが次回起動で復活する**。`state.json` を全窓が丸ごと上書きしていた
  （最後に書いた窓が勝つ）のが原因。上記の移行で解消
- ターミナルから `necoder <path>` で起動 → 終了を繰り返すと、次の素起動で過去の窓が全部開く
  （終了時にその時点で開いていた窓だけを復元対象に残す）
- statusbar の診断アイコンとプレビュー切替の目アイコンが描画されず空白になっていた（アセット表への
  登録漏れ。今後は全アイコンの登録をテストで検査）
- AI 全画面中に ⌘W / ⌘⇧T / ⌃Tab がエディタタブ側へ流れて効かなかった
- HTML プレビューを隠す・破棄する前にキーボードフォーカスを GPUI 側へ返すようにした
  （隠れた WebView がキー入力を吸い続けてショートカットが効かなくなる問題の予防）

## [0.1.9] - 2026-09-02

### Added
- **ACP エージェントの版を公開レジストリから自動解決**。アダプタ（claude-agent-acp 等）の新版が
  出ると、necoder のリリース（再ビルド）を待たずに次のセッション起動から使われる。取得は
  起動 12 秒後に背景で（1 時間キャッシュ・ACP プロジェクトの公式 CDN・オフライン時は
  組み込みカタログへフォールバック）。走行中のスレッドには影響しない
- `settings.json` に `agent_servers.<id>`: エージェント起動の上書き。
  `{"type":"custom","command":…}` で起動コマンドを丸ごと差し替え（版の解決もしない＝完全に
  ユーザー主導）、`{"type":"registry","env":{…}}` で環境変数だけ追加

### Changed
- 組み込みカタログの既定版を更新: claude-agent-acp 0.66.0 → 0.73.0 / codex-acp 1.1.14 → 1.8.0 /
  copilot 1.0.70 → 1.0.82 / qwen-code 0.19.9 → 0.22.3（今後はレジストリが先に進むため、
  カタログはオフライン時の土台）
- AI transcript のエントリコピーボタンを 22px 角に拡大（枠 1px 付き。従来の 18×14px は
  狙って押せなかった）

## [0.1.8] - 2026-08-31

### Added
- ローカル HTML プレビュー: `.html` タブをパンくずの「プレビュー」または ⌘⇧V で source ⇄ preview
  切替。macOS は WKWebView / Windows は WebView2 を**プレビュー初回表示まで遅延生成**し、
  Chromium/ブラウザエンジンは同梱しない（Linux は非対応表示）。↻ で再読込・保存成功で自動再読込
- 非表示のまま放置した HTML プレビューの WebView を自動破棄してメモリを回収
  （設定 `html_preview_evict_minutes`・既定 15 分・`0` で無効。再表示時は遅延再生成）
- Markdown の GFM 表を描画（`.md` 整形プレビューと AI transcript の両方）。列幅は内容比 + 短い列の
  最小幅保証、`|:---:|` の左/中央/右そろえ、セル内の `コード`/**強調** などインライン装飾に対応
- レールのプロジェクトアイコンに画像対応: `.necoder/icon.png`（jpg/jpeg/webp も可）を置くだけで
  絵文字/頭文字の代わりに表示（settings の `icon` にパス指定も可・プロジェクト色のリングは維持）
- Fleet の `+ Task` 前にデフォルトブランチを upstream へ自動早送り（clean かつ behind のみ
  ff-only。オフライン・upstream 無し・dirty・diverged は黙ってスキップして現 HEAD から作成）

### Fixed
- `ne .` / `ne <file>` が起動中の necoder に届かず新インスタンスが立つことがあったのを根治
  （IPC socket の自己修復 + `open -a` の書類渡しへ変更 + 単一インスタンスの防波堤。
  Dock にアイコンが複数並ぶ症状もこれで解消）
- AI エージェントタブを全画面化すると composer の折り返しが崩れ文字がちらつくのを根治
  （折り返し高さの変化を親へ通知 + スクロール位置の再クランプ）

## [0.1.7] - 2026-08-30

### Added
- ターミナル用 `ne` コマンド（VSCode の `code` 相当）。`ne .` / `ne <file>` は起動中の
  necoder があればそのウィンドウで開き（IPC 転送・前面化）、無ければアプリを起動。
  `ssh://` と既存の `config` / `fleet` / `mcp` サブコマンドは素通し。導入は
  設定 > コマンドライン（macOS の認証ダイアログ）か `necoder install-cli`、削除も同じ場所から
- About モーダル（メニュー「necoder について」）とメニュー/パレットの「アップデートを確認…」。
  手動確認は 新版あり/最新です/確認失敗 を言い分ける
- 配布物にライセンス通知を同梱（`THIRD_PARTY_NOTICES.md` + `licenses/`・macOS/Windows 両方）

### Changed
- composer 入力欄が内容に合わせて自動で伸びる（既定高〜420px・上限超えは内部スクロール）。
  長文を貼ると入力位置が見えなくなる問題の根治
- エディタ / ACP transcript / ターミナルで、選択ドラッグをビュー外へ引っ張ると自動スクロール
  して選択が伸びる

### Fixed
- AI エージェントの API ストリーミング切断（「Connection closed mid-response」）でスレッドの
  セッションごと落ちていたのを、そのターンだけの失敗に変更（同じスレッドで再送できる）

## [0.1.6] - 2026-08-28

### Added
- ターミナルのスクロールバック: ホイール / トラックパッドで過去の出力を遡れる（従来は 10k 行の
  履歴を持ちながら常に最下段へ固定されていた）。代替画面（less / vim）では矢印キーを送出し、
  入力・ペースト・IME 確定で最下段へ自動復帰
- エクスプローラ: Finder 風のドラッグ&ドロップ移動（ツリー / カラム / アイコンの 3 ビュー対応）と、
  Finder からのドロップでコピー追加（再帰・既存ファイルは上書きしない安全側）
- ⌘P: 空のローカルプロジェクトで「新規ファイル… / 新規フォルダ…」を提示
- composer: エージェント選択ピルとメニューにブランドバッジ
- 設定画面: エージェントの導入 / ログイン完了を数秒で検知して「利用可能」へ自動反映
  （設定画面を開き直さなくてよい）

### Changed
- スレッド履歴はアクティブプロジェクトのスレッドだけを表示する

### Fixed
- **checkpoint の巻き戻しがファイルを断片で上書きして壊すことがあった**。スナップショットが
  Edit ツールの `oldText`（置換した hunk だけ）をファイル全文として保存していたため、巻き戻すと
  ファイルが hunk だけになる（実害: 8,700 行のファイルが 5 行に）。常にディスクから全文を読んで
  保存するよう修正
- bypass permissions が新規タブの初回ターンと、ターン途中の切り替えで効かなかった。
  セッション生成直後にモードを適用し、承認待ちカードが出ている状態で bypass へ切り替えると
  その場で許可して続行する
- LP: 英語版ヒーローキャプチャ / ダークモードでエージェントアイコンが背景に沈む問題 / OGP カード

## [0.1.5] - 2026-08-27

### Added
- 画像ビューア: png / jpg / gif / webp 等をタブで開ける（編集・保存・⌘F・LSP は画像タブでは無効。
  ディスク上の更新は自動で再読込）
- Markdown 整形プレビュー（⌘⇧V）で画像を描画（相対パスは `.md` の親ディレクトリ基準。
  読めない場合は `🖼 alt` テキストへ劣化）。パンくず右端に整形プレビューのトグルボタン（👁）
- Windows のアップデート導線: 新版が出ると「⬆ vX.Y.Z を入手」チップを表示し、クリックで
  Release ページを既定ブラウザで開く（zip 付きリリースのみ対象）。起動後の確認待ちを 90 秒 →
  10 秒に短縮
- LP のダウンロード導線を「Mac / Windows 版」に変更し、対応環境に Windows 10+（x64）を追記

### Changed
- ⌃Tab / ⌘⌥← → は ⌘W と同じ振り分けに: エディタ面が最後ならファイルタブ、Agent 面なら
  AI スレッドを切り替える
- composer の既定高を 104px（約 4 行）→ 86px（約 3 行）に

### Fixed
- 実行中ツールの表示が複数行の Bash コマンドで縦に伸びていた（先頭行 100 文字に丸める）
- git blame の「未コミット」表示を i18n 化（英語ロケールに日本語が出ていた）

## [0.1.4] - 2026-08-26

### Added
- キーボードショートカット一覧（⌘K ⌘S）: 既定 keymap から自動生成する参照専用オーバーレイ。
  エディタ / AI チャット / 管制 / グローバルのセクション別に動作名とキーを並べ、mac 記号と
  Windows 綴りを出し分ける。Escape / 背景クリックで閉じる。コマンドパレットからも開ける

### Fixed
- **生成中のエージェント本文をドラッグ選択・⌘C できなかった**。生成中の末尾本文を別ビューで
  描いていたのをやめ、完了後と同じ選択可能な描画パスへ一本化（タイプライタ演出はそのまま）

## [0.1.3] - 2026-08-26

### Added
- 同梱テーマ 5 種: Solarized Dark / Solarized Light / Gruvbox Dark / Catppuccin Mocha /
  High Contrast Dark。`settings.json` の `theme` には id（`solarized-dark`）と表示名のどちらでも書ける
- 設定画面に「外観」: テーマ一覧をチップで表示しクリックで即適用、「settings.json を開く」ボタン
  （パレット「設定: settings.json を開く」も追加）。`settings.json` の `theme` を手で書き換えても
  即反映
- ACP: Elicitation（選択肢付き質問）に対応。エージェントが出した選択肢をカードで選び
  Accept / Decline を返す（テキスト入力を含むフォームは安全側で Decline）
- ACP: 送信キュー。生成中に Enter しても即送らず積み、ターン完了で自動送信する（うっかり二重ターンを
  撃つ事故を構造的に無くす）。宛先チップ横に「中断して今すぐ」（steer）と取消
- ACP: `initialize` の clientInfo で necoder を名乗る

### Changed
- トランスクリプトのコマンド行（⏺ + 引数）を 1 行に丸め、クリックで展開（結果 ⎿ と同じ作法）

### Fixed
- エクスプローラの深い階層でファイル名がインデントに押し出されて読めなかった（横スクロール対応）
- 「生成中なのにアイドル表示」になることがあった（生成中に積まれた 2 本目の送信で running が
  立たなかった）

## [0.1.2] - 2026-08-25

### Fixed
- **配布物が起動しなかった致命的な不具合を修正**（v0.1.0 / v0.1.1 の `.dmg` と `.zip` が該当）。
  マスコット画像の読み込みが `env!("CARGO_MANIFEST_DIR")` を**実行時のパス**として開いていたため、
  **ビルドした機械以外では起動直後に panic** していた:

  ```text
  mascot asset D:\a\necoder\necoder\crates\agent_panel/assets/mascot\idle.png:
  指定されたパスが見つかりません。 (os error 3)
  ```

  （`D:\a\necoder\necoder` は GitHub Actions ランナーのパス）
  フォント・アイコンと同じく `include_bytes!` でバイナリに埋め込むよう修正した。
  **v0.1.0 / v0.1.1 は使えません。本版へ更新してください。**
- クラッシュログが**二重 panic のとき 1 度目 = 本来の原因を消していた**。起動クロージャ内の
  panic は GPUI の extern "C" 境界を unwind できず 2 度目の panic（cannot unwind）になり、
  同一秒・同一 PID の `crash-<unix秒>-<pid>.log` を上書きしていた（上記の調査で実害）。追記式に変更

### Added
- 上記の再発を機械で止める回帰テスト（`crates/necoder/tests/no_runtime_manifest_paths.rs`）。
  ビルド時のパスを実行時に開いている箇所を全 crate から検出する。**このバグは開発機では
  原理的に再現しない**（そのパスが手元には実在するため）ので、人間の目では防げない
- release CI に**最終成果物側の裏取り**も追加: 配布バイナリに `strings` でビルド機のパス
  （`$GITHUB_WORKSPACE/crates`）が残っていたら fail。併せてリモートサーバ探索の開発用
  fallback（`target/debug/…`）を debug ビルド限定にし、release バイナリからビルド機の
  絶対パスを全廃（ガードの期待値を 0 件にできる）
- `release.yml` の Windows ジョブで VCRUNTIME 依存の検証が実際に動くようになった
  （`Program Files` 側の VS を見ておらず、これまで一度も検証していなかった）

## [0.1.1] - 2026-08-24

### Added
- **Windows ネイティブ対応**（`docs/WINDOWS-PORT.md` の W0〜W6）。`necoder-windows-x64.zip` を配布物に追加:
  - 起動・編集・保存・検索・Git・統合ターミナル（ConPTY + PowerShell）が Windows で動作
  - `paths` crate を新設し、設定/DB/ログの置き場を 1 箇所に集約。Windows は Roaming（設定）と
    Local（DB・ログ）を分ける。**Linux で `~/Library/Application Support/` を作っていた既存バグも解消**
  - 制御 IPC を Unix socket / 名前付きパイプで抽象化
  - Windows 既定 keymap（VSCode 準拠の `Ctrl-` 系）とキー表記の出し分け（`⌘S` ↔ `Ctrl+S`）
  - タイトルバーのキャプションボタン（最小化・最大化・閉じる）
  - CI に `check-windows` を常設（`-D warnings` + `cargo test --workspace`）

### Fixed
- ターミナルのドックが**中身を描画しない**不具合（`.cached()` した view の root は `flex_1()` が
  効かず高さ 0 に潰れる）。mac にも影響していた可能性がある
- 初回起動で設定ディレクトリが無いときにファイル監視が失敗していた（3 プラットフォーム共通）
- git gutter が CRLF の作業ツリーで全行 Modified になり得た問題に回帰テストを追加

### Notes
- Windows 版は**未署名**（初回起動で SmartScreen の警告が出る）
- **アプリ内更新は macOS のみ**。Windows は zip の再ダウンロードで更新する

## [0.1.0] - 2026-08-22

### Added
- 初回公開リリース（v0.1.0）に向けた整備:
  - Finder/Dock からのファイル/フォルダオープン（`CFBundleDocumentTypes` + `on_open_urls` 配線）
  - GUI 起動（Finder/Dock）時のアプリログ `~/Library/Application Support/necoder/logs/`（20 本保持）
  - 依存ライセンス監査（cargo-deny）を CI に常設
  - CLA / CONTRIBUTING / SECURITY / Code of Conduct を整備
- エディタ本体はここまでの M0〜M14 で実装済み（色レール・ACP スレッド・LSP・tree-sitter 多言語・
  Git hunk/blame・統合ターミナル・Remote SSH・マルチエージェント編隊 — 詳細は `docs/ROADMAP.md`）

### Fixed
- リリースのバージョン配線を一本化（`Cargo.toml` を唯一の出所に。タグ不一致は CI が拒否 —
  旧: Info.plist ベタ書きで、上げ忘れると更新チップが無限に出る実害）
- 更新・管制 IPC のエラーメッセージを i18n 化（英語ロケールに日本語エラーが出ていた）

<!-- リリース時: Unreleased を [x.y.z] - YYYY-MM-DD へ繰り上げ、新しい Unreleased 節を上に作る -->
