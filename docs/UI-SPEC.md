# UI-SPEC — mock v0.3 の実装仕様

`mock/index.html` が見た目の**正**。この文書は mock を GPUI 実装向けに数値・状態・振る舞いへ翻訳したもの。
変更したくなったら **mock を先に直してスクリーンショットで確認 → この文書 → 実装** の順。

## 1. デザイントークン

### 1.1 色（dark / light）

| token | dark | light | 用途 |
|---|---|---|---|
| bg0 | `#16181e` | `#f0f1f4` | 窓クローム・ドック |
| bg1 | `#1b1e25` | `#ffffff` | エディタ面・チャット面 |
| bg2 | `#232733` | `#ffffff` | カード・パレット・入力箱 |
| bg3 | `#2b3040` | `#e7eaf0` | hover・**中立の選択面** |
| fg0 | `#d7dae3` | `#333845` | 主文字 |
| fg1 | `#9aa1b2` | `#5c6370` | 副文字 |
| fg2 | `#646b7a` | `#9aa1b2` | ミュート・行番号 |
| border | `#262b36` | `#d8dce3` | 全罫線（1px） |
| ok / warn / err | `#7bc96f` `#e5c07b` `#e06c75` | `#3e9d32` `#bf8803` `#e45649` | 診断・git |
| syn-kw | `#c678dd` | `#a626a4` | キーワード |
| syn-fn | `#61afef` | `#4078f2` | 関数 |
| syn-type | `#e5c07b` | `#c18401` | 型 |
| syn-str | `#98c379` | `#50a14f` | 文字列 |
| syn-num | `#d19a66` | `#986801` | 数値 |
| syn-cmt | `#5f6672` | `#a3a6ae` | コメント（italic） |
| syn-mac | `#56b6c2` | `#0184bc` | マクロ |
| syn-punct | `#abb2bf` | `#383a42` | 記号 |

**テーマ非連動の固定色**: エディタ選択面 `#7d9bd8` 28%透過 / フォルダアイコン `#7d9bd8` 55%（bg3 と mix）/ Claude バレット `#d97757`。

**きせかえ契約**: この表が**テーマの全インターフェース**。ユーザー定義テーマ = このトークンを上書きする JSON 1枚（`~/Library/Application Support/Shirushi/themes/*.json`）。
テーマ（面と文字の配色）とプロジェクト色（識別・Peacock相当、§1.2）は**独立した2軸** — どのテーマでもプロジェクト色は §1.3 の許可リスト位置に流れる。

### 1.2 アイデンティティ色

- **プロジェクト色パレット（自動巡回）**: indigo `#7c8cf8` → teal `#34d3b6` → amber `#f0a24b` → rose `#ef7d9b` → green `#85c46c`
- **アイデンティティ・パレット（ピッカーの正典）** = `theme_core::IDENTITY_PALETTE_HEXES`: 上の巡回5色 + 予約色（Claude バレット `#d97757`・スレッド色・選択面 `#7d9bd8`）と衝突しない厳選色（cyan / chartreuse / violet / magenta / graphite）。**巡回もピッカーもこの1本が出所**
- 優先順: `.shirushi/settings.json` の `color` > **ユーザーの手動選択** > パレット巡回。手動選択の導線 = レール右クリック / **⌘K⌘C** / コマンドパレット「表示: プロジェクトの色を変更」→ 厳選スウォッチ or **任意 hex 入力**
- 任意 hex はエスケープハッチ: §1.3 の流れ先（許可リスト）は変えない。**細い accent としての可読性はユーザー責任**（Peacock と違いコントラスト自動計算はしない）
- **スレッド色パレット（自動巡回）**: `#61afef` → `#e5c07b` → `#c678dd`（プロジェクト色と独立）
- accent-dim = プロジェクト色 16% 透過（パレット選択面・タイル選択輪郭のみ）

### 1.3 色の許可リスト（**ここ以外に色を置いたらレビューで落とす**）

| 色 | 置いてよい場所（全列挙） |
|---|---|
| プロジェクト色 | **窓縁の枠 2px**（アクティブプロジェクト色・Peacock 相当の「窓ごと識別」／リモートで今どのマシンかを一目化）・レール項目の枠/リング・**アクティブなレールのアクティビティアイコン**（explorer/search/git/todos/agent/terminal のうち表示中のもの=プロジェクト色・非アクティブは fg2＝VSCode の Activity Bar 準拠。M10-2）・ピル左縁 3px・アクティブタブ上線 2px・ツリー/カラム選択の左バー 2px・キャレット 2px・タイル選択の薄輪郭・パレット選択面(dim)・拡張バッジ(dim) |
| スレッド色 | スレッドタブの上線+ドット・titlebar beacon・statusbar ドット・トークンメーター・thinking 左縁・msg-user 左縁・composer 枠・宛先チップのドット・送信ボタン |
| 禁止 | titlebar/statusbar のグラデ額縁・選択面の色塗り・フォルダアイコン・ターミナルタブ（v0.2 で廃止済み）・**エージェント状態への色相**（herdr 式の 🔴🟡🔵🟢 は不採用） |

- **エージェント状態は色相を使わない**（§6/§8・herdr 参照だが配色は持ち込まない）: 実行中/承認待ち/完了/待機は、スレッド識別色ドットの**形と動き**（リング・脈動・中空グリフ）で表す。「色は識別に集約」の掟を守るための明示判断。

### 1.4 寸法・密度・タイポグラフィ

- 密度 compact（既定）: 行高 23 / padding 6 / UI 12.5px / code 13px lh1.55。cozy: 27 / 9 / 13 / 13.5 lh1.7
- 固定寸法: レール 46・左ドック 218（カラム/アイコン時 462）・右ドック 340・titlebar 38・タブ列 34・パンくず 26・statusbar 26・ターミナル 168・行番号ガター 46（右寄せ）+ git バー 3px
- 角丸: 窓 10・ピル/ボタン 5–6・カード 8–9・パレット 12
- フォント: UI = システム（SF Pro / Hiragino Sans）、コード = SF Mono / Menlo 系。太字は 600–700 のみ

## 2. レール（プロジェクトの方向感覚・常設）

- 項目 = 30×30 角丸8。中身は優先順: `.shirushi/settings.json` の `icon`（絵文字 or 画像パス）> プロジェクト名の頭文字（例: 印）
- 状態: active = プロジェクト色の枠 + 2px リング / hover = 枠のみ / 通常 = 色 14% の面 + 35% の枠
- 下部に**実行中スレッドのドット**（5px・スレッド色・実行中 pulse 1.6s）→ 他プロジェクトの動きが常に見える
- ＋ = **統一オープン**（`open_launcher`）: フォルダ / ファイル / リモート + **最近（local + remote 混在・opened_at 降順）** を 1 枚の広いファジー面に。最近行は ●識別色 ＋ 実行中ドット、リモートは `host:path` で識別。⏎ = 現レールに開く。⌘O は開いているプロジェクト間の切替（別物）。左クリック = 窓内切替（ARCHITECTURE §5）。ドラッグ並び替えは v1
- **右クリック = コンテキストメニュー**（M10-2）: 色スウォッチ + 「その他の色…」（フル hex ピッカー）／新しいウィンドウで開く／レールから外す／(worktree タブのみ) worktree を削除・worktree ごとブランチを削除。破壊的操作は二段確認（もう一度クリックで実行）
- **アクティビティアイコン**（下部・explorer/search/git/todos/agent/terminal/remote）: 表示中のビュー/ドックだけプロジェクト色・他は fg2（§1.3・VSCode の Activity Bar 準拠）。active 判定 = explorer:左ドックがツリー / search:検索パネル開 / git:git パネル開 / todos:板開 / agent:右ドック表示 / terminal:下ドック表示 / **remote:SSH ホストピッカーを開くアクション（server アイコン・アクティブプロジェクトがリモートなら色付き・#2）**
- **リモート slot**（レールのプロジェクト）: SSH 接続先はモノグラム右下に **server バッジ**（プロジェクト色・ローカルと見分ける・#2）。窓縁 2px 枠（§1.3）と併用

## 3. titlebar / statusbar

- titlebar: 信号機 → **ピル**（枠1px・左縁3pxプロジェクト色。名前クリック=⌘O、`⎇ branch ▾` クリック=branch/worktree メニュー）→ spacer → **beacons**（アクティブ project のスレッド、色ドット+名、実行中 pulse）→ ドック開閉アイコン×3
- statusbar（登録式・左→右）: `⎇ branch`・診断 `✗n ▲n`・LSP 状態 / **中央=状態の常設ロールアップ**（herdr 本来の形＝ニュースティッカーではない。ウィンドウのレール全プロジェクトを `RunningRegistry` から集計し「N 実行 · M 承認待ち · K 完了」+ 最重要スレッドを状態ドット付きで・click で該当プロジェクト+Agent へ）/ 拡張アイテム・カーソル位置・エンコーディング・言語

## 4. エクスプローラ（3ビュー + ファイルブラウザ挙動）

- ヘッダ = **上位階層ブレッドクラム**（`Work › experience › shirushi`、現在=太字 fg0、親=fg2・クリックで上へ。プロジェクトの外に出たら「フォルダブラウズモード」— 任意フォルダを右クリック→プロジェクトとして開ける）
- フッタ = ビュー切替アイコン3種（ツリー/カラム/アイコン）+「表示モード」ラベル
- **ツリー**: 行 = chev(10px) + 名前 + git ドット（右寄せ・mod=warn色/add=ok色）。インデント 14px/段。選択 = bg3 + 左バー2pxプロジェクト色
- **カラム**（Miller）: 列 150px 固定・最終列 flex。選択経路 = bg3+左バー。下部パスバー 26px
- **アイコン**: ナビヘッダ（‹ › ↑ + 現在フォルダ名 + 項目数）、grid minmax(84px)、タイル = SVG 40×34 + 名前 10.5px 省略。フォルダ=固定ブルー、書類=拡張子タグ（TOML/MD/GIT…）。下部パスバー
- **右クリックメニュー（全ビュー共通）**: 開く / *(dir)* **ここをプロジェクトとして開く**（現レールに再ルート・remote は SSH 接続を再利用＝browse-first の「開く」主導線）/ **新しいウィンドウで開く** / *(file)* 分割して開く / ─ / Finder に表示 / パスをコピー `⌥⌘C` / 名前を変更… `⏎` / ─ / ゴミ箱に入れる `⌘⌫`
- ファイル操作（作成/リネーム/移動/削除）は **undo 可能**にする（VSCode 同様）

## 5. タブ・エディタ

- タブ: [変更ドット(warn)] 名前 [×]。active = bg1 + **上線2pxプロジェクト色**。右端に分割ボタン
- パンくず: パス › シンボル階層（シンボルは syn-type 色）
- エディタ: 行番号（fg2・現在行のみ fg0 は v1）・git バー・カーソル行 = 白4.5%面・キャレット = プロジェクト色2px 点滅1.1s・診断 = 波線・スクロールバーにマーク（diff/エラー/検索）
- 選択面はテーマ固定色（1.1）。ソフトラップは M2 では無し
- **⌘F 検索バー**（M10）: エディタ右上に浮かせるカード（bg2・枠 border・影）。行1 = ▸/▾（置換行開閉）+ クエリ + `n/m` + Aa + `.*` + ‹ › + ×。行2（⌥⌘F）= 置換 + [置換] [全置換]。アクティブ入力欄 = **アクセント枠 + 末尾キャレットバー**。**全マッチ = warn 16% 面**（選択面の下）・現在マッチ = 選択面（選択して見せる）。Enter/⇧Enter = 次/前、置換欄 Enter = 1件置換、⌘Enter = 全置換（1 undo）、Esc = 開いた位置へ戻って閉じる、× = 現在位置のまま閉じる

## 6. Agent パネル（差別化の本丸）

- **スレッドタブ**: 状態ドット8px + **エージェントアイコン**（Claude/Codex… のブランド・設定画面と同一在庫＝`AgentKind.icon`）+ 名前。active = bg1 + 上線2pxスレッド色。＋で新規（色は巡回パレットから自動、後から変更可）。**ダブルクリックで改名**（IME 正しいインライン入力・アクセント枠・Enter 確定/Esc 取消・空白は無視。手動改名は AI 自動命名で上書きしない。判定は押下即発火の `on_mouse_down` の `click_count==2`＝並べ替え `on_drag` の 2px 閾値にクリック合成を奪われないため）。
- **状態ドット（herdr の5状態マップ・§8）**: 色相は**常にスレッド識別色**（状態に色相を使わない・§1.3）。状態は「リング × 動き」で: Idle=淡・静止 / **Working=点字スピナー ⠋⠙⠹…（スレッド色・約 8 コマ/秒・herdr の動きを規律に翻訳＝2026-07-25 ユーザー選定・比較カタログ mock/working-anim-patterns.html）** / Done=リング・静止（中断は中空）/ Blocked=半円・速い脈動。タブ/List/beacon/フッター/レール/⌘O で同じ `activity_dot` を共用。**名前は初回ターン後に AI が自動命名**（`claude -p`・既定名のみ・`settings.agent_auto_name` 既定 on・#6）。タブ列右端に **🕘 履歴**（過去スレッド一覧→復元・⌘⇧H / パレット「AI: スレッド履歴」・行頭●スレッド色 + Σトークン・#5）+ ＋
- メタ行: model ピル・think ピル・**トークンメーター常時**（バー64px + `23.4k/200k` 数字。バー=スレッド色）
- transcript（VSCode Claude Code 拡張踏襲）:
  - msg-user = bg2 箱・左縁2pxスレッド色・全幅
  - `✳ Thinking…` = 常時展開・斜体 fg2・左縁スレッド色45%
  - ステップ = `⏺`（#d97757）+ 太字ツール名 + `(引数)` mono fg2 → 次行 `⎿ 結果` mono 11px fg2
  - Todos（エージェントの実行プラン）= **transcript 上部の常設チェックリスト**（`SessionUpdate::Plan` の全量置換に追従）。`☒` 完了(fg2) / `●` 進行中(**スレッド色**) / `☐` 未着手。ヘッダに `Todos n/m`。プランが無ければ非表示（M12-9 で「ステップ内表示」から変更）
  - 本文 = 13px lh1.65。結論強調はスレッド色
- **composer**: 上段に**宛先チップ**（●スレッド色 + スレッド名 + 右寄せ `project ⎇ branch`）→ textarea（placeholder に「Enter=改行 / ⌘Enter=送信」）→ 下段 model ピル + **送信ボタン（スレッド色面）**。枠 1.5px = スレッド色40%、focus で 100%
- **色貫通ルール**: アクティブスレッド切替で上記のスレッド色箇所が一斉に切り替わる（= 混戦対策の本体）
- IME: Enter 単押しは常に改行。送信は ⌘Enter のみ（IME 確定 Enter を送信にしない）

## 7. オーバーレイ

- **⌘⇧P パレット**: 入力 + リスト（カテゴリ fg2 64px + コマンド名 + キーバインド右寄せ）。選択 = accent-dim 面
- **⌘O スイッチャー**（2階層）: プロジェクト行 = ●色 + 名前 + パス(右寄せ fg2) + `⌘1..9`。配下にインデントで `⎇ branch + worktree パス + 実行中ドット`。最下部「＋ worktree を作って開く / 新しいウィンドウで開く `⌘⏎`」
- **branch/worktree メニュー**（ピルの ⎇ から・幅430）: 現在✓ / worktree 付き branch（実行中ドット）/ behind 表示 / 「＋ ブランチを worktree で開く（並行作業・エージェント用）」
- 共通: esc で閉じる・背景クリックで閉じる・入力に即フォーカス

## 8. ターミナル / 通知

- 下ドック 168px。タブ（active = bg1。色線なし）+ `＋`。プロンプト = ok 色。パス様文字列はリンク化（クリックで file:line へ）
- 通知はトースト（右下）。エージェント完了は宛先チップと同じ書式で「●rope設計 完了」
- **エージェント状態（herdr 参照・状態検知は ACP ネイティブ＝ヒューリスティック不要）**: ACP のライフサイクルから `ThreadActivity` を導出する。**Blocked**=`session/request_permission` 待ち（ターンが実際にブロック）/ **Working**=ストリーミング中 / **Done**=直近ターン完了・**未確認**（見るまで残るラッチ・中断= `StopReason` の Refusal/Cancelled は注意表示）/ **Idle**=待機。ambient 表示先は タブ/List・titlebar beacon・statusbar 中央ロールアップ・**レールのドット（他プロジェクト分・右上）**・⌘O ダッシュボード。ロールアップ優先度 Blocked>Working>Done>Idle。全窓横断の台帳は `RunningRegistry`（worktree root → 状態）

## 9. キーボード（既定・母語は Zed 互換で確定待ち）

| キー | 動作 |
|---|---|
| ⌘O | プロジェクト/ブランチスイッチャー |
| ⌘1..9 | レール n 番目のプロジェクトへ切替 |
| ⌘⏎（スイッチャー内） | 新しいウィンドウで開く |
| ⌘⇧P | コマンドパレット |
| ⌘P | ファイルファインダ（M3） |
| ⌘F / ⌥⌘F | バッファ内検索 / 置換つき（M10） |
| ⌘⇧F | プロジェクト横断検索（M6） |
| ⌘K ⌘I | hover をキャレット位置に表示（M10） |
| ⌃\` | ターミナル |
| ⌘⇧A | 新規スレッド |
| esc | オーバーレイ/メニューを閉じる |

## 10. i18n

- この文書と mock の日本語ラベルが `locales/ja.yml` の原文。実装時は**全ラベルを `t!` 経由**にし、キーは `explorer.context.open_in_new_window` のように領域プレフィックスで切る。`en.yml` を同時に書く（ja だけ増やさない）

## 11. 編隊（マルチエージェント）モード（M14）

複数エージェントを worktree ごとに並走させ、**色（識別）× 系譜グラフ（構造）× 動き（状態）で指揮する**専用ビュー。herdr（AGPL・クリーンルーム参照）の「設定不要の状態一覧」を ACP ネイティブで取り込むが、**状態に色相は持ち込まない**（§1.3・§8）。入口 = レールの編隊アクティビティ or トグル。通常ビュー（ファイル中心）と明示的に切替。**左のファイルブラウザ/タブは残す**（focus 中エージェントの worktree を映す）。mock 編隊モードが見た目の正。

- **レイアウト**: レール | herd サイドバー（状態一覧 ＋ focus-follows ファイル）| 中央（系譜グラフ ＋ N セルグリッド）| 下（ニュースフィード）。**中央はタブ 2 面**（P3・2026-07-23）: **管制（Control）** / グラフ。既定は当面グラフ（ドッグフーディング後に再判断）
- **管制（Control）タブ**（FLEET-CONTROL-PLAN P3・ビジュアルの正 = `mock/fleet-dashboard.html`）: 上から ① ヘッダ（管制 repo ⎇ main + N worktrees・目標文・stat チップ **◐要対応**（>0 で err 色ボーダー）/ ✓完了未確認 / ●稼働 / ⇡統合・Σトークン合計+メーター）② 監督バー（**集約気分マスコット**＝編隊の最悪状態に追従: 承認待ち→祈る・15s 超→頬に手・実行中→打鍵・静穏→うとうと。監督は P6 まで「未任命」・総括は Tier2 まで決定論の事実文＝**✳ を付けない**）③ **要対応キュー**（左 336px・Blocked 経過時間順→Failed→Review→Done 未確認。カード内**インライン操作** = 許可/常に許可/拒否（ACP 広告ラベルそのまま）・Radar 実行・Integrate・確認（Done→Idle の確認済み遷移）・破棄。先頭カードに「次 ⏎」バッジ）④ **稼働カード**（右・digest + ▰▱ plan メーター + files + tok + 経過。クリックで没入=セル拡大へ）⑤ **統合パイプライン**（PLANNED→WORKING→BLOCKED→REVIEW_READY→MERGE_READY→INTEGRATED→FAILED 列にタスクチップ・changes_requested は REVIEW_READY 列・integrating は MERGE_READY 列に畳む）⑥ ニュース（P2 常設）。**⏎ = キュー先頭へ没入**（keymap context `FleetControl`）
- **✳ と破線の意味論**（管制の規律・FLEET-ARCHITECTURE）: **✳ テラコッタ = LLM 生成テキストの印**（Tier 2 要約のみに付す・事実層/Tier1 には付けない）。**破線 = 画面推定**（Herdr 由来・P7。確定情報と視覚的に混ぜない）
- **遷移スナップショット（digest・P1）**: 状態遷移時のみ生成（ポーリング無し）。Blocked=許可待ちの内容 / Done=最後の発言の末尾 1〜2 文 / Failed=エラー文 / Working=ライブ素材（実行中ツール + plan 進行中・保存しない）。herd 行の 1 行サブテキスト・セルヘッダ帯・管制カードが共用
- **ニュース（P2）**: `task_events` の鏡（= 台帳に書くのと同じ場所で積む・起動時 backfill）。行 = 相対時刻 + 帰属チップ（スレッド/Task 色・**監督は丸チップ**）+ 太字名 + イベント文。エージェント別ミュート（🔔/🔕）はトースト/完了音だけ止め、ニュースには残る。完了音は **window 非アクティブ時のみ**
- **herd サイドバー（herdr の核・常設）**: プロジェクト別グループ。行 = `activity_dot`（スレッド色 × 形と動き＝§6）＋ 左バー 2px スレッド色 ＋ **エージェントアイコン**（`AgentKind.icon`・§6）＋ 名前 / 1行状態＋**最新時刻**（最終入力・未入力なら開始。両方はホバーの tooltip）・トークン。末尾に 5 状態の凡例（**形**の説明＝色は識別なので中立）。下段に focus 中エージェントの worktree ツリー（git 色つき）
- **状態は色相を持たない**（§1.3・§6・§8 の `activity_dot` を共用）: ⠿ 作業中(点字スピナー回転・2026-07-25 選定) / ◐ 承認待ち(半円・速い脈動) / ✓ 完了・未確認(静止) / ○ 待機(淡) / · その他。色は常にスレッド識別色。ACP は `ThreadActivity` で確定・統合ターミナルは画面スキャンで推定（M14）
- **focus-follows-agent**: herd 行 / グリッドセルをフォーカス → 左エクスプローラ・エディタタブ・下ターミナルがその worktree に張り替わる（レール切替と同じ `ProjectSession` 単位・ARCHITECTURE §5）。＝「今見ているエージェントのファイル」で真を一意化
- **系譜グラフ**: ネイティブ描画（webview 不使用・editor_view/terminal_view と同じ custom Element）。**扇形（Fan）既定** ＋ ツリー / カード / ハブの 4 表示（River は廃止し、Hub＝中央=リポジトリのハブ&スポークを追加）。ヘッダ右にスイッチャー（**アイコン＋語**・単体アイコンが曖昧なため語併記）＋ ⌄ で折り畳み（グリッドが上まで拡張）。レーン/エッジ=スレッド色・ノード=commit・**先端=状態（形と動き・色は据え置き）**・プロバイダバッジ・base からの fan-out とマージ。データは `git log --all --parents` でレーン自前計算
- **N セルグリッド**: 主セルは **TaskSpace（1 task = 1 worktree）**で、通常 View と同じ完全な AgentPanel を埋め込む。同じ Task の Agent は panel 内 thread tab。surface は Task / Terminal / Editor / Diff / Tests。既定 **＋Task** は常に `task/*` branch + linked worktree を作り、**＋Agent to this Task** だけが同じ worktree への複数 Agent を許す。セルの × は表示から外すだけ。
- **履歴（前の会話の復元）**: スレッドは DB 永続なので、編隊でも Agent パネルと同じ**スレッド履歴 Picker**（アーカイブ含む・detail に Σトークン＋開始/最終入力の相対時刻）から復元できる（＋タイル「履歴から復元」/ ⌘⇧T は直近）。編隊モード中に確定した会話は**グリッドのセルとして前面（拡大）に出る**。相対時刻は編隊/herd 表示中のみ 30 秒時計で更新（両方閉じたら自停止＝idle 予算）
- **ニュースフィード**: 下・トースト書式の時系列（スレッド色ドット＋文＋時刻・状態は文言で）。terminal とタブ or 排他
- **Task lifecycle / integration**: planned → working / blocked → review_ready → merge_ready → integrating → integrated。Review は read-only Conflict Radar、main への write は明示 Integrate のみ。
- **編隊操作 API**: `shirushi fleet create/list/status/wait/review/integrate` と同等 MCP tools。stable Task ledger を真実にするため Coordinator/UI の再起動を跨いで wait/resume できる。詳細は `FLEET-ARCHITECTURE.md`。
- 既存の**スレッド色貫通・宛先チップ・トークン常時表示**は編隊内でも不変。**キー**: 編隊トグル / ＋(パネル追加) / グラフ ⌄(折り畳み) / セルフォーカス(クリック)（既定キーは M14 で確定）
