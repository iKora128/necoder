# UI-SPEC — mock v0.3 の実装仕様

`mock/index.html` が見た目の**正**。この文書は mock を GPUI 実装向けに数値・状態・振る舞いへ翻訳したもの。
変更したくなったら **mock を先に直してスクリーンショットで確認 → この文書 → 実装** の順。

## 1. デザイントークン

### 1.1 色（dark / light）

| token | dark | light | 用途 |
|---|---|---|---|
| bg0 | `#16181e` | `#f0f1f4` | 窓クローム・ドック |
| bg1 | `#1b1e25` | `#ffffff` | エディタ面・チャット面 |
| bg2 | `#232733` | `#ffffff` | カード・パレット・入力箱 |
| bg3 | `#2b3040` | `#e7eaf0` | hover・**中立の選択面** |
| fg0 | `#d7dae3` | `#333845` | 主文字 |
| fg1 | `#9aa1b2` | `#5c6370` | 副文字 |
| fg2 | `#646b7a` | `#9aa1b2` | ミュート・行番号 |
| border | `#262b36` | `#d8dce3` | 全罫線（1px） |
| ok / warn / err | `#7bc96f` `#e5c07b` `#e06c75` | `#3e9d32` `#bf8803` `#e45649` | 診断・git |
| syn-kw | `#c678dd` | `#a626a4` | キーワード |
| syn-fn | `#61afef` | `#4078f2` | 関数 |
| syn-type | `#e5c07b` | `#c18401` | 型 |
| syn-str | `#98c379` | `#50a14f` | 文字列 |
| syn-num | `#d19a66` | `#986801` | 数値 |
| syn-cmt | `#5f6672` | `#a3a6ae` | コメント（italic） |
| syn-mac | `#56b6c2` | `#0184bc` | マクロ |
| syn-punct | `#abb2bf` | `#383a42` | 記号 |

**テーマ非連動の固定色**: エディタ選択面 `#7d9bd8` 28%透過 / フォルダアイコン `#7d9bd8` 55%（bg3 と mix）/ Claude バレット `#d97757`。

**きせかえ契約**: この表が**テーマの全インターフェース**。ユーザー定義テーマ = このトークンを上書きする JSON 1枚（`~/Library/Application Support/Shirushi/themes/*.json`）。
テーマ（面と文字の配色）とプロジェクト色（識別・Peacock相当、§1.2）は**独立した2軸** — どのテーマでもプロジェクト色は §1.3 の許可リスト位置に流れる。

### 1.2 アイデンティティ色

- **プロジェクト色パレット（自動巡回）**: indigo `#7c8cf8` → teal `#34d3b6` → amber `#f0a24b` → rose `#ef7d9b` → green `#85c46c`
- 優先順: `.shirushi/settings.json` の `color` > ユーザーが手動選択 > パレット巡回
- **スレッド色パレット（自動巡回）**: `#61afef` → `#e5c07b` → `#c678dd`（プロジェクト色と独立）
- accent-dim = プロジェクト色 16% 透過（パレット選択面・タイル選択輪郭のみ）

### 1.3 色の許可リスト（**ここ以外に色を置いたらレビューで落とす**）

| 色 | 置いてよい場所（全列挙） |
|---|---|
| プロジェクト色 | レール項目の枠/リング・ピル左縁 3px・アクティブタブ上線 2px・ツリー/カラム選択の左バー 2px・キャレット 2px・タイル選択の薄輪郭・パレット選択面(dim)・拡張バッジ(dim) |
| スレッド色 | スレッドタブの上線+ドット・titlebar beacon・statusbar ドット・トークンメーター・thinking 左縁・msg-user 左縁・composer 枠・宛先チップのドット・送信ボタン |
| 禁止 | titlebar/statusbar のグラデ額縁・選択面の色塗り・フォルダアイコン・ターミナルタブ（v0.2 で廃止済み） |

### 1.4 寸法・密度・タイポグラフィ

- 密度 compact（既定）: 行高 23 / padding 6 / UI 12.5px / code 13px lh1.55。cozy: 27 / 9 / 13 / 13.5 lh1.7
- 固定寸法: レール 46・左ドック 218（カラム/アイコン時 462）・右ドック 340・titlebar 38・タブ列 34・パンくず 26・statusbar 26・ターミナル 168・行番号ガター 46（右寄せ）+ git バー 3px
- 角丸: 窓 10・ピル/ボタン 5–6・カード 8–9・パレット 12
- フォント: UI = システム（SF Pro / Hiragino Sans）、コード = SF Mono / Menlo 系。太字は 600–700 のみ

## 2. レール（プロジェクトの方向感覚・常設）

- 項目 = 30×30 角丸8。中身は優先順: `.shirushi/settings.json` の `icon`（絵文字 or 画像パス）> プロジェクト名の頭文字（例: 印）
- 状態: active = プロジェクト色の枠 + 2px リング / hover = 枠のみ / 通常 = 色 14% の面 + 35% の枠
- 下部に**実行中スレッドのドット**（5px・スレッド色・実行中 pulse 1.6s）→ 他プロジェクトの動きが常に見える
- ＋ = ⌘O スイッチャーへ。最下部に `⌘O` ヒント。クリック = 窓内切替（ARCHITECTURE §5）。ドラッグ並び替えは v1

## 3. titlebar / statusbar

- titlebar: 信号機 → **ピル**（枠1px・左縁3pxプロジェクト色。名前クリック=⌘O、`⎇ branch ▾` クリック=branch/worktree メニュー）→ spacer → **beacons**（アクティブ project のスレッド、色ドット+名、実行中 pulse）→ ドック開閉アイコン×3
- statusbar（登録式・左→右）: `⎇ branch`・診断 `✗n ▲n`・LSP 状態 / spacer / 拡張アイテム・カーソル位置・エンコーディング・言語・**スレッドドット列+トークン計**

## 4. エクスプローラ（3ビュー + ファイルブラウザ挙動）

- ヘッダ = **上位階層ブレッドクラム**（`Work › experience › shirushi`、現在=太字 fg0、親=fg2・クリックで上へ。プロジェクトの外に出たら「フォルダブラウズモード」— 任意フォルダを右クリック→プロジェクトとして開ける）
- フッタ = ビュー切替アイコン3種（ツリー/カラム/アイコン）+「表示モード」ラベル
- **ツリー**: 行 = chev(10px) + 名前 + git ドット（右寄せ・mod=warn色/add=ok色）。インデント 14px/段。選択 = bg3 + 左バー2pxプロジェクト色
- **カラム**（Miller）: 列 150px 固定・最終列 flex。選択経路 = bg3+左バー。下部パスバー 26px
- **アイコン**: ナビヘッダ（‹ › ↑ + 現在フォルダ名 + 項目数）、grid minmax(84px)、タイル = SVG 40×34 + 名前 10.5px 省略。フォルダ=固定ブルー、書類=拡張子タグ（TOML/MD/GIT…）。下部パスバー
- **右クリックメニュー（全ビュー共通）**: 開く / *(dir)* **新しいウィンドウでプロジェクトとして開く** / *(file)* 分割して開く / ─ / Finder に表示 / パスをコピー `⌥⌘C` / 名前を変更… `⏎` / ─ / ゴミ箱に入れる `⌘⌫`
- ファイル操作（作成/リネーム/移動/削除）は **undo 可能**にする（VSCode 同様）

## 5. タブ・エディタ

- タブ: [変更ドット(warn)] 名前 [×]。active = bg1 + **上線2pxプロジェクト色**。右端に分割ボタン
- パンくず: パス › シンボル階層（シンボルは syn-type 色）
- エディタ: 行番号（fg2・現在行のみ fg0 は v1）・git バー・カーソル行 = 白4.5%面・キャレット = プロジェクト色2px 点滅1.1s・診断 = 波線・スクロールバーにマーク（diff/エラー/検索）
- 選択面はテーマ固定色（1.1）。ソフトラップは M2 では無し

## 6. Agent パネル（差別化の本丸）

- **スレッドタブ**: 色ドット8px + 名前。active = bg1 + 上線2pxスレッド色。実行中 = ドット pulse。＋で新規（色は巡回パレットから自動、後から変更可）
- メタ行: model ピル・think ピル・**トークンメーター常時**（バー64px + `23.4k/200k` 数字。バー=スレッド色）
- transcript（VSCode Claude Code 拡張踏襲）:
  - msg-user = bg2 箱・左縁2pxスレッド色・全幅
  - `✳ Thinking…` = 常時展開・斜体 fg2・左縁スレッド色45%
  - ステップ = `⏺`（#d97757）+ 太字ツール名 + `(引数)` mono fg2 → 次行 `⎿ 結果` mono 11px fg2
  - Todos = ステップとして `☒/☐` リスト表示
  - 本文 = 13px lh1.65。結論強調はスレッド色
- **composer**: 上段に**宛先チップ**（●スレッド色 + スレッド名 + 右寄せ `project ⎇ branch`）→ textarea（placeholder に「Enter=改行 / ⌘Enter=送信」）→ 下段 model ピル + **送信ボタン（スレッド色面）**。枠 1.5px = スレッド色40%、focus で 100%
- **色貫通ルール**: アクティブスレッド切替で上記のスレッド色箇所が一斉に切り替わる（= 混戦対策の本体）
- IME: Enter 単押しは常に改行。送信は ⌘Enter のみ（IME 確定 Enter を送信にしない）

## 7. オーバーレイ

- **⌘⇧P パレット**: 入力 + リスト（カテゴリ fg2 64px + コマンド名 + キーバインド右寄せ）。選択 = accent-dim 面
- **⌘O スイッチャー**（2階層）: プロジェクト行 = ●色 + 名前 + パス(右寄せ fg2) + `⌘1..9`。配下にインデントで `⎇ branch + worktree パス + 実行中ドット`。最下部「＋ worktree を作って開く / 新しいウィンドウで開く `⌘⏎`」
- **branch/worktree メニュー**（ピルの ⎇ から・幅430）: 現在✓ / worktree 付き branch（実行中ドット）/ behind 表示 / 「＋ ブランチを worktree で開く（並行作業・エージェント用）」
- 共通: esc で閉じる・背景クリックで閉じる・入力に即フォーカス

## 8. ターミナル / 通知

- 下ドック 168px。タブ（active = bg1。色線なし）+ `＋`。プロンプト = ok 色。パス様文字列はリンク化（クリックで file:line へ）
- 通知はトースト（右下）。エージェント完了は宛先チップと同じ書式で「●rope設計 完了」

## 9. キーボード（既定・母語は Zed 互換で確定待ち）

| キー | 動作 |
|---|---|
| ⌘O | プロジェクト/ブランチスイッチャー |
| ⌘1..9 | レール n 番目のプロジェクトへ切替 |
| ⌘⏎（スイッチャー内） | 新しいウィンドウで開く |
| ⌘⇧P | コマンドパレット |
| ⌘P | ファイルファインダ（M3） |
| ⌃\` | ターミナル |
| ⌘⇧A | 新規スレッド |
| esc | オーバーレイ/メニューを閉じる |

## 10. i18n

- この文書と mock の日本語ラベルが `locales/ja.yml` の原文。実装時は**全ラベルを `t!` 経由**にし、キーは `explorer.context.open_in_new_window` のように領域プレフィックスで切る。`en.yml` を同時に書く（ja だけ増やさない）
