# ROADMAP — 受入条件つきマイルストーン

運用: `/goal` はこのファイルの**未チェックの受入条件**を上から拾って実装する。
チェックは「実際に満たすことを検証してから」入れる（CLAUDE.md の検証ループ）。順序の正はこのファイル、機能の全量は FEATURES.md。
人の対話検証が必須の項目（M1 の窓確認・M2 の IME 対話など「人の手番」）は /goal ではスキップし、次の実装可能な項目を拾う。

## M0 — 週末テスト（ユーザー自身の関門）

- [x] 環境: Metal Toolchain 導入（2026-07-11、`xcodebuild -downloadComponent MetalToolchain`）
- [x] `cd zed && cargo run -p gpui --example hello_world` で窓が出る — **2026-07-11 通過**（3m28s ビルド後に hello_world 表示。maintainer の実機で確認）

## M1 — 骨組み ✓（2026-07-11）

- [x] workspace スキャフォールド（crates/necoder・rust-toolchain 1.95・AGPL-3.0 LICENSE・CLAUDE.md）
- [x] `cargo check -p necoder` 通過
- [ ] `cargo run -p necoder` で骨組み窓が出ることをユーザーが確認

## M2 — エディタコア（「テキストが編集できる」）

- [x] `theme_core`: UI-SPEC §1 のトークンを型と dark テーマ値で実装（light は値だけ用意）— 2026-07-11、`crates/theme_core`、unit test 9 本 green
- [x] `i18n`: `t!` 規律開始・`locales/ja.yml`+`en.yml` — 2026-07-11、`crates/i18n`（rust-i18n はマクロが crate 局所で多 crate に不適 → 薄い自作に。ARCHITECTURE §6 更新済み）、ja/en parity テスト付き
- [x] `editor_core`: Buffer(ropey 2.0・byte 索引)・Selection・edit/undo/redo（Transaction 単位）・snapshot — 2026-07-11、`crates/editor_core`、GPUI 非依存・unit test 18 本 green
- [x] `editor_view`: 行仮想化描画・スクロール・行番号ガター・キャレット描画 — 2026-07-11、`crates/editor_view`（custom Element・`shape_line`・`uniform_list` 相当の仮想化）。1.6MB 開いても idle CPU 0%（＝可視行のみ shape）。目視: font-kit 有効化後は offscreen（`render_to_image`）でもグリフが写り自己検証可能に。ユーザーが実機で描画確認済み（2026-07-12）
- [x] キー入力で挿入/削除/改行、⌘Z/⌘⇧Z、⌘S 保存、ファイルは起動引数で開く — 2026-07-11、`crates/necoder` に配線（`EntityInputHandler` 経由の入力・アクション/キーマップ）。smoke（多言語+絵文字+1.6MB）で無クラッシュ
- [ ] **IME**: 日本語入力（変換中表示・確定）が正しく動く — 受入: この README を necoder で開いて日本語の1段落を書ける ← **コードは実装済み**（`replace_and_mark_text_in_range` 等・UTF-16↔byte）。**IME 対話検証が未**（人が変換入力する必要・headless 不可）
- [ ] 受入（総合）: `necoder <file>` で開く→編集→undo→保存 が一往復できる。1MB のファイルでスクロールがもたつかない ← ロジックは全て test 済・1.6MB は idle 0% だが、**対話/目視の一往復確認が未**
- [x] 性能ベンチ導入（一部）: 起動時間計測 `scripts/startup-time.sh`（debug 実測 ~100ms cold→first render）+ editor_core スループット bench（insert 1.5M/s・1MB上 edit 40万cycle/s）。**necoder 固有の key→frame ヒストグラム計測は後回し**（要 UI・時期尚早／JOURNAL 参照）

## M3 — ワークスペースと方向感覚（「色で迷わなくなる」）

- [x] `settings_core`: 3層マージ（default→user→`.necoder/`）— 2026-07-12、`crates/settings_core`、深い JSON マージ・型付き Settings、6 test。保存即反映（監視）は後続
- [x] テーマきせかえ: light 値の実装 + テーマセレクタ（Picker 上・ライブプレビュー）+ ユーザーテーマ JSON 読み込み — 2026-07-13。`theme_core` に**ユーザーテーマ JSON（トークン上書き・欠けは appearance の組み込みへフォールバック）**・`available_themes`/`resolve` を実装（unit test）。`⌘⇧T` でテーマセレクタ（Picker）＝**ハイライト移動で即ライブプレビュー**（`PickerEvent::Highlighted`）・確定で settings.json へ theme 名保存（再起動でも効く）・中止でプレビューを戻す。`apply_theme` がクローム/エディタ/Agent パネル/Picker へ波及。light 全体反映を offscreen で目視確認
- [x] `keymap_core`: JSON keymap + コンテキスト述語 — 2026-07-12、`crates/keymap_core`、`build_action` で名前解決・`KeyBinding::load`、3 test。既定 keymap を bin で読込（全アクション解決確認）
- [x] `ui`: Picker 基盤（1個のファジーリストを全モーダルで使い回す）— 2026-07-12、`crates/ui`、fuzzy・キー操作・イベント通知、2 test。**CommandRegistry/StatusItemRegistry は未**
- [x] `workspace`: タブ・単一ペイン→分割・左右下ドック・statusbar — titlebar ✓・タブ列 ✓・パンくず ✓・左ドック(explorer) ✓・右ドック(Agent) ✓・statusbar ✓（2026-07-12）。**分割ペイン**（`⌘\` で右分割・独立エディタの比較ビュー・各ペインにタブ+×）✓・**下ドック**（統合ターミナル・`⌘J`）✓（2026-07-13）
- [x] `project`: worktree 走査・gitignore — 2026-07-12、`crates/project`、`ignore` crate で gitignore 準拠の遅延 read_dir + all_files、4 test。**ファイル監視は未**
- [x] エクスプローラ（ツリーのみ先行）+ ファイル開く — 2026-07-12、workspace の左ドック。クリックで展開/ファイル open
- [x] **レール**: プロジェクト登録・切替（窓内）・プロジェクト色（自動巡回）— 2026-07-12。**カスタムアイコン / `.necoder` 色指定は未**（頭文字 + 巡回色）
- [x] **⌘O スイッチャー** + ⌘1..9 + **新窓** — 2026-07-13。⌘O プロジェクトスイッチャー ✓（Picker）。⌘1..9 でレールのプロジェクト N 番へ切替（`ActivateProject1..9` アクション）。**新窓は ⌘⇧N**（当初案 ⌘⏎ は composer の `agent::SubmitPrompt` と衝突するため慣例的な ⌘⇧N に変更。Editor コンテキストの ⌘⏎ 送信は温存）＝アクティブプロジェクトを新ウィンドウで開く（右クリック「新規ウィンドウで開く」と同じ経路）
- [x] 状態永続化 — 2026-07-12、`state.json` に (プロジェクト群・アクティブ・開ファイル) を保存、引数無し起動で復元
- [x] ファイルファインダ ⌘P — 2026-07-12（Picker + `all_files`）。**コマンドパレット ⌘⇧P は未**（要 CommandRegistry）
- [x] 受入: **2プロジェクトをレールで色区別 + 再起動で状態復元** — 2026-07-12 達成（offscreen で 2 色レール目視・state.json 往復確認）

## M4 — ACP（「Claude Code が中で動く」）

- [x] `acp_client` 接続・initialize・prompt・**ストリーミング** — 2026-07-12、`crates/acp_client`。claude-agent-acp を子プロセス起動 + `blocking::Unblock` パイプ + `ByteStreams` + `Client.builder().connect_with`。バイナリ探索を **PATH → Zed の npx キャッシュ(.bin) → `npx @agentclientprotocol/claude-agent-acp@0.58.1`** に強化（単体は PATH に無いのが普通・これが `live_prompt` の当初失敗要因だった）。`prompt_once`（一括）に加え **`run_session`（永続セッション + `read_update` ループで `AgentMessageChunk`/`AgentThoughtChunk` を [`AgentEvent`] に簡約し逐次流す）** を実装。`ToolCall` も [`AgentEvent::ToolStarted`] にマップ。**エージェント自身が実機検証済み**: `live_prompt`（「1+1」→「2」5.7s）・`live_stream`（「3の倍数5個」→`3, 6, 9, 12, 15` を逐次）・ツール誘発プローブ（「ls して」→ ⏺ Terminal ステップ + 応答）。**権限リクエスト・`UsageUpdate` でトークン実値は達成・markdown 描画も完了（2026-07-19: pulldown-cmark 借用 + 自作レンダラ・Thinking 折り畳み。JOURNAL 参照）**
- [x] `agent_panel`: スレッド色タブ・メタ行（モデル/トークン常時メーター）・transcript（⏺/⎿・✳ Thinking 常時展開）・composer（宛先チップ・⌘Enter 送信・IME 安全）— 2026-07-12、`crates/agent_panel`（右ドックまるごと）。composer は editor_view 平坦モード再利用で IME/undo 共通。⌘Enter→`agent::SubmitPrompt`→**スレッド毎の常駐セッション**（`run_session` をバックグラウンド + `cx.spawn` フォアグラウンドで `AgentEvent` を逐次 transcript 反映）。**実機フルパス検証済み**: 起動プローブ（`NECODER_ACP_PROBE`）で composer→claude-agent-acp→ストリーミング→描画を offscreen スクショで確認（「1+1は？」→「2」がスレッド色付きで表示）。transcript の初期内容は mock 会話例（デモ・実送信で本物が積まれる）
- [x] 権限リクエスト UI（許可/拒否）・エージェント編集の diff レビュー（accept/reject）— 2026-07-12、`crates/acp_client`+`agent_panel`。`session/request_permission` を read ループの **`.if_request`** で受け、ハンドラ内 await でユーザー決定待ち（ターンブロックが正）。`ToolCallContent::Diff` を `compact_line_diff` で緑（追加=`theme.ok`）/赤（削除=`theme.err`）表示、composer 直上に承認カード（許可/常に許可/拒否）。**live: Write プローブで diff カード表示 + `NECODER_AUTO_ALLOW` で実ファイル生成まで round-trip 確認**
- [x] スレッド色貫通（UI-SPEC §6）+ titlebar beacon + statusbar ドット ← 色貫通 ✓（タブ切替で下線/トークンバー/msg-user 左縁/宛先ドット/composer 枠/送信ボタンが一斉切替・実機スクショで確認）。titlebar beacon ✓（実行中スレッドが窓上部に色ドット+名で常時表示＝BACKGROUND の原点痛点）。**statusbar スレッドドットは未**
- [x] 受入: **Claude Code サブスクで会話→ファイル編集を diff で確認→accept、を necoder 内で完結。トークン使用量が常に見えている** — 2026-07-12 達成。会話ストリーミング ✓・トークン実値メーター ✓・**ファイル編集の diff 表示→承認→実行（accept）を live で完結**（round-trip でファイル生成確認）。加えて **model/effort/権限モードの ACP 実反映**（session config options / set_mode）・**Enter 送信の設定化**（IME 誤送信対策）も同日実装

## M5 — エクスプローラ完成（Finder 感）

- [x] カラム / アイコンビュー + 左下ビュー切替 — 2026-07-12、`ExplorerView{Tree,Columns,Icons}` + `render_explorer_footer` の ☰▥▦。拡張子別アイコン（フォルダ横長/ファイル縦長・型色）。カラムは Miller columns（末尾3段・幅可変）
- [x] 上位階層ブレッドクラム — 2026-07-12、`render_explorer_header` のクリック可能パンくず（プロジェクト内 up-nav）。**プロジェクト外へ出るブラウズは未**（root 上へは行かない）
- [x] 右クリックメニュー（新しいウィンドウでプロジェクトとして開く 含む）— 2026-07-12、全3表示のエントリに右クリック。フォルダ=新規ウィンドウで開く/開く/コピー、ファイル=開く/コピー。**ファイル操作 undo は未**
- [x] エクスプローラ幅可変（右縁ドラッグ）— 2026-07-12
- [x] 受入: マウスだけで「上へ辿る→隣のリポジトリを新窓でプロジェクトとして開く」— 2026-07-13。ブレッドクラムの **⤴（上へ）** で current の親へ辿れ、ルート直上へ出ると Finder カラム表示へ自動切替（`project::Worktree::read_any_dir` がルート外を gitignore 無しで列挙）。ルート外では **⌂プロジェクト（戻る）** + 末尾数段のパンくずを出す。隣リポジトリを右クリック→新規ウィンドウで開く（既存）。offscreen で `~/Work/experience` の隣接 repo 一覧を目視確認

## M6 — 検索

- [x] バッファ内検索（regex/literal・大小トグル）— 2026-07-12、`crates/search` の `search_text`、7 test。**置換・インクリメンタル UI は未**
- [x] プロジェクト横断検索 — 2026-07-12、`search_files`（`ignore` の走査結果を渡してインプロセス検索。ripgrep 子プロセスは未採用）
- [x] 受入: `TODO` をプロジェクト横断で列挙→ジャンプ — 2026-07-13。`⌘⇧F` で検索パネル（オーバーレイ）＝クエリ + 大小/正規表現トグル + **ファイル別グルーピング結果**（行番号 + マッチ強調プレビュー）。クリック / Enter で**該当ファイルを開き対象行を中央へジャンプ**（`EditorView::reveal_position` = viewport 未確定でも効く pending_reveal・one-shot で idle 0%）。↑↓ 選択・Esc 閉じる。offscreen で「fn」440 件のファイル別結果を目視確認

## M7 — 言語知能

- [x] tree-sitter ハイライト（Rust）— 2026-07-12、`crates/lang`（tree-sitter 0.25 + tree-sitter-rust）。`HighlightKind`→theme の syn-* に接続、editor_view で行ごとに色 run 生成（編集で再解析・512KB 超はスキップ）。3 test。実 .rs を無クラッシュで開ける。**色の目視は要実機**（offscreen はグリフ非表示）。インクリメンタル解析は後続
- [x] LSP: rust-analyzer（**診断・補完・定義ジャンプ**。hover/format は後続）— 2026-07-13。`lang::lsp` に最小 LSP クライアント（JSON-RPC 封筒は自前・型は手書き・transport は std::process + 読取スレッド + futures channel）。initialize→initialized→didOpen→didChange の lifecycle。①**診断**=publishDiagnostics を gutter 下線（error=赤/warn=琥珀）+ statusbar 件数（✗N ▲N）にライブ反映（live 確認）。②**補完**（Ctrl-Space）=キャレット直下ポップアップ（種別バッジ+detail・上下/Enter・Tab/Esc・textEdit→insertText→label で挿入・識別子プレフィクス置換）。③**定義ジャンプ**（F12）=Location/LocationLink を解析→別ファイルなら開いて中央へ。位置は byte↔UTF-16 変換。`~/.rustup/toolchains/*/bin/rust-analyzer` 実体を起動。parser の unit test + 実 ra の handshake/診断 ignored test。offscreen で診断赤下線・補完ポップアップを目視
- [x] 受入: この repo の Rust を補完と診断つきで書く — 2026-07-13 概ね達成。ハイライト ✓・診断 ✓（live）・補完 ✓（ポップアップ+挿入）・定義ジャンプ ✓。**補完/定義の live round-trip 体感は人の手番**（transport・parser・UI は各々検証済み）

## M8 — Git とターミナル、ブランチ横断の完成

- [x] git status（ツリー/タブ色）+ gutter diff — 2026-07-13。`project` に **git CLI 直叩き**の `git_status`（porcelain→5分類）+ `buffer_diff`（`git show HEAD:./name` + **imara-diff** Histogram・純Rust）。ツリー行/タブ名を状態色（追加=緑/変更=琥珀/削除=赤）+ 末尾バッジ（M/A/U/D/!）、フォルダは配下変更で ●。gutter は追加/変更/削除の左端バー（editor_view・**バッファ version 変化を prepaint で検知→250msデバウンス→背景スレッドで git**＝idle 0%）。offscreen で目視（Cargo.toml=M・gutter 緑バー）
- [x] branch/worktree メニュー（切替・worktree 作成）→ スレッドの (project, branch) 帰属 — 2026-07-13。titlebar の ⎇ クリックで開く（`project::git_branches`/`git_worktrees`/`switch_branch`/`add_worktree`）。ブランチ行クリック=in-place 切替（git switch→再読込）、**⧉=worktree として新しい窓で開く**（既存 worktree 優先・無ければ `<repo親>/<repo名>-<branch>` に作成）＝**並行ブランチ×別窓×スレッド色**の入口。worktree 一覧も別窓で開ける。宛先チップの (project, branch) は切替で追従。**⌘O 2階層化は未**（⎇ 直開きで代替）
- [x] 統合ターミナル（alacritty_terminal）— 2026-07-13。新 crate `terminal_view`（crates.io `alacritty_terminal` 0.26）。**EventLoop が読取+vte parser**、idle 0% は出力時のみ Wakeup→pump(`cx.spawn`)→sync→notify（**タイマー無し**）。下ドックに配置（`⌘J` / 下ドックボタン開閉・cwd=プロジェクトルート）。custom Element でグリッド描画（16/256色 ANSI + truecolor・INVERSE/BOLD/ITALIC・ブロックカーソル）。入力は `on_key_down` 一本化（印字 + 特殊キー→エスケープ・矢印は APP_CURSOR 対応）。offscreen で実シェル（`Last login… / プロンプト / カーソル`）を目視。**パスリンクは未**（後続）
- [x] 受入: **worktree で並行ブランチを開き、それぞれ別スレッドのエージェントを走らせ、色で追える**（＝当初ビジョンの完成形）— 2026-07-13。機構が揃った: ⎇メニューの ⧉ で **ブランチ→worktree→新ウィンドウ**、各窓は自分の (project, branch) の色付きスレッド + git 色 + プロジェクト色で方向づけ、下ドックに cwd 一致のターミナル。**live のマルチウィンドウ並行運用の体感確認は人の手番**（機構は完成）

---

## M9 — Remote SSH（「場所が変わっても同じ necoder」）

- [x] local の FS/process/Git/search/save を `Host` 境界へ移し、既存 unit test を維持する
- [x] versioned length-frame RPC + raw body + frame 上限 + multiplex worker + protocol test
- [x] `necoder-remote-server` + system OpenSSH + project root scope + conflict-safe atomic save
- [x] `ssh://[user@]host[:port]/path` で tree/open/edit/search/Git を remote Host へ接続する
- [x] LSP・PTY・ACP を remote process/ControlMaster session へ移す（task 機能自体は未実装）
- [x] ControlMaster・server version/OS/arch 検出・同一 target の versioned binary 自動配備
- [x] daemon/proxy 再接続・5秒 heartbeat・master 再生成・非冪等 request の無条件再送禁止
- [x] protocol/fs/process concurrency・proxy 再接続・外部編集競合の統合 test
- [x] SSH URI の状態復元と status bar の接続先表示
- → **未チェック残件（ベンチ・musl 配布・dirty backup・障害注入 test・Remote Projects UI・受入）は M13 へ移動**（2026-07-15 判断: 日常機能 M10〜M12 を先に消化する）

設計と根拠: [`research/remote-ssh-2026.md`](./research/remote-ssh-2026.md)。

## M10 — 毎日使える（「necoder で necoder を開発する」）

2026-07-15 のギャップ分析（全 crate 棚卸し × `research/feature-matrix.md` 突合）で M10〜M13 を策定。
レイヤ自体は 13/13 に点が打ってあるが「所作」の層が薄い — ここを埋めるとドッグフーディングが始まる。

- [x] **「＋」統一オープン + リモート browse-first** — 2026-07-23。レール ＋ を**統一ランチャー**（`PickerMode::OpenLauncher`・`open_launcher`）に: 固定アクション（フォルダ/ファイル/リモート）＋ **最近（local + remote 混在・opened_at 降順・●識別色＋実行中ドット・remote は `host:path`）** を ⌘O と同じ広い `.palette` に 1 枚で（狭い popover に履歴を詰めない）。`storage.local_projects`（record/recent/forget + round-trip test）を新設し remote `remote_projects` とマージ。**リモート接続の既定を新窓→現レール**（`connect_ssh_and_open`→`open_folder_in_rail`・SSH 接続を再利用・「勝手に窓を開かない」）。**browse-first**: home 接続はブラウズ入口（履歴に残さず hint toast `ssh.browse_hint`）→ explorer フォルダ右クリック **「ここをプロジェクトとして開く」**（`open_dir_in_rail`・`host_for_project` で接続再利用・現レールに再ルート）= 「cd 連打→開き直し」を消す。**検証**: storage 11 / workspace 21 green・i18n parity 322=322。**残**: Docker 実 SSH の browse→開く実地・mock 反映・ライブ体感は人の手番

- [x] **複数タブ** — 2026-07-16。`workspace.rs` の `editor: Option<Entity>` を撤廃し、`tabs: Vec<EditorTab{path,editor,_observation}>` + `active_tab` に。**タブクリック**（`select_tab`）/ **⌘{ ⌘}**（=⌘⇧[ ⌘⇧]・`SelectPrevTab/SelectNextTab`。Zed の `pane::Activate*Item` と同じ字面）切替・**⌘W で閉じて隣へ**（`close_tab_at`＝active 追従は agent スレッドタブと同ロジック）・**ドラッグ並べ替え**（`DraggedEditorTab`+`move_tab`）・**dirty ドット個別**・**同一ファイルは重複タブを作らず既存へ切替**・**LSP didOpen/didClose 追従**（+ `lsp_sent_versions` を path 別 map 化＝複数ファイルで version 番号が衝突しても誤スキップしない）。永続化は `ProjectSlot.open_files: Vec` + `active_file`（プロジェクト単位でタブ列を保存・非アクティブは切替時に遅延復元）。ARCHITECTURE §3 に Pane/Item 初版の型契約を追記。**検証**: cargo test 全 green・offscreen で 5 タブ描画（active=lsp.rs 下線）・state.json 往復（open_files 5件+active_file:4）・無引数再起動で 5 タブ復元を目視。**編集→保存→× の対話ループ体感は人の手番**（状態分離はタブ毎の独立 Entity で構造的に保証）
- [x] **⌘F バッファ内検索/置換** — 2026-07-16。エディタ右上のフローティング検索バー（`render_buffer_search_bar`）。**インクリメンタル**（タイプ毎に `search::SearchQuery::find_in`〔新設・byte レンジのみ・行分割なし＝改行跨ぎ literal も可・20k 上限〕→ 全マッチ = warn 16% 面・現在マッチ = 選択して中央へ〔可視域内なら**スクロールしない** = pending_reveal を賢く〕）+ **n/m カウンタ**（上限超過は「+」・正規表現エラーもここに赤で）。Enter/⇧Enter = 次/前（端で回る）・Aa/`.*` トグル・⌥⌘F/▸ = 置換行（Tab で欄切替・置換欄 Enter = **1件置換して次へ**〔置換文字列が再マッチしても足踏みしない anchor 制御〕・⌘Enter/全置換 = **`Buffer::edit` 複数レンジ 1 Transaction ＝ undo 一発**・表示上限超でも全件対象）・**Esc = 開いた位置へ復帰**（`PositionSnapshot`）・× = 現在位置のまま閉じ。単一行選択があれば ⌘F でクエリに種込み。編集追従は既存 observe + (version,query,トグル) ガード（blink 再入も停止）。タブ/プロジェクト切替では畳む。t! 13 キー（ja/en）。**検証**: workspace 全 test green・offscreen で「render_」1/54（置換行+Replace ボタン）と「resizing」1/12（他マッチ琥珀・現在マッチ選択・statusbar 545:13 追従）を目視。**置換→undo の対話一往復は人の手番**（Buffer::edit 複数レンジ+undo は editor_core test 済）
- [x] **補完の自動トリガ** — 2026-07-16。①`editor_view` に **`EditorInputEvent::Typed`**（入力ハンドラ経由の確定入力のみ emit。IME 変換中・paste・undo・補完適用では出さない）→ workspace がタブ毎に subscribe ②`classify_completion_trigger`（識別子= Identifier / `.`・`::` 2個目= Fresh / 他= None・unit test 9 ケース）③ポップアップを **type-through 化**（印字キーは `insert_text` でエディタへ挿入 → Typed 経由で**クライアント側絞り込み**〔`filter_completion_indices` = 大小無視前方一致・LSP 再要求なし〕・backspace で prefix 縮小・0 件で閉じ）④**Esc 抑止** = 閉じた語の語頭 offset を記憶し同じ語では自動再表示しない（`.`/`::`/手動 Ctrl-Space で解除）⑤世代番号で連打時の古い応答を破棄 ⑥prefix と表示位置は**応答時点**のキャレットで確定（要求中に打った分も反映）。**検証**: 全 test green・**実 rust-analyzer で `t.the` タイプ→ theme/thermal のみの自動ポップアップ（other が prefix 除外）がキャレット直下に出るのを offscreen 目視**（`NECODER_TYPE_PROBE` フック新設）。Enter 挿入は既存 M7 経路（filtered 対応済み）。**タイプ→絞り込み→Enter の対話体感は人の手番**
- [x] **hover の配線** — 2026-07-16。`editor_view` に **dwell 検知**（mouse_move で 500ms タイマー・世代番号で無効化・`EditorHoverEvent::Dwell{line,character,anchor}`）と **Cancel**（クリック/スクロール/dwell 位置から 30px 超の離脱）。workspace がタブ毎 subscribe → `lang::lsp::hover()` を要求（世代番号で古い応答破棄・補完ポップアップ表示中は出さない）→ `parse_hover_lines`（MarkupContent/MarkedString/配列 → コードフェンス行だけ落とすプレーン表示・24 行で truncate）→ アンカー**直上**（入らなければ下）にコード字カード（occlude = ポップアップ上にマウスがある間は消えない）。**⌘K ⌘I**（VSCode 互換）でキャレット位置にも出せる（`workspace::ShowHover`）。タイプ・タブ切替で消える。**検証**: 全 test green・**実 ra で `Thing` 上に `struct Thing { theme: u32, … }` シグネチャのポップアップを offscreen 目視**（`NECODER_HOVER_PROBE` フック）。**マウス dwell の対話体感は人の手番**（キー経路と同一実装）
- [x] **ファイル監視（watch 基盤）** — 2026-07-16。`project::watch_root`（**notify 6.1**/FSEvents・型は外に漏らさない opaque `Watch` = M13 の remote watch への差し替え面）→ workspace の pump（futures channel + **200ms 合流**で cargo build 等の連続イベントを 1 回に畳む・`NECODER_WATCH_DEBUG=1` でトレース）→ `handle_watch_events`: ①**開バッファ**: `Buffer::disk_probably_unchanged`（len+mtime の安価比較 = **自分の保存イベントは無視**）→ 無編集なら `Buffer::reload`（選択クランプ・履歴リセット・LSP didChange は observe 経由で自動）/ **dirty なら警告バー**（⚠+再読込/このまま・上書きは絶対しない・保存時の競合検知が最後の砦）②ツリー = slot 再構築 ③git 色 + gutter diff（`.git/` は index/HEAD/refs のみ合図・objects の湧きは無視。gitignore 対象はノイズ除去 `Worktree::is_ignored`）。プロジェクト切替で張り替え・remote は張らない（M13）。**検証**: 全 test green（reload/クランプ/履歴リセット/None 系 unit test）+ **live: `echo >>` でバッファ 4 行自動リロード・新規ファイルがツリーに U 出現・gutter 緑バー**、**dirty 時は警告バーが出て外部行が混入しない**のを offscreen 目視。受入達成
- [x] **UI スレッド非ブロッキング化（監査 S1+S2）** — 2026-07-16。①アイコン/カラム表示の render 内 `read_any_dir` → **`ProjectSlot::listed_dir`（RefCell キャッシュ・初回のみ読み・watch の refresh で無効化）** ②⌘S 保存 → **`Buffer::prepare_save`/`PendingSave::write`(背景)/`complete_save`**（保存中の編集は version 比較で dirty 温存・競合は従来どおり拒否）③`open_file` → 背景 read + `Buffer::from_content`（**起動復元だけは同期 `open_file_sync`** = 完了順でタブ順が崩れるため）④`refresh_git_status` → **背景 + 世代番号**（status/branch/panel スナップショットを一括収集・古い結果は破棄。呼び出し 15 箇所へ cx 付与）⑤⌘P → `all_files_on`（新設・Rc<Worktree> は Send 不可のため host 直呼び版）を背景 ⑥ブランチメニュー列挙・switch_branch・add_worktree・commit・stage/unstage/削除/作成 → 背景（checkout/commit 系は git_busy 表示・stage 系は `run_git_index_op` 共通ヘルパ）。**検証**: 全 test green・警告 0・offscreen で複数タブ + アイコンビュー + git 色/ブランチ表示が非同期化後も揃うのを確認。**remote 切断での無フリーズ実測は M13 の障害注入 test で**（構造上は全 Host 呼び出しが背景に移動済み。同期 Host 残り = 起動復元・explorer ツリー展開クリック・watch の is_synced 判定〔local メタデータのみ〕）
- [x] **hot exit（クラッシュ耐性）** — 2026-07-16。**`crates/storage` 新設 = Turso 0.7 初導入**（pure-Rust SQLite・専用ワーカースレッド 1 本 + チャネルで直列化し外へはブロッキング API・Turso 型は crate 外へ漏らさない = rusqlite 退避面。`~/Library/Application Support/necoder/necoder.db`・`NECODER_DB` で上書き可）。**書き込みファイルは sqlite3 CLI でもそのまま読める**（SQLite 互換フォーマット実証）。workspace: 編集で **2s デバウンス**の背景スナップショット（dirty=upsert / clean=行削除。**バッファ version ガード**＝blink の notify では再予約しない）・タブ閉じ/保存で破棄・**⌘Q（正常終了）で全クリア**・起動時に候補があれば**復元/破棄バー**（復元 = `replace_all_text` 1 Transaction ＝ undo で戻れる・未オープンは開いてから適用・**未決の間は tick が書きも消しもしない**）。**検証**: storage unit test 2（round trip・再オープン=クラッシュ相当）+ **live: タイプ→ tick → `kill -9` → DB に全文残存（dump で確認）→ 再起動で「復元候補 1 件」検出 → 自動復元 → 復元後に再スナップショット**の一部始終をログで確認。受入達成（バー/復元後の PNG も撮影済・目視は Read 復旧後）
- [x] **ツリーのファイル操作** — 2026-07-16。右クリックメニューに**新規ファイル / 新規フォルダ / 名前を変更… / 複製 / ゴミ箱に入れる**（local のみ表示。remote 版は M13 の Host 拡張と一緒）。命名は**ツリー行のインライン入力**（rename=対象行を入力行に置換・New*=親フォルダ行の直後に挿入〔ルートなら先頭〕・Enter 確定/Esc 中止・アクセント枠+キャレットバー）。実装: `project` に `create_file_local`/`create_dir_local`/`rename_local`（上書き拒否）/`duplicate_local`（`name copy.ext`・衝突は `copy 2`…・dir 再帰）/`trash_local`（**/usr/bin/trash → Finder AppleScript fallback = 完全削除しない**）+ unit test。開いているタブの rename/trash は**先にタブを閉じる**（旧パスへの保存でファイルが復活する事故防止・v1）。新規ファイルは作成後すぐ開く。ツリー再構築 + git 色更新 + watch でも追従。**検証**: project unit test（create/rename/duplicate/衝突拒否）green + **live: 命名入力→ Enter 確定→実ファイル生成をエンドツーエンド確認**（`NECODER_NAMING`/`NECODER_NAMING_CONFIRM` フック）。メニュー/入力行の PNG 撮影済（目視は Read 復旧後）。**マウスだけの一巡は人の手番**
- [x] **編集の所作一式** — 2026-07-16。**ロジックは全部 editor_core**（GPUI 非依存・unit test 8 群）: ①単語境界 `prev/next_word_boundary`（識別子/記号の 2 クラス・空白スキップ・256B 窓・日本語も 1 語）→ ⌥←→/⇧⌥←→/⌥⌫ ②行操作 `move_lines`（隣と入れ替え・最終行の改行なしも正しく・端は no-op）/`duplicate_lines`（複製方向でキャレット位置を使い分け）/`delete_lines` ③`toggle_comment`（全非空行コメント済みなら外す・零幅レンジ+同一テキストで **1 Transaction=undo 一発**・prefix は `lang::comment_prefix` 表 = //・#・-- の 3 系）④`insert_newline_indented`（前行インデント継承 + `{([:` 直後は 1 段深く）⑤`indent_lines`/`outdent_lines`（⌘] ⌘[・Tab は選択があれば行インデント/なければ空白挿入）⑥**括弧/クォート自動ペア** `classify_pair_input`（Pair/Wrap（選択を囲んで内側を再選択）/SkipOver（打ち抜け）/Insert。クォートは単語隣接でペアにしない=lifetime・アポストロフィ対策・開き括弧は直後が識別子なら素通し）— editor_view の入力ハンドラに介入（IME 変換中・明示レンジ・composer は素通し）。キーは既定 keymap に 17 本追加（⌥←→・⌥⌫・⌘↑↓・⌥↑↓・⇧⌥↑↓・⌘⇧K・⌘/・Tab/⇧Tab・⌘[ ⌘]）。tab_size は暫定 4（M10-13 で設定配線）。**検証**: editor_core 32 test 全 green（新規 8 群: 境界/単語削除/行移動/複製削除/コメント/インデント改行/インデント/ペア分類）・全 suite green・警告 0。**キー操作の実機体感は人の手番**
- [x] **multi-cursor の UI 配線** — 2026-07-16。editor_core に ①`select_next_occurrence`（⌘D: 空選択→`word_range_at` で単語選択・以降は次の一致を追加選択〔wrap・選択済みスキップ・全選択済みで false〕）②`add_cursor_vertically`（⌥⌘↑↓: 端の選択と同列で上下に追加・行末クリップ・端で停止）③`add_cursor_at`（⌥クリック・重複追加なし）④`collapse_to_primary`（Esc: 複数→先頭 1 個。単一なら no-op = 他オーバーレイの Esc を邪魔しない）。view はアクション 4 本 + ⌥クリック（ドラッグ選択を開始しない）+ ⌘D 後は新しい選択へ reveal。**同時入力/削除/ペースト/キャレット複数描画は既存**（buffer.insert が全選択に効く・prepaint は全選択を描く）。keymap: ⌘D・⌥⌘↑↓・Esc。**検証**: unit test 3 群（**受入そのもの =「⌘D×3 → 3 箇所同時書き換え → undo 一発」をテスト化**・縦追加のクリップ/端停止・collapse）全 green。**実機のキー体感は人の手番**
- [x] **ナビゲーション履歴** — 2026-07-16。workspace に back/forward スタック（(path, byte offset)・上限 100・連続同位置は積まない）。**記録ポイント**: F12（定義応答の着地直前）・⌘P 確定・⌘⇧F 検索ジャンプ・**大距離クリック**（editor が 50 行以上の移動で `EditorInputEvent::CaretJumped{from}` を emit → 移動前位置を記録）。**⌃- 戻る / ⌃⇧- 進む** = 現在位置を反対スタックへ → 対象へ（同ファイル=即 reveal・開タブ=切替+reveal・**閉じたファイルは背景読みで開き直してから着地**）。keymap `ctrl--`/`ctrl-shift--` はパース検証済み（起動時の全アクション解決に警告なし）。**検証**: 全 test green・警告 0。**F12→⌃- の実機一往復は人の手番**（両経路とも既検証の select_byte_range/open 経路の合成）
- [x] **soft wrap + 行ジャンプ** — 2026-07-16（設計は JOURNAL に先行記載）。**WrapMap**（論理行→表示行。`segments`=行ごとのセグメント開始 byte 列 + `prefix_rows`=累積で両方向 O(log n)・(version,列数,on/off) 変化時のみ再構築・**off は恒等マップ=挙動不変を構造で保証**）。折返しは**等幅セル数**（ASCII=1・東アジア全角=2 = unicode-width・単語境界優先/長語は文字切り。バンドル等幅フォント前提でピクセル shaping 不要）。**描画の仮想化を表示行に変更**: 可視表示行→セグメント部分文字列だけ shape・行番号/diff/診断は先頭セグメントのみ・選択/検索/キャレット/IME は絶対 byte 範囲がそのまま機能・キャレットのセグメント帰属は「境界は次セグメント・論理行末は最終セグメント」。**↑↓/⇧↑↓ は表示行移動**（セグメント内相対 byte 維持・マップが古い 1 フレームは論理行フォールバック）。クリック位置・スクロール上限・reveal も表示行系。設定 `soft_wrap`（settings_core 追加・既定 off）+ **⌥Z** トグル + `NECODER_SOFT_WRAP=1`。**⌃G 行ジャンプ** = ミニオーバーレイ（数字のみ・Enter で `reveal_position(n-1,0)`・ナビ履歴にも積む）。**検証**: WrapMap unit test 2 群（語境界/CJK セル/長語/round trip/恒等）+ 全 suite green・警告 0・長行 Markdown（英文+全角）のスクショ撮影済（目視は Read 復旧後）
- [x] **設定の実効化** — 2026-07-16。①**font_size/tab_size を editor_view のフィールド化**（`set_typography`・行高は 23/13 比で追従・shape/仮想化/ヒットテスト/ガター全て可変化・clamp 8〜32/1〜16）②エディタ生成時に適用 + **observe_global で live 配布**（`apply_editor_settings` = 全タブ + 分割へ。soft_wrap も同経路）③**ユーザー keymap.json**（settings と同じディレクトリ）を既定の**後**に bind = 同キーはユーザー勝ち・壊れた項目は skip ④keymap.json を**ファイル監視で live reload**（200ms 合流・再 bind = gpui は後勝ち）。**検証**: 全 test green・**live: 起動中に `necoder config set font_size 19` → watcher 経由で反映**（スクショ撮影済・元値へ復元済）・**ユーザー keymap が「1 束」適用されるログを実機確認**（テスト用ファイルは後始末済み）。受入達成
- [ ] 受入（総合・ドッグフーディング開始）: **necoder で necoder を丸 1 日開発できる**（この日から自分の変更を自分で浴びる）

## M11 — 言語知能と Git の parity（「他人のリポジトリを違和感なく直せる」）

- [x] **フォーマット** — 2026-07-17。基盤 = **`Buffer::edit_batch`**（レンジ毎に異なるテキストの一括置換・1 Transaction=undo 一発・重なりは先勝ち防御・unit test）。`lang::lsp::formatting`（tabSize は設定値）→ `parse_text_edits` → `EditorView::apply_lsp_edits`（UTF-16→byte 変換・**キャレット (行,列) 復元・スクロール維持**）。**⌥⇧F**（workspace::Format）+ **⌘S を workspace::SaveActive 化** = `format_on_save`（settings_core 追加・既定 off）が有効なら整形→保存の連鎖・LSP 不在/対応外は素の保存へフォールバック。応答時に別ファイルへ移っていたら適用しない誤爆ガード。**検証**: 全 test green + **live: 崩れた .rs → プローブで整形→保存 → ディスク上で rustfmt 品質の整形を確認**（`NECODER_FORMAT_PROBE=1`）。受入達成
- [x] **rename（F2）** — 2026-07-17。F2 = キャレット下の単語を初期値にミニ入力（LSP 対応ファイルのみ）→ `textDocument/rename` → **`apply_workspace_edit` 共通経路**（code actions と共用）: `parse_workspace_edit`（changes / documentChanges 両対応・ファイル操作系は v1 スキップ）→ **開いているタブはバッファへ**（`apply_lsp_edits`・dirty のまま・undo 可）/ **未オープンはディスク直書き**（`apply_text_edits_to_string` = UTF-16→byte を行索引で・revision 条件付き書き込み = 外部変更と衝突しない）。適用数を報告（バッファ n・ディスク n・失敗 n）。unit test（UTF-16/行跨ぎ/順不同適用・changes/documentChanges パース）。**検証: 実 ra で `Thing`→`Renamed` — 開バッファ 1 + 未オープン other.rs のディスク直書き 1・失敗 0 を live 確認**。受入達成
- [x] **code actions（⌘.）** — 2026-07-17。`publishDiagnostics` の**生 JSON も保持**し、キャレット行の Diagnostic[] を context に渡して `textDocument/codeAction` → タイトル一覧ポップアップ（補完と同型・上下/Enter/Esc/クリック）→ **edit 即適用 or `codeAction/resolve` で遅延解決**して `apply_workspace_edit`（rename と共通基盤）。**罠 = initialize capabilities**: `codeActionLiteralSupport` + `resolveSupport(edit)` + `dataSupport` が無いと **ra はアクションを一切返さない**（無応答で 1 回踏んだ）。`workspace.workspaceEdit.documentChanges` も併せて宣言。**検証: 実 ra で「Remove all unused imports」→ resolve → 適用 → 保存 → ディスクから import 行が消える**のを live 確認。受入達成
- [x] **参照検索（⇧F12）** — 2026-07-17。`textDocument/references`（includeDeclaration）→ `locations_to_file_matches`（Location[] をファイル別に集約・**背景でファイルを読んでプレビュー行を生成**・UTF-16→byte 変換・unit test）→ **⌘⇧F の検索パネル UI をそのまま再利用**（クエリ表示 =「参照: <単語>」・クリック/Enter でジャンプは既存経路）。スクショ撮影済（目視は Read 復旧後）
- [x] **シンボル** — 2026-07-17。①**⌘⇧O アウトライン** = `lang::outline`（tree-sitter クエリ駆動・fn/struct/enum/trait/impl/mod/const・定義順・unit test。LSP 不要）→ Picker（fuzzy 絞り込み・確定で行ジャンプ + ナビ履歴）②**⌘T** = LSP `workspace/symbol`（空クエリの初期 200 件 → Picker の fuzzy でローカル絞り込み・containerName + ファイル名を detail 表示・確定は `open_file_then` で背景読み完了後に着地）。**⌘⇧O のスクショ撮影済**（目視は Read 復旧後）。**このついでに重大回帰を修正**: M10-6 の open_file 非同期化で「開いてからジャンプ」系（検索ジャンプ/F12 別ファイル/⌘T）が未オープンファイルで**旧バッファに誤 reveal** → 合流ヘルパ `open_file_then` を新設し 3 箇所を修正
- [x] **診断一覧 + F8** — 2026-07-17。statusbar の ✗▲ を**クリック可能化** → 生診断（raw_diagnostics）から**ファイル別一覧を検索パネル UI で**（プレビュー行 = ✗/▲ + メッセージ 1 行目・クリック/Enter でジャンプは既存経路）。**F8/⇧F8** = アクティブファイルの次/前の診断行へ（wrap・行重複除去）。keymap 4 本追加
- [x] **tree-sitter 多言語** — 2026-07-17 実装（JOURNAL「★M11 実装項目を全消化」）・**2026-08-06 ドリフト解消でチェック**（ROADMAP 未更新のまま実装済みだった）。`lang.rs` の言語表 `language_and_query` = rs / js·jsx·mjs·cjs / ts / tsx / py / go / json·jsonc / yml·yaml / toml / html·htm / css（grammar crate は tree-sitter-language shim 経由・root Cargo.toml）。`comment_prefix` は //・#・-- の 3 系（html/css/md はブロックコメントのみ＝v1 非対応と明記）。受入「.ts/.py がハイライトされ ⌘/ が正しい」= 言語表 + prefix 表で成立・`cargo test -p lang` 16 test green（2026-08-06 再確認）。**残: Markdown grammar のみ**（inline/block の二重 grammar で別枠・ハイライト対象外のまま）
- [x] **増分パース + didChange 差分化** — 2026-07-17 実装（M11-8）・**2026-08-06 ドリフト解消でチェック**。`IncrementalHighlighter` = `Tree::edit` + 差分パース + **可視 byte 範囲だけ** QueryCursor（512KB 上限撤廃・incremental==full の等価 unit test）。LSP は `did_change_incremental`（サーバが Incremental sync を広告する時のみ・`editor_area/language.rs` で配線確認済み）
- [x] **diff エディタ** — 2026-07-17。**一時タブ基盤**（`EditorTab.transient` = 永続化・⌘⇧T・LSP から除外）+ **`Buffer::set_read_only`**（edit/edit_batch/undo/redo をコアでガード = 全編集経路が構造的に止まる）。`project::unified_diff_on`（imara-diff の UnifiedDiffBuilder・HEAD vs バッファ）を背景生成 → タブ名「file.rs ⇄ HEAD」で開く（既存 diff タブは開き直し）。**F7/⇧F7 で hunk ヘッダ（@@ 行）間を移動**。入口 = gutter ポップオーバー「diff をタブで開く」+ `workspace::OpenDiff`。スクショ撮影済
- [x] **hunk 操作** — 2026-07-17。editor の **gutter diff バークリック検知**（座標→表示行→論理行→該当 hunk）→ `HunkClicked` emit → ポップオーバー（**stage / 巻き戻し / コピー / diff をタブで開く**）。stage = `hunk_patch_text`（1 hunk 分の unified diff を組む）→ `apply_patch_to_index_on`（一時ファイル経由 `git apply --cached --unidiff-zero` = host 汎用）。巻き戻し = バッファ上で HEAD 行に置換（**undo 可**）。**検証: 実 git repo で「2 hunk の片方だけ stage → staged に +zero のみ・unstaged に +THREE が残る」round trip を unit test 化** = 受入そのもの。green
- [x] **blame（インライン）** — 2026-07-17。`project::blame_line_on`（`git blame --porcelain -L n,n`・「作者, YYYY-MM-DD • 要旨」・未コミット行は「未コミット」・日付は依存なしの civil 変換 + unit test）。workspace が**キャレット行の変化だけ**を検知（同一行の blink は無視）→ 400ms デバウンス + 世代番号 → 背景 blame → `EditorView::set_line_annotation` → **行末に dim 表示**（最終セグメントのみ・幅が入る時だけ）。**実履歴テスト**（この repo の README で blame 合成を検証）green。dirty バッファは HEAD 基準の近似（注記）
- [ ] 受入（総合）: 他人の TypeScript リポジトリを clone → 読む（アウトライン/参照/hover）→ 直す（rename/quickfix/フォーマット）→ hunk stage でコミット、まで necoder 内で完結 ← **機能は全部実装済み（各項目で個別検証済み）。通しの体感確認は人の手番**。注: TS の LSP は `typescript-language-server` がインストールされている前提（ルーティング済み）・アウトラインの tree-sitter クエリは現状 Rust のみ（TS は ⌘T = LSP シンボルで代替。クエリ追加は後続）

## M12 — AI の唯一無二（「色と通知で、並行エージェントに迷わない」）

FEATURES で later タグの checkpoint / @mention / ⌘K / Todos をここへ前倒し採用（2026-07-15 分析。タグ自体は不変更 = ユーザー管理）。

- [x] **スレッド永続化** — 2026-07-17。**Turso threads/turns テーブル**（turn 毎 INSERT の追記型・直近 200 turn 復元・updated_at 降順・archived で一覧から除外＝台帳の行は残る）。スレッドに永続 id（unix ms+連番）。**TurnEnded/Failed で未永続 entries を追記 + メタ upsert**（失敗時は進めない=次回リトライ）。起動時 `set_storage` で mock 種を置き換えて復元（無ければ種のメタだけ登録・mock 会話本文は書かない）。継続送信 = 新セッションで続き（ACP session/load は不使用・ROADMAP 許容のフォールバック。resume 調査は残メモ）。**検証: 実 Claude Code で送信 → user/agent/step の turns 行・トークン実測 26.4k の永続化を sqlite3 で確認**（storage unit test も round trip/limit/archive/台帳）
- [x] **チェックポイント / 巻き戻し** — 2026-07-17（**方式 = content-addressed ブロブストア。比較表 → DECISIONS 決定ログ**）。`storage`: checkpoints/checkpoint_files テーブル + **SHA-256 blob**（`blobs/<hh>/<hash>`・重複排除・tmp→rename・依存なし SHA-256 は FIPS ベクタ test）。**記録は PermissionRequest 受信時 = 書かれる前**（手動承認/AUTO_ALLOW の一本道）。**old_text が diff に無いツール（Claude の Write）はディスクの現内容を背景で読んで変更前として保存し、自動許可の応答はスナップショット完了後**（= エージェントが書く前に読む・レース防止）。transcript に「⟲ checkpoint」行 → クリックで blob から全ファイル書き戻し（背景・watch が開バッファを自動リロード）。**検証: 実 Claude Code で「元の内容です」→ エージェントが「工業」に書換 → blob に変更前が保存 → restore → ディスクが「元の内容です」に復帰**の全往復を live 実証。unit test（round trip・重複排除で blob 2 個・新規ファイル=None→削除）
- [x] **エージェント編集の生中継** — 2026-07-17。即反映 = **M10-5 watch がそのまま担う**（外部変更の自動リロード・live 検証済）。帰属表示 = `PanelEvent::FilesTouched` → workspace が該当ファイルの開いているエディタへ `set_agent_mark_color(スレッド色)` → **gutter の diff バーがスレッド色**に。bypass モード（権限リクエスト無し）は FilesTouched が出ないため色は付かない＝リロードのみ（既知の限界・diff が来ないので変更前も取れない。注記）
- [x] **スレッド⇄成果物の色リンク** — 2026-07-17（コア部分）。`Thread.touched_files`（承認時に記録・`touched_color_for`/`active_touched_files` API）→ workspace の `agent_touched` map → **ツリー行にスレッド色ドット**。ターン完了トーストに「触ったファイル n」。**残: タブ側ドットとチップ→diff 導線は後続**（ツリードットで受入の判別は可能）
- [x] **ターン終了サマリー + 通知** — 2026-07-17。`PanelEvent::{TurnEnded, PermissionWaiting}` → ①**右下トースト**（スレッド色枠・5 秒自動消滅・最大 4 枚・サマリー = 経過秒 + 触ったファイル数）②**statusbar に権限待ちスレッドの pulse ドット**（クリックで Agent パネルへ・TurnEnded で消える）。**残: Dock バッジ**（gpui の badge API 要調査・±行サマリーも後続）
- [x] **diff レビューの本体化** — 2026-07-16。承認カードに「**エディタで開く（diff タブ）**」→ `PanelEvent::OpenDiffRequest`（old/new テキスト）→ workspace が `unified_diff_texts` で **read-only の transient diff タブ**を開く（`pending_transient_tab` 経由 = subscribe に window が無い GPUI 制約の迂回。render 冒頭で消化）。カードの compact diff は要約として残しつつ、大きな編集はタブで全変更を見てから accept/reject を押せる。検証: 結線のコードパス確認（diff タブ描画自体は M11-9 で offscreen 実証済み）
- [x] **@mention の完成（コア）** — 2026-07-16。`＋context` を **fuzzy 絞り込み**化: 開くと入力フォーカス（`context_query`/`context_focus`）・タイプでサブシーケンス一致（`fuzzy_matches`）・Enter で先頭候補を添付・候補は全ファイル（`all_files` 上限 60→**2000**）。受入の「3 秒で @ 参照」は fuzzy で成立。**残: フォルダ / 選択範囲 / ターミナル出力の mention は後続**（ファイル mention が本丸・送信時 `@path` 先頭付与は M4 から）
- [x] **⌘I インライン編集** — 2026-07-17。**キーは ⌘I**（⌘K は cmd-k cmd-t / cmd-k cmd-i のコードプレフィクスと衝突するため変更・グローバル bind でターミナルでも効く）。**方式は `claude -p` 型**（ai_commit_message と同型。当初案の ACP 経由から変更 — セッション起動コスト無し・権限フロー無しで「チャットへ行かない最短経路」の目的を最短で満たす。指示+コードは**一時ファイル経由**で shell 引用安全）。選択範囲（無ければ現在行）→ 指示入力バー → `project::inline_rewrite_on`（コードフェンス剥がし・末尾改行を元に合わせる）→ **その場に diff プレビュー**（-旧/+新・中央省略・`inline_edit_diff_lines` は unit test）→ **適用 ⏎ / 破棄 esc**（1 Transaction = ⌘Z 一発・バッファ version ガードで適用時不一致は安全側破棄）。**ターミナル同型**: ターミナルフォーカス中の ⌘I = 自然言語→zsh コマンド 1 行生成→**入力行へ挿入のみ**（実行はユーザーの Enter・`TerminalView::insert_text` は改行を落とす）。**検証: 実 claude で「Result<u16, String> を返すように」の全往復を live 実証**（`NECODER_INLINE_PROBE`: busy 表示 → diff プレビュー offscreen 目視 → `NECODER_INLINE_ACCEPT=1` で適用+保存 → ディスクが map_err 版に書き換わったのを確認）。ターミナル側の対話確認は人の手番。既知: 初回検証 1 回だけ「提案が自動適用に見えた」事象（ディスク不変・再現せず・監視）
- [x] **Todos（プラン）表示** — 2026-07-17。ACP `SessionUpdate::Plan` → `AgentEvent::Plan(Vec<PlanItem>)`（content/status の UI 非依存型）→ Thread.plan に**全量置換**（ACP 仕様通り）→ **transcript 上部の常設チェックリスト**（`render_plan`: ヘッダ `Todos n/m`・`☒` 完了 fg2 / `●` 進行中 **スレッド色** / `☐` 未着手。プランが無ければ非表示）。UI-SPEC §5 の「ステップ内 ☒/☐ 表示」から**常設へ仕様更新**（文書先行で修正済み）。検証: `NECODER_PLAN_PROBE` で 3 状態を注入し **● がスレッド色で付くのを offscreen 目視**（受入の表示要件）。実 Claude Code adapter が Plan を送るかはターン内容依存のため live は自然発生待ち（イベント配線は ACP 型で保証）
- [x] **Todo ボード** — 2026-07-17。真実 = **`.necoder/todos.md`**（`project::todos`: 行番号保持パース・トグルは該当行の `[ ]`↔`[x]` だけ書き換え・他は 1 バイトも動かさない。unit test 4 本）。**レール ☑**（Lucide square-check・`RailSettings.todos`）→ 左カラム板（explorer/git と排他）: 日付見出し・`☐/☑` クリック = **ファイル書き換え**（= 他の書き手と同じ経路）・**▶ でアクティブスレッドへ送信**（「完了したら todos.md の該当項目を [x] にせよ」自動付与・実行中は**スレッド色 pulse ドット**・TurnEnded で解除+再読込）。**watch が .necoder/todos.md を検知 → 板を自動再読込**（どの書き手でも追従）。**✨ 今日の計画** = `todos::daily_plan_on`（git status+ROADMAP+未消化を `claude -p` へ・出力ノイズ防御 `parse_plan_lines` は実測ベースの unit test・`date +%F` で今日見出しへ追記）。**検証（live・全部実 Claude）**: ①外部プロセスの追記が watch 経由で板に出るのを offscreen 目視 ②**▶ 送信 → Claude が parse_port を改善 → 自分で todos.md を `[x]` に書き換え → 板のチェックがひとりでに入り未消化 4→3・pulse 解除** ③その一部始終が **git diff に `- [ ]`→`- [x]` として残る**のを確認 ④✨で 3 タスクが今日の節へ追記。**残: 逐次消化モード**（解禁条件 checkpoint は済・UI トグルは次スレッド）・手動チェックの対話確認は人の手番・`necoder mcp` todos ツールは任意のまま
- [x] **`.necoder` の色/アイコン + 色ピッカー** — 2026-07-16。`read_project_identity`（`.necoder/settings.json` の `color`(hex)/`icon`(絵文字) を `parse_hex_color` で読込・プロジェクト slot へ配線 = レールのモノグラム/色に反映）+ **レール右クリック → 色ピッカー**（パレット 12 色・クリックで `persist_user_value` により `.necoder/settings.json` へ保存 → 再起動後も貫通）。受入経路（色/絵文字 → レール/ピル/キャレット）はプロジェクト色の既存貫通に乗る
- [x] **プロジェクト色を Peacock 相当に拡張**（M12-11 の続き）— 2026-07-19。①**パレット一本化**: `theme_core::IDENTITY_PALETTE_HEXES`（巡回5色 + 予約色と衝突しない厳選5色 cyan/chartreuse/violet/magenta/graphite）を新設し、ピッカーのシンタックス色寄せ集め12色（Claude バレット/スレッド色/選択面と衝突）を撤去 = 巡回もピッカーも出所を1本化。②**任意 hex 入力**: ピッカーに rename 式の hex 入力行（`parse_hex_color`→既存 `apply_project_color`・6 桁揃いで確定・不正は無視）。③**導線**: `ProjectColor` アクション + **⌘K⌘C**（⌘K⌘T に並置）+ `command_entries` に 1 行（コマンドパレット「表示: プロジェクトの色を変更」・キーボード起動は `open_project_color` がアクティブなレール項目にアンカー）。④色適用の波及を**全ペイン（タブ + 分割）**へ（旧: アクティブ 1 枚）。**remote 別色は入れない判断**（project=slot が固有色を持ち Peacock の remoteColor 問題が起きない・「どのマシンか」の識別は §1.3 の窓縁 2px 枠が担う）。i18n `cmd.project_color`/`color.hex_placeholder` を ja/en に。検証: `cargo test`（ja/en parity 含む）green + **offscreen 目視**（10 スウォッチ識別可・hex 行 `# rrggbb`/`# 34d3b6`・ピッカーがアクティブ項目にアンカー・`NECODER_COLOR_PICKER` プローブ追加）。**残: ライブの ⌘K⌘C / hex 確定の対話確認は人の手番**
- [x] **⌘O の 2 階層化 + worktree ダッシュボード** — 2026-07-17。UI-SPEC §7 通り**1 リストの 2 階層**: プロジェクト行（**行頭●プロジェクト色** + 名前 + パス）+ 配下にインデントで **`⎇ branch` 行**（worktree 名・`✓`現在・`↑ahead ↓behind`・dirty `●`・**右端に実行中スレッドの色ドット列**）。開いた瞬間はプロジェクト行のみ → **背景で全プロジェクトの `git worktree list` + `git status --short --branch`**（`worktree_status_on`・パーサ `parse_status_branch` は unit test）→ `Picker::set_items` で差し込み（開く速さを守る）。**実行中ドットの正体 = `RunningRegistry`（GPUI Global・全窓横断）**: 各窓の AgentPanel が dest_cwd キーで (名前,色,running) を送信開始/TurnEnded で上書き → どの窓の worktree で何が走っているかを ⌘O 一発で一望。確定: プロジェクト行=切替（従来）/ worktree 行=レールに居れば切替・居なければ**新窓で開く**（ウィンドウモデル §5）。PickerItem に `accent`（行頭●）/`dots`（右端色ドット列）を追加。**検証: 実 Claude を走らせながら ⌘O を開き、`⎇ main ✓●` に実行中スレッド色ドット・`⎇ feature` に dirty ● が並ぶのを offscreen 目視**。**残: 最下部「＋ worktree を作って開く」行と ⌘⏎ 新窓**（⎇ メニューの既存導線で代替可）・3 worktree 並行の体感確認は人の手番
- [x] **トークン台帳** — 2026-07-16。`storage::token_ledger`（turns テーブルの集計クエリ・別ストア不要）→ スレッドタブ列の左端に **Σ 累計チップ**（アクティブプロジェクトの全スレッド累計）。検証: unit test（集計クエリ）+ 実 DB の Σ 23.4k 表示を offscreen 目視
- [x] **スレッド名: 改名 + AI 自動命名**（Claude 拡張風）— 2026-07-19。タブ **ダブルクリック**で `EditorView::plain`（IME 正しい）のインライン改名（`Thread.name_is_custom` で以後の自動命名から保護）+ 初回ターン後の **`claude -p` 自動命名**（`project::name_thread_on`・既定名かつ未手動改名のみ・`settings.agent_auto_name` 既定 on・`is_placeholder_name` で再命名防止）。offscreen でタブのアクセント枠入力を目視。**live（IME 改名・実 claude 命名）は人の手番**。詳細 JOURNAL 2026-07-19。
- [x] **スレッド履歴ビュー**（Claude 拡張風）— 2026-07-19。`storage::load_all_threads`（archived 含む・unit test）+ Agent タブ列 **🕘 ボタン / ⌘⇧H / パレット「AI: スレッド履歴」** → `PickerMode::ThreadHistory`（`open_picker` 再利用・行頭●スレッド色 + Σ トークン detail・updated_at 降順）→ 選択で復元（`open_thread_from_history`・開いてれば切替）。offscreen で 3 スレッド色付き一覧を目視。⌘⇧T（直近1件）の全件ブラウズ版。
- [x] 受入（総合・当初ビジョンの完成形）— 2026-07-17 **機構完成**: worktree 並行（⎇/⌘O 新窓）× スレッド色貫通 × 通知（トースト/pulse）× diff レビュー（承認カード→タブ）× checkpoint 巻き戻し（live 実証済）× **⌘O ダッシュボードで「どこで何が走っているか」一望**が全部繋がった。各要素は個別に live 実証済み（M12-2/3/5/8/10/12 の検証欄）。**worktree×3 の 3 スレッド並走を色と通知だけで捌く「体感」の最終確認は人の手番**（M9 受入と同じ扱い）
- [ ] **トランスクリプトのパスをクリックで開く**（2026-08-27 追加・ドッグフーディング要望）: エージェント返答内の `path(:line)` らしきトークンと http(s) URL をリンク化。現状は `md_highlights` が色＋下線を付けるだけで `on_click` が無い（`agent_panel.rs` の `render_markdown`）。方針: `terminal_view::find_path_links`（unit test 済のパーサ）を共通 util へ切り出して transcript でも流用 → クリックで `OpenPath` 相当イベント → `workspace/panels.rs` の既存解決（絶対 / `~` / worktree 相対 → 該当行へジャンプ）に乗せる。URL と `.html` / 画像などエディタ向きでないファイルは **OS 既定（ブラウザ等）で開く**分岐を持つ（`open` コマンド or gpui の open_url）。受入: エージェントが「`crates/foo/src/bar.rs:42` を直した」と言ったらクリック一発で該当行に着地し、`docs/index.html` ならブラウザが開く
- [ ] **composer への画像添付（drop / paste で送信）**（2026-08-27 追加・ドッグフーディング要望・優先高）: 現状 ACP 送信は text のみ（`SessionCommand::Prompt(String)`・画像 paste は `EditorView::paste` が text 以外を捨てる）。Zed の message_editor 方式に倣う: ① `initialize` の `prompt_capabilities.image` を確認 ② `acp_client` の Prompt を content block 列（text + `ContentBlock::Image`）へ拡張 ③ composer への画像ファイル drop（現 drop は `@path` 添付のみ）とクリップボード画像 paste を受けて**添付チップ**（サムネ or ファイル名）表示 ④ 送信時に base64 image block として同梱。image 非対応エージェントへはパス文字列で送るフォールバック。受入: スクショを composer に drag&drop → 送信 → Claude Code が画像の内容に言及した返答を返す（live 実証）

## M13 — 公開準備（「英語話者が DL して 10 分で使い始める」）

旧 M9 の未チェック残件をここへ統合（2026-07-15 判断: 日常機能 M10〜M12 を先に）。

- [x] **コマンドパレット ⌘⇧P + CommandRegistry** — 2026-07-17。`command_entries()` 登録表（(i18n キー, アクション名) 39 個 — 新アクションは 1 行足すだけ）→ Picker 再利用で「カテゴリ: 名前 + **キー併記**」（`keymap_core::key_for_action` 逆引き + `pretty_keystroke` 記号化 ⌘⇧P・unit test）。確定 = **パレットを閉じてから `cx.build_action`→`window.dispatch_action`**（フォーカス復帰後 = Editor/Workspace コンテキストで解決）。**検証: live で「Terminal」を確定 → 下ドックに実シェルが開く**（dispatch 経路の実動）+ 一覧描画 offscreen 目視（`NECODER_PALETTE_PROBE`）。StatusItemRegistry は未（statusbar は現状直書き・残件）
- [x] **Workspace ownership refactor** — 2026-07-20。約 13,000 行・約 95 直接フィールドの `workspace.rs` を、8フィールドの shell + `ProjectSessions` + 長寿命 `ProjectSession` + `EditorArea` + feature Entity へ再編。`explorer` / `git_ui` / `search_ui` は workspace 非依存、child subscription は `PanelRegistry`、palette action は `CommandRegistry` に集約。通常の rail 切替は active index だけを変更し、dirty/undo、Editor Entity、Agent session、Terminal process を保持する。root Render の pending effect は effect-cycle 末尾へ遅延し、Host/FS/Git/DB を描画中に呼ばない。`workspace.rs` は合計 1,399 行（production body 975 行、残りは完了監査 test）。`d83578a` で公開 API/actions/i18n/state 互換、A→B→A の dirty/undo/child Entity 保持、local/remote root draw の Host call 0、release probe 不在を自動検証し、9 UI 状態を offscreen 目視済み。全 workspace test / release check green。詳細は [`REFACTOR-WORKSPACE.md`](./REFACTOR-WORKSPACE.md)。
- [x] **i18n の回収** — 2026-07-17。ハードコード UI 文字列 **98 箇所**を `t!` 化（レール/エクスプローラ/検索/git/タブ/ターミナル/dock/テーマ/agent パネル。変数付きは `%{name}` 置換）+ パレット 39 コマンド名 + welcome/update 系 = **locales は計 ~250 キー**（ja/en parity テスト green）。**検証: `locale=en` 実機スクショ**（git パネル = Source control/✓ Commit/History・agent = Idle/Send ⌘⏎・Terminal 1 等が英語）。対象外と判断: eprintln 開発ログ・seed スレッドのモック会話・ユーザーデータ
- [x] **入力レイテンシベンチ（コア分 + CI ガード）** — 2026-07-17。`editor_core/examples/bench_editor`（依存ゼロ Instant 直測・insert/undo/snapshot/byte_to_point・**予算超過で exit 1**）を **CI（ci.yml）で release 実行** = 予算超過で CI が fail する。実測は全項目 ~1µs で予算内。起動時間はローカル計測スクリプト（`scripts/startup-time.sh`・NECODER_STARTUP_LOG 対応済み）。**残: key→frame ヒストグラム（zed input_latency_ui 相当）は GUI 実機が要るため CI 外**（人の手番・実機で回す）
- [x] **自動アップデート** — 2026-07-17（自前・velq/karui 同型）。`workspace::updater`: 起動 10s 後に GitHub Releases latest を確認（当初 90s・2026-08-27 に短縮）（curl・失敗は静か・NECODER_NO_UPDATE_CHECK/スクショ時はスキップ）→ 新版なら **statusbar に「⬆ vX.Y.Z に更新」チップ** → クリックで .dmg DL → **Apple 署名/公証を検証**（`spctl --assess` — CI が codesign+notarize 済みなので自前鍵不要）→ `hdiutil`+`ditto` で実行中 .app を差し替え →「⟳ 再起動で反映」。semver 比較・release JSON パースは unit test（prerelease/draft 除外・.dmg アセット抽出）。チップ描画は offscreen 目視（`NECODER_UPDATE_PROBE`）。**残: 実 GitHub Release での E2E（初回リリース時に確認）**
- [x] **About モーダル + メニュー「アップデートを確認…」** — 2026-08-30（ユーザー要望: About が右下トーストなのは Mac らしくない）。①`workspace/about.rs` 新設: About を**ネイティブ About パネル準拠のモーダル**に（アイコン `icon/necoder.png` を Assets に追加 / 名前 / vX.Y.Z / タグライン / necoder.com・GitHub リンク / © AGPL-3.0。shortcut_sheet と同じ器 = 薄暗背景・背景クリック/Escape で閉じる）。②**手動アップデート確認を About に集約**（Sparkle の作法）: 新アクション `CheckForUpdates` = アプリメニュー「アップデートを確認…」（About の直下）+ パレット 2 項目（about / check_updates）。`updater::check_for_update_manual` は自動チェックと違い**結果を言い分ける**（Ok(Some)=新版 → 自動チェックと同じ `updater.status` に合流＝チップも同時に出る・[今すぐ更新] は既存 `install_update` 経路 / Ok(None)=「最新です」/ Err=ネット断を赤字）。多重 spawn は `ManualUpdateCheck::Checking` ガード。i18n ja/en +12 キー。**検証**: offscreen 目視（`NECODER_ABOUT=1`・モーダル全要素）+ `NECODER_MENU_PROBE` でメニュー 12 項目化 + 実 GitHub API で tag/assets 取得確認 + 全 test green
- [ ] **Linux ビルド**（部分）: **CI に check-linux ジョブを追加**（ubuntu + Wayland/X11 依存 apt → `cargo check`・安定するまで continue-on-error）— 2026-07-17。**残: 初回 CI 実行での追従修正・実 Ubuntu での起動/編集/保存/ターミナル確認（実環境必須）**。**進捗（2026-07-21）: aarch64-linux（`host` crate = GPUI 非依存）は実 Linux（Docker）で `necoder-remote-server` の動作を実証済み**＝Linux ターゲットの一部（remote 側）は生きている。GUI 本体（gpui）の Ubuntu 起動は別。
- [x] **ターミナル仕上げ** — 2026-07-17。**file:line リンク**: `find_path_links`（cargo/rustc/grep 形式の手書きパーサ・unit test）→ prepaint で表示セルから行再構成 + 検出 → **下線描画** + paint 登録の mouse listener（セル座標→リンク hit）→ `TerminalEvent::OpenPath` → workspace が root 基準で解決し **open_file_then で該当行へ**（`--> sample.rs:28:9` の下線 + 28 行目着地を offscreen 実証）。**IME**: `EntityInputHandler` 最小実装（確定文字列を PTY へ・候補ウィンドウはカーソル位置 = 日本語入力が可能に）。**残: 実クリックの hit 判定と IME の対話確認は人の手番・変換中テキストのインライン表示は後続**
- [x] Remote（旧 M9）**benchmark** — 2026-07-21（**Docker で実環境ブロッカーを解消**）。Docker の Linux ホスト（ubuntu:24.04・aarch64・sshd 鍵認証）へ実 SSH 接続し `host` 統合テスト（[`crates/host/tests/remote_ssh_live.rs`](../crates/host/tests/remote_ssh_live.rs)）で計測: **round-trip read avg 0.352ms / remote server 常駐 RSS 6.5MB / idle CPU 0.00%**（localhost 予算で assert 付き自動検証）。**対 VSCode Remote の肝＝node を両側に置かない**が数値で出た（node server 数百 MB に対し musl static 1 個 6.5MB）。
- [x] Remote（旧 M9）**musl 配布 + checksum + cleanup** — 2026-07-21。**aarch64 musl** の `necoder-remote-server` を cargo-zigbuild で建てて（2.4MB static-pie ELF・x86_64 は CI 済み）配備・接続まで実証。`ensure_remote_server` に **checksum 検証**（両端 sha256sum/shasum で突合・不一致は bad binary を消して bail・`parse_sha256_hex` unit test）と **古い version の cleanup**（現行以外の `servers/<ver>/` を掃除）を追加＝Docker で「偽 `0.0.0-p0` を仕込む→再配備で消え現行だけ残る・remote/local sha256 完全一致」を実証。**残: GUI askpass**（設計上 鍵/agent 委譲で不要寄り・後続）・**署名 manifest**（自己配備では checksum で足りる）。
- [x] Remote（旧 M9）**remote watch / remote hot exit / handle 再同期** — 2026-07-21。①**remote watch push**: protocol に `Watch`/`Unwatch` request + `Event` frame を配線。daemon の接続スコープに poll 差分マネージャ（無 watch 時は完全ブロック＝idle 0%・使用中のみ 700ms）→ 変更を Event frame で共有 writer へ push。client の RpcClient reader が Event を `WatchEventSink` へ demux、`RemoteHost::watch`→`HostWatch`、keeper が再接続（generation 変化）で自動再購読。`project::watch_root` を host 対応化し workspace の pump を remote でも張る（`handle_watch_events` は host 非依存でそのまま流用）。**live 実証: out-of-band 編集→`README.md` 通知 / ControlMaster kill→再接続→再購読→`notes.md` 通知**（`remote_ssh_live.rs` watch×2 green）。②**remote hot exit（host-scoped）**: storage の hot_exit に **`scope` 列**（local="local" / remote=SSH URI・作り直しマイグレーション）＝local と remote の同一絶対パスの衝突を解消。snapshot/close/restore を scope 対応化し、復元は**現アクティブ host の scope だけ**適用（取り違え防止）。**unit test: 同一パス×別 scope が別レコード green**。③**LSP/PTY handle 再同期**: `spawn_process`/`terminal_launch` は `ensure_master` を呼ぶので再接続後の再 spawn が成立。`HostProcess::is_alive()`（死亡検知プリミティブ）を追加。**live 実証: spawn→ControlMaster kill→再 spawn で stdio が通る**（process 再spawn test green）。**残: consumer 側の自動再 spawn（lang の LSP didOpen 再送 / terminal 再起動）と remote 未保存の reconnect-on-restore**（scope 衝突は解消済み・「起動時に remote を自動再接続して未保存を書き戻す」は後続）。
- [x] Remote（旧 M9）**障害注入 test** — 2026-07-21。`remote_ssh_live.rs` に実 SSH の障害注入を追加し green: **ControlMaster kill →** 次 request が master 再生成 + 再接続で回復（`RemoteHost::debug_stop_master`）/ **server(daemon+proxy) kill →** 新 daemon 立ち上げ + project 開き直し（scoped_request の OpenProject 再送）で回復 / **巨大 tree（5000 ファイル）→** `list_files(limit)` が 1.3ms で打ち切り（ハング/爆発なし）。CRUD/検索/command（revision 競合含む）も同ファイルで end-to-end 実証。加えて `start_master` に **`ConnectTimeout=10`** を新規追加（死んだ/到達不能ホストの無限ハングを断つ＝sleep/VPN 断の復帰性）。**残: 実マシンでの sleep/VPN 断長時間**（Docker では kill で近似）。
- [x] Remote（旧 M9）**Remote Projects UI・SSH picker・構造化接続ログ** — SSH config ピッカー（`~/.ssh/config` 列挙・2026-07-19）+ 最近のリモートプロジェクト履歴（storage・2026-07-20）は実装済み（**ドリフト解消**＝既に done だった）。2026-07-21 に **構造化接続ログ**（`connect_ssh` の各フェーズを stderr へ所要 ms 付きで出力 + `.context()` でフェーズ別失敗文脈＝トーストに「remote-server の配備に失敗: …」等が出る）を追加。**残: retry/cancel ボタン・localhost port forwarding**（`ConnectTimeout` で hung は 10s で断つ・再接続はピッカー再選択で足りる）。
- [ ] Remote（旧 M9）受入: remote Linux で編集/Git/LSP/terminal/agent を一日使い、切断復帰しても未保存変更を失わない ← **機構は Docker で end-to-end 実証済み**（CRUD/検索/command/git・障害注入・配備・benchmark・**remote watch・hot exit host-scoped・handle 再同期**）。**残: consumer 側 LSP/PTY 自動再 spawn と remote 未保存の起動時 reconnect-復元・実マシン長時間運用**。切断→再接続→再購読/再 spawn/未保存の scope 分離は Docker で実証済み。
- テスト seam（2026-07-21 追加）: `NECODER_SSH_CONFIG`（`-F` 透過＝ユーザーの `~/.ssh/config`/known_hosts を汚さず CI/Docker で remote テストを回せる）・`RemoteHost::debug_stop_master`（障害注入）・`NECODER_WINDOW_SIZE`（縦長パネルのスクショ検証）。
- [x] **初回起動体験** — 2026-07-17。空状態 = 「necoder — 色による方向感覚」+ **最初の 4 手のキーバッジ**（⌘O 開く / ⌘P ファイル / ⌘⇧A AI スレッド / ⌘⇧P 全コマンド・i18n 両対応・offscreen 目視）。README に **英語 Quick start** + hero スクリーンショット（docs/images/）+ 古い記述の修正（GPUI git 依存・ライセンス戦略）
- [x] **設定画面（`render_settings`）** — 2026-07-21。エージェント選択に加え **「動作とエディタ」セクション**を新設＝settings.json を唯一の真実に、UI から直接操作（`set_user_value` が永続化 + observe_global 波及で全ビュー即反映）。**トグル5**（submit_on_enter / soft_wrap / format_on_save / agent_auto_name / completion_sound・スライドスイッチ）+ **ステッパー2**（font_size 8–32 / tab_size 1–16・[−]値[+]）+ **セグメント**（agent_tabs_view = 横タブ/縦リスト）+ テーマ hint（`⌘⇧T`）。i18n ja/en 追加（parity green）。**検証: offscreen で全コントロールが seed 値を正しく反映**（submit=ON / soft_wrap=OFF / format=ON / auto_name=ON / sound=OFF / font 16 / tab 2 / Thread tabs=List 選択）を目視。**残（任意）: theme/locale/density/rail.* の UI 化**（真実は settings.json＝手編集・CLI・監視でも効く）。**エージェントアイコンの出所**: repo にロゴを同梱せず ACP レジストリから実行時取得（Zed 方式・未知は sparkles フォールバック）＝ DECISIONS「再ライセンス自由」と両立。
- [x] **panic hook + クラッシュログ + バグ報告導線** — 2026-07-22。①`workspace::crash` 新設: main() 冒頭で hook（全スレッド・既定 hook へ連鎖 = stderr 表示は温存）→ `~/Library/Application Support/necoder/crashes/crash-<unix秒>-<pid>.log`（version/OS/thread/location/backtrace・新しい 20 本だけ保持）+ `pending` マーカー → **次回起動で 1 回だけ** statusbar「⚠ 前回終了時にクラッシュ」チップ（`theme.warn`＝診断と同じ警告色・識別色は使わない §1.3）→ クリックで **GitHub new issue をブラウザで開く**（title/body に環境〔version/OS/sw_vers/locale〕+ ログ末尾抜粋を事前記入・percent-encode 自前・**自動送信はしない = telemetry never を維持**）②⌘⇧P「ヘルプ: バグを報告」= `ReportBug` アクション（ログ無し版・CommandRegistry +1 行）③`.github/ISSUE_TEMPLATE/bug_report.yml`（手動起票用・日英併記）。unit test 6（marker 往復 1 回消費/prune/URL 組み立て/抜粋の行単位切り詰め/encode）。**検証: 実 E2E** = `NECODER_PANIC_PROBE`（背景スレッド panic）→ ログ+マーカー生成 → 再起動でチップを offscreen 目視 → マーカーが 1 回で消費されるのを確認（`NECODER_CRASH_DIR` で隔離・スクショ時は隔離 dir 指定時のみ読む = ユーザーの実マーカーを消費しない）。**残: チップクリック→ブラウザ実起動は人の手番。repo public 化までは Issue URL は本人のみ到達可**
- [x] **テスト増強（ベータ前の信頼性）** — 2026-07-22。①**editor_core ランダム操作ファズ** `tests/random_edits.rs`（依存ゼロ SplitMix64・失敗メッセージにシード = `NECODER_FUZZ_SEED` で単発再現・`NECODER_FUZZ_ITERS` で強化）: **厳密系** = insert/delete_backward/forward/undo/redo を参照実装（String + 状態スタック）と毎手突合（テキスト・選択位置・undo/redo の可否まで）/ **全部系** = 全公開操作（行操作・コメント・インデント・multi-cursor・edit_batch 等 18 種）を乱打し大域不変条件（選択が常に範囲内 + char 境界・snapshot 整合・行再構成=全文・byte↔point 往復・undo 全巻き戻し=初期文・undo 回数ぶん redo=最終文）。**初回実行で実バグ 1 件検出** = `move_lines`/`duplicate_lines`/`indent_lines` の手動選択シフトが multi-cursor × マルチバイト時に範囲外/char 境界外へ → `set_selections` 経由（クランプ）に修正。500 iter release ランも green（0.2s = CI 常設コストほぼゼロ）②**データ完全性テスト追加**: prepare/write 間の外部変更→競合拒否で両者の作業が残る・外部削除後も reload 失敗でバッファ保持・save_as 後の競合検知が新パスへ束縛 + 既存 round-trip に「自己保存は `disk_probably_unchanged`=true（watch の自己イベント無視が成立）」「書き込み中編集は 2 周目保存でクリーン化」を追記 ③**保存意味論の改善**: **外部削除は競合にしない**（上書きで壊す相手が居ない）= 消されたファイルも ⌘S で作り直し**未保存の作業を救出**できる（`write_file_local` 一箇所 = local/remote daemon 共通・VSCode 同挙動・git checkout 中の保存で詰まない。host unit test 追加）
- [x] **macOS ネイティブメニューバー** — 2026-07-23。`crates/necoder/src/menus.rs` 新設（**Zed と同じ置き場**＝メニュー = アプリの組み立て = bin crate）: `cx.set_menus` で **8 メニュー**（necoder 11 項目 / File 10 / Edit 27 / View 14 / Go 15 / AI 5 / Window 2 / Help 1）+ `cx.set_dock_menu`（New Window）。既存アクションへの参照だけで構成（keymap/パレットと**同じ dispatch 系**・キー表記 ⌘S 等は gpui が keymap から自動付与・Cut/Copy/Paste/Undo/Redo/SelectAll は `OsAction` 対応付きで IME/NSText 系と整合）。**新設アクション 7**: `OpenSettings`（⌘, 新バインド・設定画面を開く・パレット「表示: 設定を開く」も追加）/ `About`（バージョントースト）/ `Hide`・`HideOthers`・`ShowAll`（cx.hide 系）/ `Minimize`・`Zoom`（window.minimize/zoom・⌘M/⌘H/⌥⌘H 新バインド）— handlers は workspace root の on_action 鎖。Services は `os_submenu`（OS 提供）。ラベルは i18n `menu.*` **72 キー ja/en**（parity green）。ユーザー keymap の live reload 後は `set_menus` を呼び直してキー表記を追従（set_menus はスナップショットのため）。**検証**: `NECODER_MENU_PROBE`（`get_menus` 読み戻し = mac 実装は `setMainMenu_` と同時に owned copy を保持 → **OS 登録の機械的証拠**）で 8 メニュー+項目数を確認 + **OpenSettings をパレット実 dispatch**（メニュークリックと同一経路）→ 設定画面が開くのを offscreen 目視。全 test green（46 suite）・警告 0。**残: 実メニューのクリック一巡は人の手番**（screencapture/AppleScript は画面収録・補助アクセス権限でブロック = 自動化不可の領域）
- [x] **Open Recent + 開く導線の VSCode 化 + 擬似 tear-off** — 2026-07-23。①**File メニューを VSCode 並び**（New Window → **開く…**=`OpenDialog`（ネイティブダイアログ）→ **最近使った項目を開く…**=`OpenRecent`（ランチャー起動・データは storage の recent_local/remote_projects 既存）→ ⌘P/⌘O/SSH → 保存 → タブ）+ パレット 2 コマンド追加 ②**開く意味論 = 原則 workspace**（Enter = レールに追加して切替・従来どおり）+ **⌘⏎ = 新しいウィンドウで開く**（Picker に `ConfirmedSecondary` イベント新設・VSCode の Open Recent と同じ操作系。対応行はランチャーの最近〔ローカル〕・他モードは通常確定へフォールバック）③**Dock 連携**: `record_recent_project` が local 開きで `cx.add_recent_document` = Dock 右クリックに OS 標準の最近リスト ④**擬似 tear-off**: レール項目をドラッグ（24px 閾値）→ **窓の外で離すとその位置に新窓**（`tear_off_rail_item` = 窓 origin+窓内座標 → `open_source_as_window_at`・レールから外す〔最後の 1 個は残す〕）。押下窓がドラッグ中もマウスをキャプチャし続ける性質を利用＝**本物の tear-off（Chrome 式・窓が付いてくる）は gpui にクロスウィンドウドラッグが無く fork 級なので不採用**（rev 固定方針と衝突・Zed も未実装）。**検証**: File メニュー 13 項目の OS 登録（`NECODER_MENU_PROBE`）・OpenRecent 実 dispatch → ランチャー表示（offscreen 目視）・`NECODER_TEAROFF_PROBE` で **windows 1 → 2**（新窓生成の機械検証）・i18n parity/keymap/workspace/ui test green・警告 0。**残: 実マウスでのドラッグアウト一巡と ⌘⏎ の対話確認は人の手番**
- [ ] **完了音の磨き**（後続・優先度低）: ①**window 非フォーカス時のみ**鳴らす（見てる最中は鳴らさない）②**独自チャイム同梱**（macOS `Glass.aiff` → 自前 wav + `rodio` でクロスプラットフォーム）。**やるのはクロスプラットフォーム化 or 公開ブランディングのタイミングで十分**（今は system sound で機能する）
- [ ] **エージェントタブ Grouped ビュー**（要設計判断）: List の第3案 = プロジェクト⎇ブランチで束ねる縦リスト。**現モデルは「1 パネル=1 プロジェクト」**で同一パネル内スレッドは同一 dest のため、①**全ウィンドウ横断集約**（`RunningRegistry`・⌘O ダッシュボードの右ドック常設化）か ②**スレッドが別ブランチ dest を持てるモデル拡張**、のどちらを取るか決めてから着手
- [x] **CLI（`ne <path>` で開く・VSCode の `code` 相当）** — 2026-08-30（FEATURES §13 の v1 消化）。①**`crates/cli_shim` 新設**（GPUI 非依存）: `/usr/local/bin/ne` に置く委譲シムの生成/検出/設置/削除（他所製の同名は上書き・削除とも拒否・`NECODER_BIN=` 行が機械可読の印・unit test 3）。②**`necoder cli` サブコマンド**（`crates/necoder/src/cli.rs`）: パスを cwd 基準で絶対化 → **実行中 GUI がいれば IPC `open` で転送 + 前面化**（Turso 排他ロック＝二重インスタンスを作らない・単一 writer 原則の延長）、いなければ .app を `open -n`（dev バイナリは process_group 切り離し spawn）で起動。ssh:// は素通しで新インスタンス。`ne fleet/config/mcp` はシムが素通し。③**管制 IPC に `open` メソッド**（control_ipc）: 存在パスを `chrome.pending_external_open` → effect cycle 末尾で Finder と同じ `open_external_paths` に合流・`cx.activate(true)`・skipped を応答。④**設定画面「コマンドライン」セクション**（導入系の直後）: 状態表示（未設置/設置済み→実体パス）+ インストール/削除ボタン。/usr/local/bin に書けない時は **osascript 管理者ダイアログ**（VSCode/Zed 同型・prompt は i18n）。⑤`install-mac.sh` が差し替え時に**旧実体を指すシムを向け直し**（未設置なら案内のみ＝sudo を勝手に求めない）。**検証**: cli_shim 3 + cli 2 unit green・**実 e2e**: 隔離 sock/DB で GUI 起動 → `necoder cli proj-b` → 「実行中の necoder で開きました（1 件）」→ スクショでレール 2 項目 + proj-b アクティブを目視。設定画面セクションも offscreen 目視。**残**: 実設置（`ne` 本番投入）は次回 install-mac.sh 実行時（現 /Applications 版は cli 未対応の旧バイナリのため）
- [x] **公開整備一式（リリースレビュー消化）** — 2026-08-06。公開レビュー（ROADMAP 突合 + リポジトリ全数スキャン）の指摘を全部消化: ①**バージョン配線一本化**（Cargo.toml が唯一の出所 → bundle-mac.sh が Info.plist へ注入・release.yml がタグ一致検証＝上げ忘れで「更新チップ無限」になる実害バグの根治）②**署名を公証仕様に**（--deep 廃止・内包 Mach-O→bundle の個別署名・`necoder.entitlements` 新設 = apple-events）③ DMG に /Applications リンク・Info.plist 拡充（13.0 / DocumentTypes / developer-tools）④**Finder/Dock からのファイルオープン**（`on_open_urls`→チャネル→`open_external_paths` 新設・フォルダ=レール追加/ファイル=タブ・無プロジェクト時は親フォルダごと）⑤**GUI 起動ログ**（`workspace::logging` 新設 = stderr が /dev/null の時だけ dup2・logs/ 20 本保持・bug_report.yml に導線）⑥ updater/管制 IPC/fleet_view の **i18n 化 +26 キー**（EN ユーザーに日本語エラーが出ていた）⑦ **cargo-deny CI 常設**（deny.toml・licenses/sources/advisories/bans 全 green・storage の Cargo.toml 逸脱修正）⑧ **受け入れ設備**（CLA.md + cla.yml + CONTRIBUTING + SECURITY + CoC + PR テンプレ + issue config + CHANGELOG）⑨ **README を EN ユーザー向けに全面改稿**（.dmg 手順・「GPL 資産の移植元」等の危険文とローカルパスを除去・DECISIONS §5 に現状サマリ注記）⑩ 残骸掃除（chatgpt-images 10MB・タチコマ画像 3 枚+参照 4 箇所・fixture の実ホスト名・個人名コメント）。手順の正: [`RELEASE.md`](./RELEASE.md)
- [ ] 受入（総合・**人の手番**）: 英語話者が GitHub から DL → 10 分で「開く・編集・保存・検索・AI に 1 タスク」まで到達できる。**ソフト側の導線は完備**（署名 dmg CI・自動更新・EN UI・welcome・⌘⇧P）— 残るは [`RELEASE.md`](./RELEASE.md) のチェックリスト（secrets 投入 → public 化 → v0.1.0 タグ → 実 DL で通し確認 → v0.1.1 で更新 E2E）を実際に回すこと

## M14 — マルチエージェント編隊（「並走するエージェントを色とグラフと動きで指揮する」）

2026-07-20 着想。herdr の「設定不要の状態一覧」を比較対象にし、ACP と necoder の型・要件から編隊機能を実装する。**状態に色相は使わない**（色=識別・状態=形と動き＝UI-SPEC §1.3/§6/§8 の `activity_dot`）。設計の正は UI-SPEC §11 ＋ mock の編隊モード（グラフ = 扇形（Fan）既定 ＋ ツリー/カード/ハブ）。M13 末尾の「エージェントタブ Grouped ビュー（要設計判断）」は **②スレッドが別ブランチ dest を持つモデル拡張**で決着（この M14 が本体）。追加するのはエージェント自身が編隊を操作する necoder API（#3-4 相当）で、プラグイン市場（#5）は不採用。

- [x] **TaskSpace-first への再設計（2026-07-22）** — 正は `FLEET-ARCHITECTURE.md`。既定 `+ Task` = `task/*` branch + linked worktree + ProjectSession + 完全な AgentPanel。cell は thread でなく TaskSpace、同一 Task の追加 Agent は panel 内 thread。Terminal / Editor / Diff / Tests surface、stable Space ID、Task/Agent storage scope、lifecycle/event ledger、Conflict Radar、explicit Integration、CLI/MCP create/list/status/wait/review/integrate を実装。

- [x] **herd サイドバー（状態一覧の常設パネル）** — 状態基盤は **2026-07-20 実装済み**（別セッション・JOURNAL 参照）: `ThreadActivity{Idle,Working,Blocked,Done{interrupted}}`（Blocked=`pending_permission` / Working=ストリーミング / Done=未確認ラッチ / Idle=待機）・共有 `activity_dot`（スレッド色 × 形と動き）・statusbar 中央ロールアップ「N 実行 · M 承認待ち · K 完了」・レール/beacon/⌘O の状態化・`AgentKind.icon`・状態つき `RunningRegistry`。**残 = それを左ドックの専用パネルに常設**（「専用パネルは作らない＝`RunningRegistry` の上に安く足せる」方針の回収）: `activity_dot` ＋ エージェントアイコン ＋ ⎇ ＋ 1行状態 ＋ トークン・プロジェクト別グループ ＋ focus 中の worktree ツリー。受入: 3スレッド並走で状態一覧が形と動きで左に出る。**2026-07-20 本実装**: 左ドックに専用ボード `render_herd_sidebar`（explorer/git/todo と排他・レール ⚡ アイコン `icons/activity.svg` ＋ `RailSettings.herd` ＋ コマンドパレット「表示: 編隊」＋ `ToggleHerdSidebar` ＋ `NECODER_HERD` プローブ）。**プロジェクト別グループ**（各 `ProjectSession.agent_panel` を横断＝非アクティブ project も含む）で、行 = 左 2px スレッド色バー ＋ `activity_dot`（形と動き・**色相は識別のまま §1.3**）＋ エージェントアイコン（`agent_badge`）＋ 名前 / ⎇ブランチ・1 行状態 ＋ トークン（`human_tokens`）。ヘッダに実行中/承認待ち/完了ロールアップ・末尾に中立色の 4 状態凡例（形の説明）。行クリックで該当 project ＋ スレッドへ（focus-follows の入口）。`agent_panel` に `AgentStatus`/`statuses()`/`focus_thread()` を公開。**検証: 全 test green（i18n parity 含む）・警告 0 ＋ offscreen で 3 スレッド（Working=満 / Blocked=リング / Done=リング）が blue/amber/purple の識別色 × 形で左に並ぶのを目視**（`NECODER_HERD=1`＋`NECODER_ACTIVITY_PROBE`・titlebar beacon / statusbar ロールアップ / レール ⚡＋project ドットと一斉一致）。**2 プロジェクト起動（necoder/crates）で別グループが出る＝プロジェクト別グループも offscreen 実証**（非アクティブ crates の session を跨いで読み・Idle=淡）。受入達成。**下段 = focus 中エージェント（＝アクティブ project）の worktree ツリー**（mock `.fleet-files`・git 色つき・`render_tree` 再利用・ヘッダ「ファイル · <スレッド名> の worktree」・行クリックで `switch_project` 追従。herd は explorer と排他表示なので重複せず、herd ビュー中の唯一のファイルツリーになる）も実装＝mock の `.fleet-left`（herd ＋ fleet-files の2段）に整合。**残 = 左エクスプローラ/エディタタブ/下ターミナルまで別 worktree へ張り替える完全な focus-follows は M14 #3**（スレッド↔worktree 帰属モデル拡張＝M13-7 ② が前提）
- [ ] **統合ターミナルの状態推定（非 ACP・独立実装）** — `terminal_view` の `TerminalContent.cells` を走査するヒューリスティックを、公開された端末出力と necoder の状態型から実装する。PTY 子プロセス（claude/codex/…）＋画面パターンで `ThreadActivity` を推定。スコープは necoder 内の統合ターミナルのみ。受入: 統合ターミナルで `claude` を直起動→承認待ち/作業中/待機が同じ `activity_dot` で出る
- [x] **編隊グリッド ＋ worktree 帰属** — TaskSpace cell に完全な AgentPanel を埋め込み、worktree 固有の host/cwd/context と storage scope を全非 active session にも設定。Task / Terminal / Editor / Diff / Tests surface を 1..8 自動配置する。
  - **方式決定（2026-07-20・ユーザー）= ①「worktree = 切替 space」**（②スレッド帰属拡張ではなく、worktree を同ウィンドウのレール/herd に **space=`ProjectSession`**〔独立 terminal/editor/explorer〕として並べ `switch_project` で丸ごと追従）。**一部実装済み**: worktree を space 化する機構は既存（`open_folder_in_rail`／⎇ メニュー＝新窓でなく現ウィンドウのレールに追加）。herd をブランチ見出しの「**worktree space 一覧**」化 ＋ `refresh_all_git_status`（全 slot のブランチを起動時に埋める）。**実 git worktree（main / feat/auth）を2 space で起動 → herd に `⎇ main`/`⎇ feat/auth` の2グループが並ぶのを offscreen 実証**。**(a) 済（2026-07-21）= 編隊 ＋タイルの「Agent＋⎇worktree（隔離）」ボタン**＝`add_worktree_agent` が `git worktree add …-agent-<n>`（branch `agent/<n>`）を作り `open_folder_in_rail` で space 化 → その space に新エージェント起動 → セル追加（アプリが worktree を所有・ACP には切らせない）。**方式決着（2026-07-21・ユーザー）= ③ハイブリッド B ＋ 設定 A 化**: 既定=素の ＋Agent は同 space・設定 `fleet_agent_worktree`（既定 off）on で常に worktree（herdr 純度）。**残 = (b) 複数 space 同時表示の編隊グリッド（今は1 space ずつ切替）・(c) 切替で下ターミナルが別 cwd になる可視デモ（構造は per-session terminal で保証済み）**
- [ ] **系譜グラフ（最重要ビジュアル）** — ネイティブ描画の DAG Element（webview 不使用）。**扇形（Fan）既定** ＋ ツリー/カード/ハブの4表示をヘッダの**アイコン＋語**スイッチャーで切替・⌄ で折り畳み。レーン/ノード=スレッド色・**先端=状態（形と動き・色は据え置き）**・プロバイダバッジ。データは `git log --all --parents` でレーン自前計算。受入: 4体が base から fan-out し、作業中先端が脈動・承認待ちが速い脈動・完了はリング→main 統合、が**色を足さずに**読める
  - **編隊モード ＋ 系譜グラフ 4 表示 実装済み（2026-07-21）**: titlebar 右上「Multi Agent」トグル（`chrome.fleet_mode` / `ToggleFleet` / パレット / `NECODER_FLEET`）で全画面を mock の編隊ビューへ置換 = レール | herd サイドバー | 中央（**系譜グラフ ＋ N分割グリッド ＋ ニュース**）。`fleet_view.rs` 新設。**系譜グラフはネイティブ 4 表示 + スイッチャー**（`GraphView{Fan,River,Tree,Card}` + ⌄ 折り畳み・`gpui::canvas` + `PathBuilder` cubic bezier・割合座標で 1 paint 統一・ノード＝`activity_dot`）＝ **扇形/リバー(太帯)/縦ツリー/カード**を offscreen 全実証。**グリッドは対話式**（＋ で Agent/Terminal 追加・× で閉じ・列数自動・**フル画面 flex_1 セル**）＝ Agent セルに**実 transcript**、Terminal セルに**実端末**（`terminal_dock` 埋込）。ノード/セル click → focus（`switch_project`+`focus_thread`）。**残 = (a) データを `git log --all --parents` の実レーンへ（今はスレッド由来＝全 main）(b) 複数 Terminal の独立端末化 (c) 完了ノードの main 統合アニメ (d) ニュースを時系列イベントログに**
- [x] **編隊操作 API（エージェントが編隊を動かす・#3-4）** — `necoder fleet create/list/status/wait/review/integrate` と同等 MCP 6 tools。永続 Task ledger を真実にし、wait は process restart を跨ぐ。review/integrate は GUI と同じ Git safety gate を使う。
- [x] **ニュースフィード ＋ 通知の細部（2026-07-23・FLEET-CONTROL-PLAN P2 として実装）** — ソースは当初案の `PanelEvent`/`RunningRegistry` でなく **`task_events` の鏡**（`NotificationCenter.news`・P1 の digest が載る）: 起動時に `load_recent_task_events(60)` で backfill + `transition_task_space` が台帳へ書くのと同じ場所で live 追記（`news_text_for_phase` 写像・将来の監督の采配も同じログ＝`NewsKind::Coordinator` を語彙として確保）。編隊下ドックを mock `fleet-dashboard.html` 下段の書式へ: 時刻（相対）+ 帰属チップ（スレッド/Task 色・coordinator は丸）+ 太字名 + イベント文・新しい順・h118。**エージェント別ミュート** = `Thread.muted`（herd 行の 🔔/🔕・muted 中は常時表示）→ トースト抑止（ニュースには載る＝見えるが鳴らない）。**通知音は window 非アクティブ時のみ**（`window_active` を render で追跡・ミュートスレッドは鳴らさない）。受入実証: offscreen で 承認待ち/review_ready(完了)/failed が下フィードに時系列で出るのを目視・i18n parity green
- [ ] **管制（Control）＝ 編隊統括ダッシュボード・遷移スナップショット・監督・Herdr sidecar** — 2026-07-23 設計確定。**実装計画の正は `docs/FLEET-CONTROL-PLAN.md`**（P0 縫い目一本化 → P1 digest Tier1 → P2 ニュース → P3 管制タブ → P4 ✳要約 → P5 fleet API 拡張(spawn 断絶解消) → P6 監督席+依存待ち → **P9 リモート管制(QR・スマホ)** → P7 Herdr 常駐 → P8 observer/テスト常駐）。**P9 を P7/P8 より前に繰り上げ**（2026-07-25・ユーザー判断。P9a/b は P7 に依存しない）。ビジュアルの正: `mock/fleet-dashboard.html`。上のニュースフィード項目は同計画の P2 として消化する
  - [x] **P0 縫い目の一本化（2026-07-23）** — `TaskPhase`/`SpaceKind` を storage crate へ単一定義（`fleet.rs::PHASES` 廃止・workspace は再輸出）。Integration は phase から外し `SpaceKind::Integration` に分離（DB は phase 列 sentinel で無 migration 互換）。`TaskSpaceRecord` を型付き（kind+phase）へ。**遷移の唯一の入口 = `Storage::commit_task_transition`**（snapshot upsert + `task_events` 追記を同一 transaction・IntegrationSpace 拒否）で GUI `transition_task_space` と CLI/MCP `update_task` が同一コードパスに。受入実証: `necoder fleet create/status`（scratch repo）で planned→working・不正 phase は有効値一覧つき拒否・全 test green
  - [x] **P2 ニュース常設（2026-07-23）** — 上の「ニュースフィード ＋ 通知の細部」を P2 として消化（詳細はそちら）
  - [x] **P6 監督席（Coordinator seat）+ 依存待ち（2026-07-24）** — **depends_on**: `task_deps` 別テーブル（既存 DB 無 migration）+ `TaskSpaceRecord.depends_on` + CLI/MCP `fleet depend <id> <on...>` / `fleet wait-deps <id> <phase>` / `fleet wait` は **phase（台帳）と activity（GUI live・idle/working/blocked/done/interrupted）の両対応**に拡張（MCP 16 tools）。**監督 = 任命制のただの ACP スレッド**: 任命は settings `coordinator_agent`（プロジェクト設定 `.necoder/settings.json` でも可・既定ドリフト禁止）→ IntegrationSpace の panel に pinned thread「監督」（`ensure_named_thread`・名前で再利用・自動命名から保護）。**wake はイベント駆動**（常駐ポーリング禁止）: Task の Done/Failed 遷移で即時・Blocked は 15s 閾値（worry と同じ・すぐ人間が許可したら起こさない）・実行中は重ねない。テンプレートは役割/規律（**手を動かさない・integrate は人間 gate**）/ fleet CLI 全道具（`current_exe` の実パス入り）。**采配の監査**: 監督ターン完了 → `coordinator` イベントとして task_events + ニュース（丸チップ）。受入実証（実運転 e2e）: 監督（実 claude）が task A の完了 wake で起き、**fleet CLI だけで両タスクを review → merge_ready へ進め、「統合は人間が承認する段階」と采配を残して停止**（= M14 総合受入の自走部分。integrate 人間 gate 遵守を実挙動で確認）。depend/wait-deps/activity 待ちも実地スモーク済み・全 52 test green
  - [x] **P5 fleet API 拡張（spawn 断絶解消・GUI ライブ制御 IPC・2026-07-24）** — `~/.necoder/gui.sock`（0600・`NECODER_GUI_SOCK` で差替・SUN_LEN 制限に注意）の 1 行 JSON プロトコル。**新 CLI/MCP**: `fleet spawn-agent <id> [agent] [prompt]`（worktree を GUI が開いて thread 起動 = 空スレッド使い回し `acquire_thread`・Task セル追加・prompt 送信）/ `fleet send` / `fleet digest`（**3 段圧縮**: 事実層+Tier1+キャッシュ済み Tier2 のみ・transcript は返さない。GUI 不在時は台帳 fallback）/ `fleet events [since]`（差分読み `load_task_events_since`）— MCP は 15 tools に。**単一 writer の発見と確立**: Turso は排他ロック＝GUI 稼働中は headless から DB を開けない → **GUI 稼働中は台帳の読み書きを GUI の storage ハンドル（単一ワーカー）へ IPC で集約**（create/status/wait/list/events 全部にロック検出→IPC fallback を実装。GUI 側 method: task/tasks/update_task/record_task/events・update_task は開いている slot なら GUI の遷移入口＝ニュースにも載る）。受入実証（実 e2e）: **GUI 起動中に headless で `fleet spawn-agent`（実 claude ターン実行）→ `fleet wait review_ready` が通り、result_summary にエージェントの返答が確定・`fleet digest` が GUI メモリの live thread（digest/tokens 24.4k）を返す**＝「GUI に thread が現れる」をデータで実証。全 test green
  - [x] **P4 Tier 2（✳ 要約）+ 監督バー総括（2026-07-23）** — **タスクレベル**: Done/Failed 遷移（`turn_finished`）で `maybe_tier2_summary` → 汎用化した `project::oneshot_line_on`（`name_thread_on` から切り出し・既定 Agent の oneshot CLI = スレッド命名と同一機構）で 1 行生成 → `Thread.tier2`・`PanelEvent::SummaryReady` → workspace が `task_events` に kind `tier2` でキャッシュ。キューカードに **✳ テラコッタ + イタリック行**（`tier2_line`・✳=LLM 生成の印の規律）。新ターン開始・生成中の次ターン開始で破棄。**編隊レベル**: キューに影響する遷移（`transition_task_space` / SummaryReady）から **5s デバウンス**（世代カウンタ・最新のみ生成）→ 現況の事実圧縮（`control_summary_facts`・フル transcript は渡さない=3 段圧縮）→ oneshot → 監督バーの ✳ 文（無ければ決定論の事実文のまま）。**要約は状態を上書きしない**（数字とキューは常に事実層）。設定 `tier2_summaries`（既定 on）でオフ可・oneshot 対応外エージェント/失敗は Tier 1 に自然フォールバック（UI が欠けない）。受入実証: offscreen で監督バー + 2 カードに ✳ 行を目視・全 62 test green。実 CLI 生成経路はスレッド自動命名と同一（実機実証済み機構の再利用）
  - [x] **P3 管制タブ（2026-07-23）** — 編隊中央を `FleetCenterView { Control, Graph }` のタブ 2 面に（既定は当面 Graph・`ToggleControl` = パレット「表示: 管制」）。**mock/fleet-dashboard.html 準拠の 1 画面**: ① ヘッダ（repo ⎇ main + N worktrees・目標プレースホルダ・stat チップ ◐要対応=err ボーダー/✓完了未確認/●稼働/⇡統合・Σトークン+メーター）② 監督バー（**集約気分マスコット** `fleet_mood_mascot`＝最悪状態追従・render_mascot をサイズ可変化して再利用。監督は P6 まで未任命表示・総括は Tier2 まで決定論の事実文=✳ 無し・「⏎ 次: <先頭> の<種別>」ボタン）③ **要対応キュー**（336px・Blocked 経過順→Failed→Review→Done 未確認・カード内インライン操作: `respond_permission`（ACP 広告ラベルの許可/常に許可/拒否・`answer_permission` と同一の一本道）/ Radar=`review_task_for_merge` / `integrate_task` / **確認=`mark_done_seen`（Done→Idle の確認済み遷移・herdr の done/idle 区別）** / 破棄=Archived 遷移。先頭に「次 ⏎」バッジ）④ 稼働カード（digest+▰▱+tok+経過・クリック没入=`reveal_agent_in_fleet`）⑤ 統合パイプライン（7 列・changes_requested→REVIEW_READY / integrating→MERGE_READY に畳む）⑥ ニュース常設。**⏎=キュー先頭へ**（keymap context `FleetControl`・`control_focus`）。受入実証: `NECODER_CONTROL_PROBE`（5 擬似 TaskSpace: Working/Blocked/MergeReady/Failed/Planned・Git/DB 無書込）+ offscreen で mock 同等の 1 画面を目視・全 test green（keymap 4 セクション化追従・i18n `control.*` 39 キー ja/en）。**残**: 目標文の ledger 持ち（P6 の監督席と同時）・「推定」バッジ（P7 Herdr）・許可判断のインライン完了は配線済みだが実クリック検証は実機の手番
  - [x] **P1 遷移スナップショット Tier 1（digest・LLM なし・2026-07-23）** — `Thread.digest` を**状態遷移時のみ**更新（Blocked=`PermissionRequest.title` / Done=最終発言の末尾 1〜2 文 `digest_tail`・素材が無いターンは保持 / Failed=エラー文）。Working 中は `live_digest()`（最新ツール + plan 進行中項目）をその場で合成＝生成コストゼロ。`AgentStatus` に digest/plan_done/plan_total/files_touched/turn_elapsed_secs を追加。**herd 行の sub を digest に差し替え**（無ければ従来の「状態 · 時刻」）+ 編隊 Task セルのヘッダ直下に digest 帯（▰▱ plan メーター付き・`plan_meter`）。`PanelEvent::TurnEnded/PermissionWaiting` に digest/title を載せ、`transition_task_space(digest)` が task_events payload へ記録（再起動後も残る）・TurnEnded 時の `result_summary` は digest を確定値に。受入実証: offscreen（`NECODER_HERD`+`NECODER_ACTIVITY_PROBE`）で Working=「Bash cargo test…」/ Blocked=「workspace.rs への書き…」/ Done=「全 21 test green。」が herd 3 行に出るのを目視・`digest_tail` 単体テスト（和文/英文/長文畳み）green
  - [ ] **P1 続き — 「自分が頼んだこと」を digest の隣に出す** — 2026-07-25 着想（MulmoTerminal 分析で唯一の実質ギャップ）。digest は「エージェントが何をしたか」だが、**「自分が何を頼んだか」を原文のまま**並べると、N 体並走で「ズレているやつ」が一目でわかる。`Thread` に `last_prompt`（送信時に更新・起動復元は `turns` から）を持ち、Fleet サイドバー行と管制の稼働カードで **digest の上**に dim の 1 行（「頼んだこと → やったこと」の読み順。長文は**先頭から詰める**＝指示は最初に用件が来る）。**✳ は付けない**（LLM 生成ではなく人間の発話＝規律上 digest と別の層）。受入: 3 体並走で各行に「指示 / 結果」の 2 段が出る
  - [ ] **P9a リモート管制 — ローカル管制サーバ + QR ペアリング（費用ゼロ・ドメイン不要）** — 2026-07-25 設計確定（前提3点と確定判断10点は FLEET-CONTROL-PLAN §2 P9）。`necoder serve --control`（GUI 稼働中でも不在でも動く＝P5 のロック検出→`gui.sock` IPC fallback を再利用）+ 管制タブの写像を返す読み取りビュー（正は `mock/fleet-dashboard.html` のレスポンシブ派生）+ **QR ペアリング**（使い捨てコード TTL 90s → 不透明トークン 256bit → `storage` のデバイス表・設定画面に一覧と取り消し）+ **`control_ipc` に permission の list/respond を追加**（「裁く」半分が IPC に出ていない唯一の穴）。LAN は平の HTTP＝P9c 障害時の保険として残す。受入: 同じ Wi-Fi のスマホで QR を撮ると管制の読み取りビューが出て permission に応答できる
  - [ ] **P9b リモート管制 — 静的 PWA + Web Push（ドメイン代のみ・リレー不要）** — ドメイン1つ + Cloudflare Pages（静的のみ・ユーザーデータ 0）+ PWA（manifest/SW/ホーム画面追加）+ **Web Push 送信を Rust で**（VAPID の ES256 署名 + RFC 8291 aes128gcm → 購読 endpoint へ直接 POST・Apple にも中身は読めない＝telemetry never を維持）。**通知ペイロード（約4KB）に digest を載せる**＝「知る・読む」がリレー無しで完成。WebAuthn（Face ID）を書き込みの第2要素に。検証: デスクトップ Chrome が本物の Web Push を受け取れるので送信経路は実証可能・封の往復/ペアリング/再接続はヘッドレス Chrome（iOS 固有＝ホーム画面追加と Face ID だけ実機）。受入: 外出先で完了/承認待ちが通知で届き、通知だけで何が起きたか読める
  - [ ] **P9c リモート管制 — リレー（Cloudflare Workers + Durable Objects・月$5）** — `relay/`（本体と同じ repo・AGPL）に交換機（room id が一致する接続2本をつないでバイト列を流すだけ・**何も永続化しない**）。**Hibernation API 準拠が必須**（`state.acceptWebSocket()` + `webSocketMessage()`/`webSocketClose()` メソッド。クロージャに状態を持たせると hibernate せず 100 ユーザーで月$5→約$400）。`idFromName(room_id)` で決定的に同じ部屋・**QR はペアリング1回きり**（room id と鍵を Mac の `storage` とスマホの localStorage に保存）・**再接続処理は必須**（Worker デプロイで全 WS が切れる）。初日にレート制限/フレーム上限/room TTL/請求アラート。`relay_url` 差し替えで self-host。受入: モバイル回線から permission に応答でき、Mac 再起動と Worker デプロイを跨いで自動再接続する
- [ ] 受入（総合）: **1プロジェクトで N 体を別々の worktree に並走させ、系譜グラフ（扇形）＋ `activity_dot` の状態一覧だけで「どれが止まってて・どれが終わって・どの系列か」を色相を足さずに捌き、"B の完了を待って merge" を自走させる**。プラグイン市場（herdr の #5）は採らない。**進捗（2026-07-24）: 自走部分は P6 の実運転 e2e で達成**（監督が完了イベントで wake → fleet CLI で review → merge_ready 提案・integrate 人間 gate 遵守）。1 画面で捌く部分は P1-P4 の管制タブで達成（offscreen 実証）。**残 = 実プロジェクトでの日常ドッグフーディング**（チェックはその後）

---

## M15 — Windows 対応（「Mac 以外の人が DL して使える」）

2026-08-22 調査で着手可能と確認し、**同日 W0 で実機実証済み**。**GPUI 側は動く**（固定 rev の `gpui_windows`＝DirectX + DirectWrite が実際にコンパイルできた）。詰まるのは **necoder 側に残った unix 前提**（Unix socket 直使用 2 ファイル・`~/Library/Application Support` ハードコード 9 ファイル・`HOME` 直読み 19 箇所・`sh -c` 10 箇所・mac 専用コマンド 8 種）。

**ただしコンパイルを止めているのは Unix socket の 2 ファイルだけ**で、残り（パス・`sh -c`・mac 専用コマンド）は**ビルドは通るが実行時に壊れる**類。＝「起動までは近い、正しく動かすまでが本番」。

**受入条件・設計決定・作業手順の正は [`WINDOWS-PORT.md`](./WINDOWS-PORT.md)**（W0〜W6）。`/goal-win` がそれを上から消化する。ここには要約だけを置く。

- [x] **W0 現在地の確定** — 2026-08-22 完了。**necoder 側 20 crate 中 19 が Windows で check green**、落ちるのは `workspace/control_ipc.rs:22` の 1 エラーのみ（`necoder` bin はその下流で未測定）。**依存は全部通った**（`gpui_windows` / `turso` ＝rusqlite 退避不要 / `alacritty_terminal` / tree-sitter 17 本）
- [x] **W1 `paths` crate** — 2026-08-22 完了。パス解決を依存ゼロの foundation crate に集約。**mac の戻り値は unit test で固定**（`Platform` を引数で受ける設計＝**Windows/Linux の CI でも mac の期待値を検証できる**）。**Linux で `~/Library/Application Support/` を作る現状バグも解消**。追加で `paths::canonicalize`（Windows の `\\?\` verbatim を剥がす＝git 出力とパスが一致するようになった）
- [x] **W2 コンパイル通し** — 2026-08-22 完了。**Windows で `cargo check --workspace --all-targets` がエラー 0・警告 0**、**`cargo test --workspace` が 235 テスト green**。IPC は名前付きパイプ（Win32 直叩き・往復テスト green）・外部コマンドは `Host::shell_script()` で**実行先 host が分岐を持つ**形に・Remote SSH は既存 cfg で除外済み
- [ ] **W3 起動して編集・保存** — DirectWrite で文字が出る / Windows 既定 keymap（VSCode 準拠）/ CRLF round-trip / IME
- [ ] **W4 ターミナル・Git・ACP** — ConPTY + PowerShell / `git.exe` / `claude.cmd` 起動
- [ ] **W5 検証ループと CI 常設** — **`ci.yml` の `check-windows` は 2026-08-22 に新設済み**（`-D warnings` + `cargo test --workspace`・PR で走る必須ジョブ）。残りは PowerShell 版の検証スクリプト（screenshot / startup-time / memory-usage）
- [ ] **W6 配布** — 署名なし zip → Authenticode。MSIX/Store は当面やらない

---

以降（later）: FEATURES.md のタグに従う（拡張モデル ADR・WASM ホスト・minimap・multibuffer 本体・vim・テーマ/言語パック配布 等）。
