# ROADMAP — 受入条件つきマイルストーン

運用: `/goal` はこのファイルの**未チェックの受入条件**を上から拾って実装する。
チェックは「実際に満たすことを検証してから」入れる（CLAUDE.md の検証ループ）。順序の正はこのファイル、機能の全量は FEATURES.md。
人の対話検証が必須の項目（M1 の窓確認・M2 の IME 対話など「人の手番」）は /goal ではスキップし、次の実装可能な項目を拾う。

## M0 — 週末テスト（ユーザー自身の関門）

- [x] 環境: Metal Toolchain 導入（2026-07-11、`xcodebuild -downloadComponent MetalToolchain`）
- [x] `cd zed && cargo run -p gpui --example hello_world` で窓が出る — **2026-07-11 通過**（3m28s ビルド後に hello_world 表示。daichi さん自身で確認）

## M1 — 骨組み ✓（2026-07-11）

- [x] workspace スキャフォールド（crates/shirushi・rust-toolchain 1.95・AGPL-3.0 LICENSE・CLAUDE.md）
- [x] `cargo check -p shirushi` 通過
- [ ] `cargo run -p shirushi` で骨組み窓が出ることをユーザーが確認

## M2 — エディタコア（「テキストが編集できる」）

- [x] `theme_core`: UI-SPEC §1 のトークンを型と dark テーマ値で実装（light は値だけ用意）— 2026-07-11、`crates/theme_core`、unit test 9 本 green
- [x] `i18n`: `t!` 規律開始・`locales/ja.yml`+`en.yml` — 2026-07-11、`crates/i18n`（rust-i18n はマクロが crate 局所で多 crate に不適 → 薄い自作に。ARCHITECTURE §6 更新済み）、ja/en parity テスト付き
- [x] `editor_core`: Buffer(ropey 2.0・byte 索引)・Selection・edit/undo/redo（Transaction 単位）・snapshot — 2026-07-11、`crates/editor_core`、GPUI 非依存・unit test 18 本 green
- [x] `editor_view`: 行仮想化描画・スクロール・行番号ガター・キャレット描画 — 2026-07-11、`crates/editor_view`（custom Element・`shape_line`・`uniform_list` 相当の仮想化）。1.6MB 開いても idle CPU 0%（＝可視行のみ shape）。目視: font-kit 有効化後は offscreen（`render_to_image`）でもグリフが写り自己検証可能に。ユーザーが実機で描画確認済み（2026-07-12）
- [x] キー入力で挿入/削除/改行、⌘Z/⌘⇧Z、⌘S 保存、ファイルは起動引数で開く — 2026-07-11、`crates/shirushi` に配線（`EntityInputHandler` 経由の入力・アクション/キーマップ）。smoke（多言語+絵文字+1.6MB）で無クラッシュ
- [ ] **IME**: 日本語入力（変換中表示・確定）が正しく動く — 受入: この README を Shirushi で開いて日本語の1段落を書ける ← **コードは実装済み**（`replace_and_mark_text_in_range` 等・UTF-16↔byte）。**IME 対話検証が未**（人が変換入力する必要・headless 不可）
- [ ] 受入（総合）: `shirushi <file>` で開く→編集→undo→保存 が一往復できる。1MB のファイルでスクロールがもたつかない ← ロジックは全て test 済・1.6MB は idle 0% だが、**対話/目視の一往復確認が未**
- [x] 性能ベンチ導入（一部）: 起動時間計測 `scripts/startup-time.sh`（debug 実測 ~100ms cold→first render）+ editor_core スループット bench（insert 1.5M/s・1MB上 edit 40万cycle/s）。**`input_latency_ui` の key→frame ヒストグラム移植は後回し**（要 UI・時期尚早／JOURNAL 参照）

## M3 — ワークスペースと方向感覚（「色で迷わなくなる」）

- [x] `settings_core`: 3層マージ（default→user→`.shirushi/`）— 2026-07-12、`crates/settings_core`、深い JSON マージ・型付き Settings、6 test。保存即反映（監視）は後続
- [x] テーマきせかえ: light 値の実装 + テーマセレクタ（Picker 上・ライブプレビュー）+ ユーザーテーマ JSON 読み込み — 2026-07-13。`theme_core` に**ユーザーテーマ JSON（トークン上書き・欠けは appearance の組み込みへフォールバック）**・`available_themes`/`resolve` を実装（unit test）。`⌘⇧T` でテーマセレクタ（Picker）＝**ハイライト移動で即ライブプレビュー**（`PickerEvent::Highlighted`）・確定で settings.json へ theme 名保存（再起動でも効く）・中止でプレビューを戻す。`apply_theme` がクローム/エディタ/Agent パネル/Picker へ波及。light 全体反映を offscreen で目視確認
- [x] `keymap_core`: JSON keymap + コンテキスト述語 — 2026-07-12、`crates/keymap_core`、`build_action` で名前解決・`KeyBinding::load`、3 test。既定 keymap を bin で読込（全アクション解決確認）
- [x] `ui`: Picker 基盤（1個のファジーリストを全モーダルで使い回す）— 2026-07-12、`crates/ui`、fuzzy・キー操作・イベント通知、2 test。**CommandRegistry/StatusItemRegistry は未**
- [x] `workspace`: タブ・単一ペイン→分割・左右下ドック・statusbar — titlebar ✓・タブ列 ✓・パンくず ✓・左ドック(explorer) ✓・右ドック(Agent) ✓・statusbar ✓（2026-07-12）。**分割ペイン**（`⌘\` で右分割・独立エディタの比較ビュー・各ペインにタブ+×）✓・**下ドック**（統合ターミナル・`⌘J`）✓（2026-07-13）
- [x] `project`: worktree 走査・gitignore — 2026-07-12、`crates/project`、`ignore` crate で gitignore 準拠の遅延 read_dir + all_files、4 test。**ファイル監視は未**
- [x] エクスプローラ（ツリーのみ先行）+ ファイル開く — 2026-07-12、workspace の左ドック。クリックで展開/ファイル open
- [x] **レール**: プロジェクト登録・切替（窓内）・プロジェクト色（自動巡回）— 2026-07-12。**カスタムアイコン / `.shirushi` 色指定は未**（頭文字 + 巡回色）
- [x] **⌘O スイッチャー** + ⌘1..9 + **新窓** — 2026-07-13。⌘O プロジェクトスイッチャー ✓（Picker）。⌘1..9 でレールのプロジェクト N 番へ切替（`ActivateProject1..9` アクション）。**新窓は ⌘⇧N**（当初案 ⌘⏎ は composer の `agent::SubmitPrompt` と衝突するため慣例的な ⌘⇧N に変更。Editor コンテキストの ⌘⏎ 送信は温存）＝アクティブプロジェクトを新ウィンドウで開く（右クリック「新規ウィンドウで開く」と同じ経路）
- [x] 状態永続化 — 2026-07-12、`state.json` に (プロジェクト群・アクティブ・開ファイル) を保存、引数無し起動で復元
- [x] ファイルファインダ ⌘P — 2026-07-12（Picker + `all_files`）。**コマンドパレット ⌘⇧P は未**（要 CommandRegistry）
- [x] 受入: **2プロジェクトをレールで色区別 + 再起動で状態復元** — 2026-07-12 達成（offscreen で 2 色レール目視・state.json 往復確認）

## M4 — ACP（「Claude Code が中で動く」）

- [x] `acp_client` 接続・initialize・prompt・**ストリーミング** — 2026-07-12、`crates/acp_client`。claude-agent-acp を子プロセス起動 + `blocking::Unblock` パイプ + `ByteStreams` + `Client.builder().connect_with`。バイナリ探索を **PATH → Zed の npx キャッシュ(.bin) → `npx @agentclientprotocol/claude-agent-acp@0.58.1`** に強化（単体は PATH に無いのが普通・これが `live_prompt` の当初失敗要因だった）。`prompt_once`（一括）に加え **`run_session`（永続セッション + `read_update` ループで `AgentMessageChunk`/`AgentThoughtChunk` を [`AgentEvent`] に簡約し逐次流す）** を実装。`ToolCall` も [`AgentEvent::ToolStarted`] にマップ。**エージェント自身が実機検証済み**: `live_prompt`（「1+1」→「2」5.7s）・`live_stream`（「3の倍数5個」→`3, 6, 9, 12, 15` を逐次）・ツール誘発プローブ（「ls して」→ ⏺ Terminal ステップ + 応答）。**権限リクエスト・`UsageUpdate` でトークン実値は達成・markdown 描画も完了（2026-07-19: pulldown-cmark 借用 + 自作レンダラ・Thinking 折り畳み。JOURNAL 参照）**
- [x] `agent_panel`: スレッド色タブ・メタ行（モデル/トークン常時メーター）・transcript（⏺/⎿・✳ Thinking 常時展開）・composer（宛先チップ・⌘Enter 送信・IME 安全）— 2026-07-12、`crates/agent_panel`（右ドックまるごと）。composer は editor_view 平坦モード再利用で IME/undo 共通。⌘Enter→`agent::SubmitPrompt`→**スレッド毎の常駐セッション**（`run_session` をバックグラウンド + `cx.spawn` フォアグラウンドで `AgentEvent` を逐次 transcript 反映）。**実機フルパス検証済み**: 起動プローブ（`SHIRUSHI_ACP_PROBE`）で composer→claude-agent-acp→ストリーミング→描画を offscreen スクショで確認（「1+1は？」→「2」がスレッド色付きで表示）。transcript の初期内容は mock 会話例（デモ・実送信で本物が積まれる）
- [x] 権限リクエスト UI（許可/拒否）・エージェント編集の diff レビュー（accept/reject）— 2026-07-12、`crates/acp_client`+`agent_panel`。`session/request_permission` を read ループの **`.if_request`** で受け、ハンドラ内 await でユーザー決定待ち（ターンブロックが正）。`ToolCallContent::Diff` を `compact_line_diff` で緑（追加=`theme.ok`）/赤（削除=`theme.err`）表示、composer 直上に承認カード（許可/常に許可/拒否）。**live: Write プローブで diff カード表示 + `SHIRUSHI_AUTO_ALLOW` で実ファイル生成まで round-trip 確認**
- [x] スレッド色貫通（UI-SPEC §6）+ titlebar beacon + statusbar ドット ← 色貫通 ✓（タブ切替で下線/トークンバー/msg-user 左縁/宛先ドット/composer 枠/送信ボタンが一斉切替・実機スクショで確認）。titlebar beacon ✓（実行中スレッドが窓上部に色ドット+名で常時表示＝BACKGROUND の原点痛点）。**statusbar スレッドドットは未**
- [x] 受入: **Claude Code サブスクで会話→ファイル編集を diff で確認→accept、を Shirushi 内で完結。トークン使用量が常に見えている** — 2026-07-12 達成。会話ストリーミング ✓・トークン実値メーター ✓・**ファイル編集の diff 表示→承認→実行（accept）を live で完結**（round-trip でファイル生成確認）。加えて **model/effort/権限モードの ACP 実反映**（session config options / set_mode）・**Enter 送信の設定化**（IME 誤送信対策）も同日実装

## M5 — エクスプローラ完成（Finder 感）

- [x] カラム / アイコンビュー + 左下ビュー切替 — 2026-07-12、`ExplorerView{Tree,Columns,Icons}` + `render_explorer_footer` の ☰▥▦。拡張子別アイコン（フォルダ横長/ファイル縦長・型色）。カラムは Miller columns（末尾3段・幅可変）
- [x] 上位階層ブレッドクラム — 2026-07-12、`render_explorer_header` のクリック可能パンくず（プロジェクト内 up-nav）。**プロジェクト外へ出るブラウズは未**（root 上へは行かない）
- [x] 右クリックメニュー（新しいウィンドウでプロジェクトとして開く 含む）— 2026-07-12、全3表示のエントリに右クリック。フォルダ=新規ウィンドウで開く/開く/コピー、ファイル=開く/コピー。**ファイル操作 undo は未**
- [x] エクスプローラ幅可変（右縁ドラッグ）— 2026-07-12
- [x] 受入: マウスだけで「上へ辿る→隣のリポジトリを新窓でプロジェクトとして開く」— 2026-07-13。ブレッドクラムの **⤴（上へ）** で current の親へ辿れ、ルート直上へ出ると Finder カラム表示へ自動切替（`project::Worktree::read_any_dir` がルート外を gitignore 無しで列挙）。ルート外では **⌂プロジェクト（戻る）** + 末尾数段のパンくずを出す。隣リポジトリを右クリック→新規ウィンドウで開く（既存）。offscreen で `~/Work/experience` の隣接 repo 一覧を目視確認

## M6 — 検索

- [x] バッファ内検索（regex/literal・大小トグル）— 2026-07-12、`crates/search` の `search_text`、7 test。**置換・インクリメンタル UI は未**
- [x] プロジェクト横断検索 — 2026-07-12、`search_files`（`ignore` の走査結果を渡してインプロセス検索。ripgrep 子プロセスは未採用）
- [x] 受入: `TODO` をプロジェクト横断で列挙→ジャンプ — 2026-07-13。`⌘⇧F` で検索パネル（オーバーレイ）＝クエリ + 大小/正規表現トグル + **ファイル別グルーピング結果**（行番号 + マッチ強調プレビュー）。クリック / Enter で**該当ファイルを開き対象行を中央へジャンプ**（`EditorView::reveal_position` = viewport 未確定でも効く pending_reveal・one-shot で idle 0%）。↑↓ 選択・Esc 閉じる。offscreen で「fn」440 件のファイル別結果を目視確認

## M7 — 言語知能

- [x] tree-sitter ハイライト（Rust）— 2026-07-12、`crates/lang`（tree-sitter 0.25 + tree-sitter-rust）。`HighlightKind`→theme の syn-* に接続、editor_view で行ごとに色 run 生成（編集で再解析・512KB 超はスキップ）。3 test。実 .rs を無クラッシュで開ける。**色の目視は要実機**（offscreen はグリフ非表示）。インクリメンタル解析は後続
- [x] LSP: rust-analyzer（**診断・補完・定義ジャンプ**。hover/format は後続）— 2026-07-13。`lang::lsp` に最小 LSP クライアント（JSON-RPC 封筒は自前・型は手書き・transport は std::process + 読取スレッド + futures channel）。initialize→initialized→didOpen→didChange の lifecycle。①**診断**=publishDiagnostics を gutter 下線（error=赤/warn=琥珀）+ statusbar 件数（✗N ▲N）にライブ反映（live 確認）。②**補完**（Ctrl-Space）=キャレット直下ポップアップ（種別バッジ+detail・上下/Enter・Tab/Esc・textEdit→insertText→label で挿入・識別子プレフィクス置換）。③**定義ジャンプ**（F12）=Location/LocationLink を解析→別ファイルなら開いて中央へ。位置は byte↔UTF-16 変換。`~/.rustup/toolchains/*/bin/rust-analyzer` 実体を起動。parser の unit test + 実 ra の handshake/診断 ignored test。offscreen で診断赤下線・補完ポップアップを目視
- [x] 受入: この repo の Rust を補完と診断つきで書く — 2026-07-13 概ね達成。ハイライト ✓・診断 ✓（live）・補完 ✓（ポップアップ+挿入）・定義ジャンプ ✓。**補完/定義の live round-trip 体感は人の手番**（transport・parser・UI は各々検証済み）

## M8 — Git とターミナル、ブランチ横断の完成

- [x] git status（ツリー/タブ色）+ gutter diff — 2026-07-13。`project` に **git CLI 直叩き**の `git_status`（porcelain→5分類）+ `buffer_diff`（`git show HEAD:./name` + **imara-diff** Histogram・純Rust）。ツリー行/タブ名を状態色（追加=緑/変更=琥珀/削除=赤）+ 末尾バッジ（M/A/U/D/!）、フォルダは配下変更で ●。gutter は追加/変更/削除の左端バー（editor_view・**バッファ version 変化を prepaint で検知→250msデバウンス→背景スレッドで git**＝idle 0%）。offscreen で目視（Cargo.toml=M・gutter 緑バー）
- [x] branch/worktree メニュー（切替・worktree 作成）→ スレッドの (project, branch) 帰属 — 2026-07-13。titlebar の ⎇ クリックで開く（`project::git_branches`/`git_worktrees`/`switch_branch`/`add_worktree`）。ブランチ行クリック=in-place 切替（git switch→再読込）、**⧉=worktree として新しい窓で開く**（既存 worktree 優先・無ければ `<repo親>/<repo名>-<branch>` に作成）＝**並行ブランチ×別窓×スレッド色**の入口。worktree 一覧も別窓で開ける。宛先チップの (project, branch) は切替で追従。**⌘O 2階層化は未**（⎇ 直開きで代替）
- [x] 統合ターミナル（alacritty_terminal）— 2026-07-13。新 crate `terminal_view`（crates.io `alacritty_terminal` 0.26）。**EventLoop が読取+vte parser**、idle 0% は出力時のみ Wakeup→pump(`cx.spawn`)→sync→notify（**タイマー無し**）。下ドックに配置（`⌘J` / 下ドックボタン開閉・cwd=プロジェクトルート）。custom Element でグリッド描画（16/256色 ANSI + truecolor・INVERSE/BOLD/ITALIC・ブロックカーソル）。入力は `on_key_down` 一本化（印字 + 特殊キー→エスケープ・矢印は APP_CURSOR 対応）。offscreen で実シェル（`Last login… / プロンプト / カーソル`）を目視。**パスリンクは未**（後続）
- [x] 受入: **worktree で並行ブランチを開き、それぞれ別スレッドのエージェントを走らせ、色で追える**（＝当初ビジョンの完成形）— 2026-07-13。機構が揃った: ⎇メニューの ⧉ で **ブランチ→worktree→新ウィンドウ**、各窓は自分の (project, branch) の色付きスレッド + git 色 + プロジェクト色で方向づけ、下ドックに cwd 一致のターミナル。**live のマルチウィンドウ並行運用の体感確認は人の手番**（機構は完成）

---

## M9 — Remote SSH（「場所が変わっても同じ Shirushi」）

- [x] local の FS/process/Git/search/save を `Host` 境界へ移し、既存 unit test を維持する
- [x] versioned length-frame RPC + raw body + frame 上限 + multiplex worker + protocol test
- [x] `shirushi-remote-server` + system OpenSSH + project root scope + conflict-safe atomic save
- [x] `ssh://[user@]host[:port]/path` で tree/open/edit/search/Git を remote Host へ接続する
- [x] LSP・PTY・ACP を remote process/ControlMaster session へ移す（task 機能自体は未実装）
- [x] ControlMaster・server version/OS/arch 検出・同一 target の versioned binary 自動配備
- [x] daemon/proxy 再接続・5秒 heartbeat・master 再生成・非冪等 request の無条件再送禁止
- [x] protocol/fs/process concurrency・proxy 再接続・外部編集競合の統合 test
- [x] SSH URI の状態復元と status bar の接続先表示
- → **未チェック残件（ベンチ・musl 配布・dirty backup・障害注入 test・Remote Projects UI・受入）は M13 へ移動**（2026-07-15 判断: 日常機能 M10〜M12 を先に消化する）

設計と根拠: [`research/remote-ssh-2026.md`](./research/remote-ssh-2026.md)。

## M10 — 毎日使える（「Shirushi で Shirushi を開発する」）

2026-07-15 のギャップ分析（全 crate 棚卸し × `research/feature-matrix.md` 突合）で M10〜M13 を策定。
レイヤ自体は 13/13 に点が打ってあるが「所作」の層が薄い — ここを埋めるとドッグフーディングが始まる。

- [x] **複数タブ** — 2026-07-16。`workspace.rs` の `editor: Option<Entity>` を撤廃し、`tabs: Vec<EditorTab{path,editor,_observation}>` + `active_tab` に。**タブクリック**（`select_tab`）/ **⌘{ ⌘}**（=⌘⇧[ ⌘⇧]・`SelectPrevTab/SelectNextTab`。Zed の `pane::Activate*Item` と同じ字面）切替・**⌘W で閉じて隣へ**（`close_tab_at`＝active 追従は agent スレッドタブと同ロジック）・**ドラッグ並べ替え**（`DraggedEditorTab`+`move_tab`）・**dirty ドット個別**・**同一ファイルは重複タブを作らず既存へ切替**・**LSP didOpen/didClose 追従**（+ `lsp_sent_versions` を path 別 map 化＝複数ファイルで version 番号が衝突しても誤スキップしない）。永続化は `ProjectSlot.open_files: Vec` + `active_file`（プロジェクト単位でタブ列を保存・非アクティブは切替時に遅延復元）。ARCHITECTURE §3 に Pane/Item 初版の型契約を追記。**検証**: cargo test 全 green・offscreen で 5 タブ描画（active=lsp.rs 下線）・state.json 往復（open_files 5件+active_file:4）・無引数再起動で 5 タブ復元を目視。**編集→保存→× の対話ループ体感は人の手番**（状態分離はタブ毎の独立 Entity で構造的に保証）
- [x] **⌘F バッファ内検索/置換** — 2026-07-16。エディタ右上のフローティング検索バー（`render_buffer_search_bar`）。**インクリメンタル**（タイプ毎に `search::SearchQuery::find_in`〔新設・byte レンジのみ・行分割なし＝改行跨ぎ literal も可・20k 上限〕→ 全マッチ = warn 16% 面・現在マッチ = 選択して中央へ〔可視域内なら**スクロールしない** = pending_reveal を賢く〕）+ **n/m カウンタ**（上限超過は「+」・正規表現エラーもここに赤で）。Enter/⇧Enter = 次/前（端で回る）・Aa/`.*` トグル・⌥⌘F/▸ = 置換行（Tab で欄切替・置換欄 Enter = **1件置換して次へ**〔置換文字列が再マッチしても足踏みしない anchor 制御〕・⌘Enter/全置換 = **`Buffer::edit` 複数レンジ 1 Transaction ＝ undo 一発**・表示上限超でも全件対象）・**Esc = 開いた位置へ復帰**（`PositionSnapshot`）・× = 現在位置のまま閉じ。単一行選択があれば ⌘F でクエリに種込み。編集追従は既存 observe + (version,query,トグル) ガード（blink 再入も停止）。タブ/プロジェクト切替では畳む。t! 13 キー（ja/en）。**検証**: workspace 全 test green・offscreen で「render_」1/54（置換行+Replace ボタン）と「resizing」1/12（他マッチ琥珀・現在マッチ選択・statusbar 545:13 追従）を目視。**置換→undo の対話一往復は人の手番**（Buffer::edit 複数レンジ+undo は editor_core test 済）
- [x] **補完の自動トリガ** — 2026-07-16。①`editor_view` に **`EditorInputEvent::Typed`**（入力ハンドラ経由の確定入力のみ emit。IME 変換中・paste・undo・補完適用では出さない）→ workspace がタブ毎に subscribe ②`classify_completion_trigger`（識別子= Identifier / `.`・`::` 2個目= Fresh / 他= None・unit test 9 ケース）③ポップアップを **type-through 化**（印字キーは `insert_text` でエディタへ挿入 → Typed 経由で**クライアント側絞り込み**〔`filter_completion_indices` = 大小無視前方一致・LSP 再要求なし〕・backspace で prefix 縮小・0 件で閉じ）④**Esc 抑止** = 閉じた語の語頭 offset を記憶し同じ語では自動再表示しない（`.`/`::`/手動 Ctrl-Space で解除）⑤世代番号で連打時の古い応答を破棄 ⑥prefix と表示位置は**応答時点**のキャレットで確定（要求中に打った分も反映）。**検証**: 全 test green・**実 rust-analyzer で `t.the` タイプ→ theme/thermal のみの自動ポップアップ（other が prefix 除外）がキャレット直下に出るのを offscreen 目視**（`SHIRUSHI_TYPE_PROBE` フック新設）。Enter 挿入は既存 M7 経路（filtered 対応済み）。**タイプ→絞り込み→Enter の対話体感は人の手番**
- [x] **hover の配線** — 2026-07-16。`editor_view` に **dwell 検知**（mouse_move で 500ms タイマー・世代番号で無効化・`EditorHoverEvent::Dwell{line,character,anchor}`）と **Cancel**（クリック/スクロール/dwell 位置から 30px 超の離脱）。workspace がタブ毎 subscribe → `lang::lsp::hover()` を要求（世代番号で古い応答破棄・補完ポップアップ表示中は出さない）→ `parse_hover_lines`（MarkupContent/MarkedString/配列 → コードフェンス行だけ落とすプレーン表示・24 行で truncate）→ アンカー**直上**（入らなければ下）にコード字カード（occlude = ポップアップ上にマウスがある間は消えない）。**⌘K ⌘I**（VSCode 互換）でキャレット位置にも出せる（`workspace::ShowHover`）。タイプ・タブ切替で消える。**検証**: 全 test green・**実 ra で `Thing` 上に `struct Thing { theme: u32, … }` シグネチャのポップアップを offscreen 目視**（`SHIRUSHI_HOVER_PROBE` フック）。**マウス dwell の対話体感は人の手番**（キー経路と同一実装）
- [x] **ファイル監視（watch 基盤）** — 2026-07-16。`project::watch_root`（**notify 6.1**/FSEvents・型は外に漏らさない opaque `Watch` = M13 の remote watch への差し替え面）→ workspace の pump（futures channel + **200ms 合流**で cargo build 等の連続イベントを 1 回に畳む・`SHIRUSHI_WATCH_DEBUG=1` でトレース）→ `handle_watch_events`: ①**開バッファ**: `Buffer::disk_probably_unchanged`（len+mtime の安価比較 = **自分の保存イベントは無視**）→ 無編集なら `Buffer::reload`（選択クランプ・履歴リセット・LSP didChange は observe 経由で自動）/ **dirty なら警告バー**（⚠+再読込/このまま・上書きは絶対しない・保存時の競合検知が最後の砦）②ツリー = slot 再構築 ③git 色 + gutter diff（`.git/` は index/HEAD/refs のみ合図・objects の湧きは無視。gitignore 対象はノイズ除去 `Worktree::is_ignored`）。プロジェクト切替で張り替え・remote は張らない（M13）。**検証**: 全 test green（reload/クランプ/履歴リセット/None 系 unit test）+ **live: `echo >>` でバッファ 4 行自動リロード・新規ファイルがツリーに U 出現・gutter 緑バー**、**dirty 時は警告バーが出て外部行が混入しない**のを offscreen 目視。受入達成
- [x] **UI スレッド非ブロッキング化（監査 S1+S2）** — 2026-07-16。①アイコン/カラム表示の render 内 `read_any_dir` → **`ProjectSlot::listed_dir`（RefCell キャッシュ・初回のみ読み・watch の refresh で無効化）** ②⌘S 保存 → **`Buffer::prepare_save`/`PendingSave::write`(背景)/`complete_save`**（保存中の編集は version 比較で dirty 温存・競合は従来どおり拒否）③`open_file` → 背景 read + `Buffer::from_content`（**起動復元だけは同期 `open_file_sync`** = 完了順でタブ順が崩れるため）④`refresh_git_status` → **背景 + 世代番号**（status/branch/panel スナップショットを一括収集・古い結果は破棄。呼び出し 15 箇所へ cx 付与）⑤⌘P → `all_files_on`（新設・Rc<Worktree> は Send 不可のため host 直呼び版）を背景 ⑥ブランチメニュー列挙・switch_branch・add_worktree・commit・stage/unstage/削除/作成 → 背景（checkout/commit 系は git_busy 表示・stage 系は `run_git_index_op` 共通ヘルパ）。**検証**: 全 test green・警告 0・offscreen で複数タブ + アイコンビュー + git 色/ブランチ表示が非同期化後も揃うのを確認。**remote 切断での無フリーズ実測は M13 の障害注入 test で**（構造上は全 Host 呼び出しが背景に移動済み。同期 Host 残り = 起動復元・explorer ツリー展開クリック・watch の is_synced 判定〔local メタデータのみ〕）
- [x] **hot exit（クラッシュ耐性）** — 2026-07-16。**`crates/storage` 新設 = Turso 0.7 初導入**（pure-Rust SQLite・専用ワーカースレッド 1 本 + チャネルで直列化し外へはブロッキング API・Turso 型は crate 外へ漏らさない = rusqlite 退避面。`~/Library/Application Support/Shirushi/shirushi.db`・`SHIRUSHI_DB` で上書き可）。**書き込みファイルは sqlite3 CLI でもそのまま読める**（SQLite 互換フォーマット実証）。workspace: 編集で **2s デバウンス**の背景スナップショット（dirty=upsert / clean=行削除。**バッファ version ガード**＝blink の notify では再予約しない）・タブ閉じ/保存で破棄・**⌘Q（正常終了）で全クリア**・起動時に候補があれば**復元/破棄バー**（復元 = `replace_all_text` 1 Transaction ＝ undo で戻れる・未オープンは開いてから適用・**未決の間は tick が書きも消しもしない**）。**検証**: storage unit test 2（round trip・再オープン=クラッシュ相当）+ **live: タイプ→ tick → `kill -9` → DB に全文残存（dump で確認）→ 再起動で「復元候補 1 件」検出 → 自動復元 → 復元後に再スナップショット**の一部始終をログで確認。受入達成（バー/復元後の PNG も撮影済・目視は Read 復旧後）
- [x] **ツリーのファイル操作** — 2026-07-16。右クリックメニューに**新規ファイル / 新規フォルダ / 名前を変更… / 複製 / ゴミ箱に入れる**（local のみ表示。remote 版は M13 の Host 拡張と一緒）。命名は**ツリー行のインライン入力**（rename=対象行を入力行に置換・New*=親フォルダ行の直後に挿入〔ルートなら先頭〕・Enter 確定/Esc 中止・アクセント枠+キャレットバー）。実装: `project` に `create_file_local`/`create_dir_local`/`rename_local`（上書き拒否）/`duplicate_local`（`name copy.ext`・衝突は `copy 2`…・dir 再帰）/`trash_local`（**/usr/bin/trash → Finder AppleScript fallback = 完全削除しない**）+ unit test。開いているタブの rename/trash は**先にタブを閉じる**（旧パスへの保存でファイルが復活する事故防止・v1）。新規ファイルは作成後すぐ開く。ツリー再構築 + git 色更新 + watch でも追従。**検証**: project unit test（create/rename/duplicate/衝突拒否）green + **live: 命名入力→ Enter 確定→実ファイル生成をエンドツーエンド確認**（`SHIRUSHI_NAMING`/`SHIRUSHI_NAMING_CONFIRM` フック）。メニュー/入力行の PNG 撮影済（目視は Read 復旧後）。**マウスだけの一巡は人の手番**
- [x] **編集の所作一式** — 2026-07-16。**ロジックは全部 editor_core**（GPUI 非依存・unit test 8 群）: ①単語境界 `prev/next_word_boundary`（識別子/記号の 2 クラス・空白スキップ・256B 窓・日本語も 1 語）→ ⌥←→/⇧⌥←→/⌥⌫ ②行操作 `move_lines`（隣と入れ替え・最終行の改行なしも正しく・端は no-op）/`duplicate_lines`（複製方向でキャレット位置を使い分け）/`delete_lines` ③`toggle_comment`（全非空行コメント済みなら外す・零幅レンジ+同一テキストで **1 Transaction=undo 一発**・prefix は `lang::comment_prefix` 表 = //・#・-- の 3 系）④`insert_newline_indented`（前行インデント継承 + `{([:` 直後は 1 段深く）⑤`indent_lines`/`outdent_lines`（⌘] ⌘[・Tab は選択があれば行インデント/なければ空白挿入）⑥**括弧/クォート自動ペア** `classify_pair_input`（Pair/Wrap（選択を囲んで内側を再選択）/SkipOver（打ち抜け）/Insert。クォートは単語隣接でペアにしない=lifetime・アポストロフィ対策・開き括弧は直後が識別子なら素通し）— editor_view の入力ハンドラに介入（IME 変換中・明示レンジ・composer は素通し）。キーは既定 keymap に 17 本追加（⌥←→・⌥⌫・⌘↑↓・⌥↑↓・⇧⌥↑↓・⌘⇧K・⌘/・Tab/⇧Tab・⌘[ ⌘]）。tab_size は暫定 4（M10-13 で設定配線）。**検証**: editor_core 32 test 全 green（新規 8 群: 境界/単語削除/行移動/複製削除/コメント/インデント改行/インデント/ペア分類）・全 suite green・警告 0。**キー操作の実機体感は人の手番**
- [x] **multi-cursor の UI 配線** — 2026-07-16。editor_core に ①`select_next_occurrence`（⌘D: 空選択→`word_range_at` で単語選択・以降は次の一致を追加選択〔wrap・選択済みスキップ・全選択済みで false〕）②`add_cursor_vertically`（⌥⌘↑↓: 端の選択と同列で上下に追加・行末クリップ・端で停止）③`add_cursor_at`（⌥クリック・重複追加なし）④`collapse_to_primary`（Esc: 複数→先頭 1 個。単一なら no-op = 他オーバーレイの Esc を邪魔しない）。view はアクション 4 本 + ⌥クリック（ドラッグ選択を開始しない）+ ⌘D 後は新しい選択へ reveal。**同時入力/削除/ペースト/キャレット複数描画は既存**（buffer.insert が全選択に効く・prepaint は全選択を描く）。keymap: ⌘D・⌥⌘↑↓・Esc。**検証**: unit test 3 群（**受入そのもの =「⌘D×3 → 3 箇所同時書き換え → undo 一発」をテスト化**・縦追加のクリップ/端停止・collapse）全 green。**実機のキー体感は人の手番**
- [x] **ナビゲーション履歴** — 2026-07-16。workspace に back/forward スタック（(path, byte offset)・上限 100・連続同位置は積まない）。**記録ポイント**: F12（定義応答の着地直前）・⌘P 確定・⌘⇧F 検索ジャンプ・**大距離クリック**（editor が 50 行以上の移動で `EditorInputEvent::CaretJumped{from}` を emit → 移動前位置を記録）。**⌃- 戻る / ⌃⇧- 進む** = 現在位置を反対スタックへ → 対象へ（同ファイル=即 reveal・開タブ=切替+reveal・**閉じたファイルは背景読みで開き直してから着地**）。keymap `ctrl--`/`ctrl-shift--` はパース検証済み（起動時の全アクション解決に警告なし）。**検証**: 全 test green・警告 0。**F12→⌃- の実機一往復は人の手番**（両経路とも既検証の select_byte_range/open 経路の合成）
- [x] **soft wrap + 行ジャンプ** — 2026-07-16（設計は JOURNAL に先行記載）。**WrapMap**（論理行→表示行。`segments`=行ごとのセグメント開始 byte 列 + `prefix_rows`=累積で両方向 O(log n)・(version,列数,on/off) 変化時のみ再構築・**off は恒等マップ=挙動不変を構造で保証**）。折返しは**等幅セル数**（ASCII=1・東アジア全角=2 = unicode-width・単語境界優先/長語は文字切り。バンドル等幅フォント前提でピクセル shaping 不要）。**描画の仮想化を表示行に変更**: 可視表示行→セグメント部分文字列だけ shape・行番号/diff/診断は先頭セグメントのみ・選択/検索/キャレット/IME は絶対 byte 範囲がそのまま機能・キャレットのセグメント帰属は「境界は次セグメント・論理行末は最終セグメント」。**↑↓/⇧↑↓ は表示行移動**（セグメント内相対 byte 維持・マップが古い 1 フレームは論理行フォールバック）。クリック位置・スクロール上限・reveal も表示行系。設定 `soft_wrap`（settings_core 追加・既定 off）+ **⌥Z** トグル + `SHIRUSHI_SOFT_WRAP=1`。**⌃G 行ジャンプ** = ミニオーバーレイ（数字のみ・Enter で `reveal_position(n-1,0)`・ナビ履歴にも積む）。**検証**: WrapMap unit test 2 群（語境界/CJK セル/長語/round trip/恒等）+ 全 suite green・警告 0・長行 Markdown（英文+全角）のスクショ撮影済（目視は Read 復旧後）
- [x] **設定の実効化** — 2026-07-16。①**font_size/tab_size を editor_view のフィールド化**（`set_typography`・行高は 23/13 比で追従・shape/仮想化/ヒットテスト/ガター全て可変化・clamp 8〜32/1〜16）②エディタ生成時に適用 + **observe_global で live 配布**（`apply_editor_settings` = 全タブ + 分割へ。soft_wrap も同経路）③**ユーザー keymap.json**（settings と同じディレクトリ）を既定の**後**に bind = 同キーはユーザー勝ち・壊れた項目は skip ④keymap.json を**ファイル監視で live reload**（200ms 合流・再 bind = gpui は後勝ち）。**検証**: 全 test green・**live: 起動中に `shirushi config set font_size 19` → watcher 経由で反映**（スクショ撮影済・元値へ復元済）・**ユーザー keymap が「1 束」適用されるログを実機確認**（テスト用ファイルは後始末済み）。受入達成
- [ ] 受入（総合・ドッグフーディング開始）: **Shirushi で Shirushi を丸 1 日開発できる**（この日から自分の変更を自分で浴びる）

## M11 — 言語知能と Git の parity（「他人のリポジトリを違和感なく直せる」）

- [x] **フォーマット** — 2026-07-17。基盤 = **`Buffer::edit_batch`**（レンジ毎に異なるテキストの一括置換・1 Transaction=undo 一発・重なりは先勝ち防御・unit test）。`lang::lsp::formatting`（tabSize は設定値）→ `parse_text_edits` → `EditorView::apply_lsp_edits`（UTF-16→byte 変換・**キャレット (行,列) 復元・スクロール維持**）。**⌥⇧F**（workspace::Format）+ **⌘S を workspace::SaveActive 化** = `format_on_save`（settings_core 追加・既定 off）が有効なら整形→保存の連鎖・LSP 不在/対応外は素の保存へフォールバック。応答時に別ファイルへ移っていたら適用しない誤爆ガード。**検証**: 全 test green + **live: 崩れた .rs → プローブで整形→保存 → ディスク上で rustfmt 品質の整形を確認**（`SHIRUSHI_FORMAT_PROBE=1`）。受入達成
- [x] **rename（F2）** — 2026-07-17。F2 = キャレット下の単語を初期値にミニ入力（LSP 対応ファイルのみ）→ `textDocument/rename` → **`apply_workspace_edit` 共通経路**（code actions と共用）: `parse_workspace_edit`（changes / documentChanges 両対応・ファイル操作系は v1 スキップ）→ **開いているタブはバッファへ**（`apply_lsp_edits`・dirty のまま・undo 可）/ **未オープンはディスク直書き**（`apply_text_edits_to_string` = UTF-16→byte を行索引で・revision 条件付き書き込み = 外部変更と衝突しない）。適用数を報告（バッファ n・ディスク n・失敗 n）。unit test（UTF-16/行跨ぎ/順不同適用・changes/documentChanges パース）。**検証: 実 ra で `Thing`→`Renamed` — 開バッファ 1 + 未オープン other.rs のディスク直書き 1・失敗 0 を live 確認**。受入達成
- [x] **code actions（⌘.）** — 2026-07-17。`publishDiagnostics` の**生 JSON も保持**し、キャレット行の Diagnostic[] を context に渡して `textDocument/codeAction` → タイトル一覧ポップアップ（補完と同型・上下/Enter/Esc/クリック）→ **edit 即適用 or `codeAction/resolve` で遅延解決**して `apply_workspace_edit`（rename と共通基盤）。**罠 = initialize capabilities**: `codeActionLiteralSupport` + `resolveSupport(edit)` + `dataSupport` が無いと **ra はアクションを一切返さない**（無応答で 1 回踏んだ）。`workspace.workspaceEdit.documentChanges` も併せて宣言。**検証: 実 ra で「Remove all unused imports」→ resolve → 適用 → 保存 → ディスクから import 行が消える**のを live 確認。受入達成
- [x] **参照検索（⇧F12）** — 2026-07-17。`textDocument/references`（includeDeclaration）→ `locations_to_file_matches`（Location[] をファイル別に集約・**背景でファイルを読んでプレビュー行を生成**・UTF-16→byte 変換・unit test）→ **⌘⇧F の検索パネル UI をそのまま再利用**（クエリ表示 =「参照: <単語>」・クリック/Enter でジャンプは既存経路）。スクショ撮影済（目視は Read 復旧後）
- [x] **シンボル** — 2026-07-17。①**⌘⇧O アウトライン** = `lang::outline`（tree-sitter クエリ駆動・fn/struct/enum/trait/impl/mod/const・定義順・unit test。LSP 不要）→ Picker（fuzzy 絞り込み・確定で行ジャンプ + ナビ履歴）②**⌘T** = LSP `workspace/symbol`（空クエリの初期 200 件 → Picker の fuzzy でローカル絞り込み・containerName + ファイル名を detail 表示・確定は `open_file_then` で背景読み完了後に着地）。**⌘⇧O のスクショ撮影済**（目視は Read 復旧後）。**このついでに重大回帰を修正**: M10-6 の open_file 非同期化で「開いてからジャンプ」系（検索ジャンプ/F12 別ファイル/⌘T）が未オープンファイルで**旧バッファに誤 reveal** → 合流ヘルパ `open_file_then` を新設し 3 箇所を修正
- [x] **診断一覧 + F8** — 2026-07-17。statusbar の ✗▲ を**クリック可能化** → 生診断（raw_diagnostics）から**ファイル別一覧を検索パネル UI で**（プレビュー行 = ✗/▲ + メッセージ 1 行目・クリック/Enter でジャンプは既存経路）。**F8/⇧F8** = アクティブファイルの次/前の診断行へ（wrap・行重複除去）。keymap 4 本追加
- [ ] **tree-sitter 多言語**: TS/TSX/JS/Python/Go/JSON/TOML/YAML/Markdown/HTML/CSS の grammar 追加（`lang.rs` `for_extension` は現状 rs のみ。コメント prefix・インデント規則も言語表へ）。LSP 側は 7 言語ルーティング済み — この非対称を解消する。受入: .ts / .py がハイライトされ ⌘/ が正しいコメント記号になる
- [ ] **増分パース + didChange 差分化**: `Tree::edit` で再解析を編集近傍に限定（512KB スキップの緩和）・LSP didChange を FULL→incremental へ。受入: 1MB の .rs で 1 文字編集のハイライト更新に体感遅延なし
- [x] **diff エディタ** — 2026-07-17。**一時タブ基盤**（`EditorTab.transient` = 永続化・⌘⇧T・LSP から除外）+ **`Buffer::set_read_only`**（edit/edit_batch/undo/redo をコアでガード = 全編集経路が構造的に止まる）。`project::unified_diff_on`（imara-diff の UnifiedDiffBuilder・HEAD vs バッファ）を背景生成 → タブ名「file.rs ⇄ HEAD」で開く（既存 diff タブは開き直し）。**F7/⇧F7 で hunk ヘッダ（@@ 行）間を移動**。入口 = gutter ポップオーバー「diff をタブで開く」+ `workspace::OpenDiff`。スクショ撮影済
- [x] **hunk 操作** — 2026-07-17。editor の **gutter diff バークリック検知**（座標→表示行→論理行→該当 hunk）→ `HunkClicked` emit → ポップオーバー（**stage / 巻き戻し / コピー / diff をタブで開く**）。stage = `hunk_patch_text`（1 hunk 分の unified diff を組む）→ `apply_patch_to_index_on`（一時ファイル経由 `git apply --cached --unidiff-zero` = host 汎用）。巻き戻し = バッファ上で HEAD 行に置換（**undo 可**）。**検証: 実 git repo で「2 hunk の片方だけ stage → staged に +zero のみ・unstaged に +THREE が残る」round trip を unit test 化** = 受入そのもの。green
- [x] **blame（インライン）** — 2026-07-17。`project::blame_line_on`（`git blame --porcelain -L n,n`・「作者, YYYY-MM-DD • 要旨」・未コミット行は「未コミット」・日付は依存なしの civil 変換 + unit test）。workspace が**キャレット行の変化だけ**を検知（同一行の blink は無視）→ 400ms デバウンス + 世代番号 → 背景 blame → `EditorView::set_line_annotation` → **行末に dim 表示**（最終セグメントのみ・幅が入る時だけ）。**実履歴テスト**（この repo の README で blame 合成を検証）green。dirty バッファは HEAD 基準の近似（注記）
- [ ] 受入（総合）: 他人の TypeScript リポジトリを clone → 読む（アウトライン/参照/hover）→ 直す（rename/quickfix/フォーマット）→ hunk stage でコミット、まで Shirushi 内で完結 ← **機能は全部実装済み（各項目で個別検証済み）。通しの体感確認は人の手番**。注: TS の LSP は `typescript-language-server` がインストールされている前提（ルーティング済み）・アウトラインの tree-sitter クエリは現状 Rust のみ（TS は ⌘T = LSP シンボルで代替。クエリ追加は後続）

## M12 — AI の唯一無二（「色と通知で、並行エージェントに迷わない」）

FEATURES で later タグの checkpoint / @mention / ⌘K / Todos をここへ前倒し採用（2026-07-15 分析。タグ自体は不変更 = ユーザー管理）。

- [x] **スレッド永続化** — 2026-07-17。**Turso threads/turns テーブル**（turn 毎 INSERT の追記型・直近 200 turn 復元・updated_at 降順・archived で一覧から除外＝台帳の行は残る）。スレッドに永続 id（unix ms+連番）。**TurnEnded/Failed で未永続 entries を追記 + メタ upsert**（失敗時は進めない=次回リトライ）。起動時 `set_storage` で mock 種を置き換えて復元（無ければ種のメタだけ登録・mock 会話本文は書かない）。継続送信 = 新セッションで続き（ACP session/load は不使用・ROADMAP 許容のフォールバック。resume 調査は残メモ）。**検証: 実 Claude Code で送信 → user/agent/step の turns 行・トークン実測 26.4k の永続化を sqlite3 で確認**（storage unit test も round trip/limit/archive/台帳）
- [x] **チェックポイント / 巻き戻し** — 2026-07-17（**方式 = content-addressed ブロブストア。比較表 → DECISIONS 決定ログ**）。`storage`: checkpoints/checkpoint_files テーブル + **SHA-256 blob**（`blobs/<hh>/<hash>`・重複排除・tmp→rename・依存なし SHA-256 は FIPS ベクタ test）。**記録は PermissionRequest 受信時 = 書かれる前**（手動承認/AUTO_ALLOW の一本道）。**old_text が diff に無いツール（Claude の Write）はディスクの現内容を背景で読んで変更前として保存し、自動許可の応答はスナップショット完了後**（= エージェントが書く前に読む・レース防止）。transcript に「⟲ checkpoint」行 → クリックで blob から全ファイル書き戻し（背景・watch が開バッファを自動リロード）。**検証: 実 Claude Code で「元の内容です」→ エージェントが「工業」に書換 → blob に変更前が保存 → restore → ディスクが「元の内容です」に復帰**の全往復を live 実証。unit test（round trip・重複排除で blob 2 個・新規ファイル=None→削除）
- [x] **エージェント編集の生中継** — 2026-07-17。即反映 = **M10-5 watch がそのまま担う**（外部変更の自動リロード・live 検証済）。帰属表示 = `PanelEvent::FilesTouched` → workspace が該当ファイルの開いているエディタへ `set_agent_mark_color(スレッド色)` → **gutter の diff バーがスレッド色**に。bypass モード（権限リクエスト無し）は FilesTouched が出ないため色は付かない＝リロードのみ（既知の限界・diff が来ないので変更前も取れない。注記）
- [x] **スレッド⇄成果物の色リンク** — 2026-07-17（コア部分）。`Thread.touched_files`（承認時に記録・`touched_color_for`/`active_touched_files` API）→ workspace の `agent_touched` map → **ツリー行にスレッド色ドット**。ターン完了トーストに「触ったファイル n」。**残: タブ側ドットとチップ→diff 導線は後続**（ツリードットで受入の判別は可能）
- [x] **ターン終了サマリー + 通知** — 2026-07-17。`PanelEvent::{TurnEnded, PermissionWaiting}` → ①**右下トースト**（スレッド色枠・5 秒自動消滅・最大 4 枚・サマリー = 経過秒 + 触ったファイル数）②**statusbar に権限待ちスレッドの pulse ドット**（クリックで Agent パネルへ・TurnEnded で消える）。**残: Dock バッジ**（gpui の badge API 要調査・±行サマリーも後続）
- [x] **diff レビューの本体化** — 2026-07-16。承認カードに「**エディタで開く（diff タブ）**」→ `PanelEvent::OpenDiffRequest`（old/new テキスト）→ workspace が `unified_diff_texts` で **read-only の transient diff タブ**を開く（`pending_transient_tab` 経由 = subscribe に window が無い GPUI 制約の迂回。render 冒頭で消化）。カードの compact diff は要約として残しつつ、大きな編集はタブで全変更を見てから accept/reject を押せる。検証: 結線のコードパス確認（diff タブ描画自体は M11-9 で offscreen 実証済み）
- [x] **@mention の完成（コア）** — 2026-07-16。`＋context` を **fuzzy 絞り込み**化: 開くと入力フォーカス（`context_query`/`context_focus`）・タイプでサブシーケンス一致（`fuzzy_matches`）・Enter で先頭候補を添付・候補は全ファイル（`all_files` 上限 60→**2000**）。受入の「3 秒で @ 参照」は fuzzy で成立。**残: フォルダ / 選択範囲 / ターミナル出力の mention は後続**（ファイル mention が本丸・送信時 `@path` 先頭付与は M4 から）
- [x] **⌘I インライン編集** — 2026-07-17。**キーは ⌘I**（⌘K は cmd-k cmd-t / cmd-k cmd-i のコードプレフィクスと衝突するため変更・グローバル bind でターミナルでも効く）。**方式は `claude -p` 型**（ai_commit_message と同型。当初案の ACP 経由から変更 — セッション起動コスト無し・権限フロー無しで「チャットへ行かない最短経路」の目的を最短で満たす。指示+コードは**一時ファイル経由**で shell 引用安全）。選択範囲（無ければ現在行）→ 指示入力バー → `project::inline_rewrite_on`（コードフェンス剥がし・末尾改行を元に合わせる）→ **その場に diff プレビュー**（-旧/+新・中央省略・`inline_edit_diff_lines` は unit test）→ **適用 ⏎ / 破棄 esc**（1 Transaction = ⌘Z 一発・バッファ version ガードで適用時不一致は安全側破棄）。**ターミナル同型**: ターミナルフォーカス中の ⌘I = 自然言語→zsh コマンド 1 行生成→**入力行へ挿入のみ**（実行はユーザーの Enter・`TerminalView::insert_text` は改行を落とす）。**検証: 実 claude で「Result<u16, String> を返すように」の全往復を live 実証**（`SHIRUSHI_INLINE_PROBE`: busy 表示 → diff プレビュー offscreen 目視 → `SHIRUSHI_INLINE_ACCEPT=1` で適用+保存 → ディスクが map_err 版に書き換わったのを確認）。ターミナル側の対話確認は人の手番。既知: 初回検証 1 回だけ「提案が自動適用に見えた」事象（ディスク不変・再現せず・監視）
- [x] **Todos（プラン）表示** — 2026-07-17。ACP `SessionUpdate::Plan` → `AgentEvent::Plan(Vec<PlanItem>)`（content/status の UI 非依存型）→ Thread.plan に**全量置換**（ACP 仕様通り）→ **transcript 上部の常設チェックリスト**（`render_plan`: ヘッダ `Todos n/m`・`☒` 完了 fg2 / `●` 進行中 **スレッド色** / `☐` 未着手。プランが無ければ非表示）。UI-SPEC §5 の「ステップ内 ☒/☐ 表示」から**常設へ仕様更新**（文書先行で修正済み）。検証: `SHIRUSHI_PLAN_PROBE` で 3 状態を注入し **● がスレッド色で付くのを offscreen 目視**（受入の表示要件）。実 Claude Code adapter が Plan を送るかはターン内容依存のため live は自然発生待ち（イベント配線は ACP 型で保証）
- [x] **Todo ボード** — 2026-07-17。真実 = **`.shirushi/todos.md`**（`project::todos`: 行番号保持パース・トグルは該当行の `[ ]`↔`[x]` だけ書き換え・他は 1 バイトも動かさない。unit test 4 本）。**レール ☑**（Lucide square-check・`RailSettings.todos`）→ 左カラム板（explorer/git と排他）: 日付見出し・`☐/☑` クリック = **ファイル書き換え**（= 他の書き手と同じ経路）・**▶ でアクティブスレッドへ送信**（「完了したら todos.md の該当項目を [x] にせよ」自動付与・実行中は**スレッド色 pulse ドット**・TurnEnded で解除+再読込）。**watch が .shirushi/todos.md を検知 → 板を自動再読込**（どの書き手でも追従）。**✨ 今日の計画** = `todos::daily_plan_on`（git status+ROADMAP+未消化を `claude -p` へ・出力ノイズ防御 `parse_plan_lines` は実測ベースの unit test・`date +%F` で今日見出しへ追記）。**検証（live・全部実 Claude）**: ①外部プロセスの追記が watch 経由で板に出るのを offscreen 目視 ②**▶ 送信 → Claude が parse_port を改善 → 自分で todos.md を `[x]` に書き換え → 板のチェックがひとりでに入り未消化 4→3・pulse 解除** ③その一部始終が **git diff に `- [ ]`→`- [x]` として残る**のを確認 ④✨で 3 タスクが今日の節へ追記。**残: 逐次消化モード**（解禁条件 checkpoint は済・UI トグルは次スレッド）・手動チェックの対話確認は人の手番・`shirushi mcp` todos ツールは任意のまま
- [x] **`.shirushi` の色/アイコン + 色ピッカー** — 2026-07-16。`read_project_identity`（`.shirushi/settings.json` の `color`(hex)/`icon`(絵文字) を `parse_hex_color` で読込・プロジェクト slot へ配線 = レールのモノグラム/色に反映）+ **レール右クリック → 色ピッカー**（パレット 12 色・クリックで `persist_user_value` により `.shirushi/settings.json` へ保存 → 再起動後も貫通）。受入経路（色/絵文字 → レール/ピル/キャレット）はプロジェクト色の既存貫通に乗る
- [x] **プロジェクト色を Peacock 相当に拡張**（M12-11 の続き）— 2026-07-19。①**パレット一本化**: `theme_core::IDENTITY_PALETTE_HEXES`（巡回5色 + 予約色と衝突しない厳選5色 cyan/chartreuse/violet/magenta/graphite）を新設し、ピッカーのシンタックス色寄せ集め12色（Claude バレット/スレッド色/選択面と衝突）を撤去 = 巡回もピッカーも出所を1本化。②**任意 hex 入力**: ピッカーに rename 式の hex 入力行（`parse_hex_color`→既存 `apply_project_color`・6 桁揃いで確定・不正は無視）。③**導線**: `ProjectColor` アクション + **⌘K⌘C**（⌘K⌘T に並置）+ `command_entries` に 1 行（コマンドパレット「表示: プロジェクトの色を変更」・キーボード起動は `open_project_color` がアクティブなレール項目にアンカー）。④色適用の波及を**全ペイン（タブ + 分割）**へ（旧: アクティブ 1 枚）。**remote 別色は入れない判断**（project=slot が固有色を持ち Peacock の remoteColor 問題が起きない・「どのマシンか」の識別は §1.3 の窓縁 2px 枠が担う）。i18n `cmd.project_color`/`color.hex_placeholder` を ja/en に。検証: `cargo test`（ja/en parity 含む）green + **offscreen 目視**（10 スウォッチ識別可・hex 行 `# rrggbb`/`# 34d3b6`・ピッカーがアクティブ項目にアンカー・`SHIRUSHI_COLOR_PICKER` プローブ追加）。**残: ライブの ⌘K⌘C / hex 確定の対話確認は人の手番**
- [x] **⌘O の 2 階層化 + worktree ダッシュボード** — 2026-07-17。UI-SPEC §7 通り**1 リストの 2 階層**: プロジェクト行（**行頭●プロジェクト色** + 名前 + パス）+ 配下にインデントで **`⎇ branch` 行**（worktree 名・`✓`現在・`↑ahead ↓behind`・dirty `●`・**右端に実行中スレッドの色ドット列**）。開いた瞬間はプロジェクト行のみ → **背景で全プロジェクトの `git worktree list` + `git status --short --branch`**（`worktree_status_on`・パーサ `parse_status_branch` は unit test）→ `Picker::set_items` で差し込み（開く速さを守る）。**実行中ドットの正体 = `RunningRegistry`（GPUI Global・全窓横断）**: 各窓の AgentPanel が dest_cwd キーで (名前,色,running) を送信開始/TurnEnded で上書き → どの窓の worktree で何が走っているかを ⌘O 一発で一望。確定: プロジェクト行=切替（従来）/ worktree 行=レールに居れば切替・居なければ**新窓で開く**（ウィンドウモデル §5）。PickerItem に `accent`（行頭●）/`dots`（右端色ドット列）を追加。**検証: 実 Claude を走らせながら ⌘O を開き、`⎇ main ✓●` に実行中スレッド色ドット・`⎇ feature` に dirty ● が並ぶのを offscreen 目視**。**残: 最下部「＋ worktree を作って開く」行と ⌘⏎ 新窓**（⎇ メニューの既存導線で代替可）・3 worktree 並行の体感確認は人の手番
- [x] **トークン台帳** — 2026-07-16。`storage::token_ledger`（turns テーブルの集計クエリ・別ストア不要）→ スレッドタブ列の左端に **Σ 累計チップ**（アクティブプロジェクトの全スレッド累計）。検証: unit test（集計クエリ）+ 実 DB の Σ 23.4k 表示を offscreen 目視
- [x] **スレッド名: 改名 + AI 自動命名**（Claude 拡張風）— 2026-07-19。タブ **ダブルクリック**で `EditorView::plain`（IME 正しい）のインライン改名（`Thread.name_is_custom` で以後の自動命名から保護）+ 初回ターン後の **`claude -p` 自動命名**（`project::name_thread_on`・既定名かつ未手動改名のみ・`settings.agent_auto_name` 既定 on・`is_placeholder_name` で再命名防止）。offscreen でタブのアクセント枠入力を目視。**live（IME 改名・実 claude 命名）は人の手番**。詳細 JOURNAL 2026-07-19。
- [x] **スレッド履歴ビュー**（Claude 拡張風）— 2026-07-19。`storage::load_all_threads`（archived 含む・unit test）+ Agent タブ列 **🕘 ボタン / ⌘⇧H / パレット「AI: スレッド履歴」** → `PickerMode::ThreadHistory`（`open_picker` 再利用・行頭●スレッド色 + Σ トークン detail・updated_at 降順）→ 選択で復元（`open_thread_from_history`・開いてれば切替）。offscreen で 3 スレッド色付き一覧を目視。⌘⇧T（直近1件）の全件ブラウズ版。
- [x] 受入（総合・当初ビジョンの完成形）— 2026-07-17 **機構完成**: worktree 並行（⎇/⌘O 新窓）× スレッド色貫通 × 通知（トースト/pulse）× diff レビュー（承認カード→タブ）× checkpoint 巻き戻し（live 実証済）× **⌘O ダッシュボードで「どこで何が走っているか」一望**が全部繋がった。各要素は個別に live 実証済み（M12-2/3/5/8/10/12 の検証欄）。**worktree×3 の 3 スレッド並走を色と通知だけで捌く「体感」の最終確認は人の手番**（M9 受入と同じ扱い）

## M13 — 公開準備（「英語話者が DL して 10 分で使い始める」）

旧 M9 の未チェック残件をここへ統合（2026-07-15 判断: 日常機能 M10〜M12 を先に）。

- [x] **コマンドパレット ⌘⇧P + CommandRegistry** — 2026-07-17。`command_entries()` 登録表（(i18n キー, アクション名) 39 個 — 新アクションは 1 行足すだけ）→ Picker 再利用で「カテゴリ: 名前 + **キー併記**」（`keymap_core::key_for_action` 逆引き + `pretty_keystroke` 記号化 ⌘⇧P・unit test）。確定 = **パレットを閉じてから `cx.build_action`→`window.dispatch_action`**（フォーカス復帰後 = Editor/Workspace コンテキストで解決）。**検証: live で「Terminal」を確定 → 下ドックに実シェルが開く**（dispatch 経路の実動）+ 一覧描画 offscreen 目視（`SHIRUSHI_PALETTE_PROBE`）。StatusItemRegistry は未（statusbar は現状直書き・残件）
- [x] **Workspace ownership refactor** — 2026-07-20。約 13,000 行・約 95 直接フィールドの `workspace.rs` を、8フィールドの shell + `ProjectSessions` + 長寿命 `ProjectSession` + `EditorArea` + feature Entity へ再編。`explorer` / `git_ui` / `search_ui` は workspace 非依存、child subscription は `PanelRegistry`、palette action は `CommandRegistry` に集約。通常の rail 切替は active index だけを変更し、dirty/undo、Editor Entity、Agent session、Terminal process を保持する。root Render の pending effect は effect-cycle 末尾へ遅延し、Host/FS/Git/DB を描画中に呼ばない。`workspace.rs` は合計 1,399 行（production body 975 行、残りは完了監査 test）。`d83578a` で公開 API/actions/i18n/state 互換、A→B→A の dirty/undo/child Entity 保持、local/remote root draw の Host call 0、release probe 不在を自動検証し、9 UI 状態を offscreen 目視済み。全 workspace test / release check green。詳細は [`REFACTOR-WORKSPACE.md`](./REFACTOR-WORKSPACE.md)。
- [x] **i18n の回収** — 2026-07-17。ハードコード UI 文字列 **98 箇所**を `t!` 化（レール/エクスプローラ/検索/git/タブ/ターミナル/dock/テーマ/agent パネル。変数付きは `%{name}` 置換）+ パレット 39 コマンド名 + welcome/update 系 = **locales は計 ~250 キー**（ja/en parity テスト green）。**検証: `locale=en` 実機スクショ**（git パネル = Source control/✓ Commit/History・agent = Idle/Send ⌘⏎・Terminal 1 等が英語）。対象外と判断: eprintln 開発ログ・seed スレッドのモック会話・ユーザーデータ
- [x] **入力レイテンシベンチ（コア分 + CI ガード）** — 2026-07-17。`editor_core/examples/bench_editor`（依存ゼロ Instant 直測・insert/undo/snapshot/byte_to_point・**予算超過で exit 1**）を **CI（ci.yml）で release 実行** = 予算超過で CI が fail する。実測は全項目 ~1µs で予算内。起動時間はローカル計測スクリプト（`scripts/startup-time.sh`・SHIRUSHI_STARTUP_LOG 対応済み）。**残: key→frame ヒストグラム（zed input_latency_ui 相当）は GUI 実機が要るため CI 外**（人の手番・実機で回す）
- [x] **自動アップデート** — 2026-07-17（自前・velq/karui 同型）。`workspace::updater`: 起動 90s 後に GitHub Releases latest を確認（curl・失敗は静か・SHIRUSHI_NO_UPDATE_CHECK/スクショ時はスキップ）→ 新版なら **statusbar に「⬆ vX.Y.Z に更新」チップ** → クリックで .dmg DL → **Apple 署名/公証を検証**（`spctl --assess` — CI が codesign+notarize 済みなので自前鍵不要）→ `hdiutil`+`ditto` で実行中 .app を差し替え →「⟳ 再起動で反映」。semver 比較・release JSON パースは unit test（prerelease/draft 除外・.dmg アセット抽出）。チップ描画は offscreen 目視（`SHIRUSHI_UPDATE_PROBE`）。**残: 実 GitHub Release での E2E（初回リリース時に確認）**
- [ ] **Linux ビルド**（部分）: **CI に check-linux ジョブを追加**（ubuntu + Wayland/X11 依存 apt → `cargo check`・安定するまで continue-on-error）— 2026-07-17。**残: 初回 CI 実行での追従修正・実 Ubuntu での起動/編集/保存/ターミナル確認（実環境必須）**
- [x] **ターミナル仕上げ** — 2026-07-17。**file:line リンク**: `find_path_links`（cargo/rustc/grep 形式の手書きパーサ・unit test）→ prepaint で表示セルから行再構成 + 検出 → **下線描画** + paint 登録の mouse listener（セル座標→リンク hit）→ `TerminalEvent::OpenPath` → workspace が root 基準で解決し **open_file_then で該当行へ**（`--> sample.rs:28:9` の下線 + 28 行目着地を offscreen 実証）。**IME**: `EntityInputHandler` 最小実装（確定文字列を PTY へ・候補ウィンドウはカーソル位置 = 日本語入力が可能に）。**残: 実クリックの hit 判定と IME の対話確認は人の手番・変換中テキストのインライン表示は後続**
- [ ] Remote（旧 M9・**実環境必須のため残件** — 実 Linux ホスト/長時間運用が要る。機構は M9 で完成済み）: local/remote の latency・CPU・memory benchmark と性能予算の自動検証
- [ ] Remote（旧 M9）: Linux x86_64/aarch64 musl artifact・署名/checksum・古い server cleanup・GUI askpass
- [ ] Remote（旧 M9）: dirty buffer crash backup（M10 hot exit の remote 版）・watch subscription/event/cancel（M10 watch の remote 版）・再接続後の LSP/PTY handle 再同期
- [ ] Remote（旧 M9）: sleep/VPN断/ControlMaster kill/server kill/巨大 tree の実ホスト障害注入 test
- [ ] Remote（旧 M9）: Remote Projects UI・SSH config picker・構造化接続ログ・retry/cancel・localhost port forwarding
- [ ] Remote（旧 M9）受入: remote Linux で編集/Git/LSP/terminal/agent を一日使い、切断復帰しても未保存変更を失わない
- [x] **初回起動体験** — 2026-07-17。空状態 = 「Shirushi — 色による方向感覚」+ **最初の 4 手のキーバッジ**（⌘O 開く / ⌘P ファイル / ⌘⇧A AI スレッド / ⌘⇧P 全コマンド・i18n 両対応・offscreen 目視）。README に **英語 Quick start** + hero スクリーンショット（docs/images/）+ 古い記述の修正（GPUI git 依存・ライセンス戦略）
- [ ] **設定画面（`render_settings`）の完成** — 現状はエージェント選択（既定/導入/ログイン + アイコン）のみで**全然未完成**。真実は settings.json、画面はそれを操作するだけ。段階的にトグル/UI 化する:
  - `completion_sound`（完了音 on/off・既定 on・**実装済み** 2026-07-19 → 画面トグルだけ未）
  - `agent_tabs_view`（Bar/List・**永続化は実装済み** 2026-07-19 → 画面トグルは任意）
  - `submit_on_enter` / `soft_wrap` / `format_on_save` / `agent_auto_name` / `density` / `font_size` / `theme` / `locale` / `rail.*`（既存設定の UI 化）
  - **エージェントアイコンの出所**: repo にロゴを同梱せず **ACP レジストリから実行時取得**（Zed 方式・未知は sparkles フォールバック）＝ DECISIONS の「再ライセンス自由」と両立。手描き monochrome 同梱が保守的な代替
- [ ] **完了音の磨き**（後続・優先度低）: ①**window 非フォーカス時のみ**鳴らす（見てる最中は鳴らさない）②**独自チャイム同梱**（macOS `Glass.aiff` → 自前 wav + `rodio` でクロスプラットフォーム）。**やるのはクロスプラットフォーム化 or 公開ブランディングのタイミングで十分**（今は system sound で機能する）
- [ ] **エージェントタブ Grouped ビュー**（要設計判断）: List の第3案 = プロジェクト⎇ブランチで束ねる縦リスト。**現モデルは「1 パネル=1 プロジェクト」**で同一パネル内スレッドは同一 dest のため、①**全ウィンドウ横断集約**（`RunningRegistry`・⌘O ダッシュボードの右ドック常設化）か ②**スレッドが別ブランチ dest を持てるモデル拡張**、のどちらを取るか決めてから着手
- [ ] 受入（総合・**人の手番**）: 英語話者が GitHub から DL → 10 分で「開く・編集・保存・検索・AI に 1 タスク」まで到達できる。**ソフト側の導線は完備**（署名 dmg CI・自動更新・EN UI・welcome・⌘⇧P）— 残るは初回リリースを切って実際に通しで試すこと

---

以降（later）: FEATURES.md のタグに従う（拡張モデル ADR・WASM ホスト・minimap・multibuffer 本体・vim・テーマ/言語パック配布 等）。
